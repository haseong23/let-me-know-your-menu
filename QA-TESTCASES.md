# QA 테스트케이스 & 결과 — v10.13

**이 회차 결론: 자동 128건(순수 로직 44 + 화면·플로우 84) 전부 통과.** v10.09 점검에서 연 결함 5건은 모두 고쳤고, 고치는 도중 드러난 선행 버그 1건(§2 F-2 ③)도 함께 닫았다. 남은 것은 실기기 판독이 필요한 **U-2**(사다리 10명 라벨 7.1px)와 사소한 **U-3**(320px 일괄 버튼 두 줄) 둘이다.

이 문서는 v9.x 회차별로 쌓아 오던 앞 판을 **v10.09 기준으로 다시 쓰고, v10.10 수정 결과를 반영한 것**이다. 회차 로그가 아니라 *지금 코드에 대해 무엇이 참인가*를 적는다. 지워진 케이스와 그 이유는 §7 에 있다. 앞 판 전문은 `git show 64fd54f:QA-TESTCASES.md` 로 볼 수 있다.

---

## 1. 검증 방법

| 층 | 무엇을 | 어떻게 | 결과 |
|---|---|---|---|
| 순수 로직 | 사다리 기하·가중 추첨·돌림판 물리·레이스 물리·기간 프리셋 | `node --test tests/*.test.mjs` | **44/44 통과** |
| 화면·플로우 | 입장부터 마감까지 실제 DOM 을 클릭해서 | 헤드리스 Chrome + CDP, 로컬 사본 | **84/84 통과** |
| 눈으로만 보이는 것 | 캔버스 그림·회전 각도·라벨 크기 | 스크린샷 + 합성 행렬 실측 | §2 참조 |
| 실기기 | 멀티터치·모바일 키보드·백그라운드 복귀·폴링 | 사람이 밟는다 | §5 체크리스트 |

**로컬 사본 규칙**: `index.html` 을 복사해 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 만 빈 문자열로 바꾸고(→ `SHARED=false` → `LocalSync`), supabase CDN 과 PostHog 스니펫 두 줄을 뺀다. 그 밖의 바이트는 손대지 않는다 — 그래야 테스트가 실앱을 본 것이 된다. **실데모·실셀에는 어떤 쓰기도 하지 않는다.**

관리자 대시보드(`?admin`)와 셀 대시보드(`?cell=…&view=dashboard`)는 Supabase RPC 가 있어야 그려지므로 로컬 사본에서는 **기간 계산 함수만** 검증했다(H3·H4·H5). 화면 자체는 §5 실기기 몫이다.

---

## 2. 연 결함과 처리 결과

우선순위는 "몇 명이 언제 부딪히는가"로 잡았다. **F**=기능 · **U**=UI · **X**=UX.
각 항목의 마지막 줄이 v10.10 에서 무엇을 했는지다. `열림` 은 아직 안 고친 것이다.

### F-1 · 돌림판 이름·번호가 뒤집혀 나온다 (치명) — ✅ v10.10 수정

| | |
|---|---|
| 증상 | 판이 도는 내내, 그리고 **멈춘 뒤에도** 조각 안의 이름(또는 번호)이 판과 같이 돌아 거꾸로 서 있다. 4명이면 이름 넷 다, 24명이면 번호 24개 다 뒤집힌다. |
| 실측 | 판이 화면 각도 **135.1°** 에서 멈췄을 때 라벨 넷의 화면 각도도 전부 **135.1°**. 역회전이 **0°** 만큼 먹었다(= 아예 안 먹었다). |
| 원인 | `index.html:632` 의 `.wlab { … transform:rotate(0deg); }` 는 **CSS 속성**이고, `playWheel` 의 `paint()` 가 매 프레임 넣는 `el.setAttribute("transform", "rotate(-deg …)")` 는 **presentation attribute** 다. SVG2/CSS Transforms 규칙상 CSS 속성이 이긴다 — 그래서 라벨은 언제나 `rotate(0deg)` 로 고정되고, 부모 `#wRot` 의 회전을 그대로 뒤집어쓴다. |
| 곁가지 | 같은 블록의 `@keyframes wspin`·`wlabspin`(637–638행)은 **아무도 안 쓴다**. v10.08 의 CSS 애니메이션 경로가 v10.09 에서 rAF 재생으로 바뀌면서 남은 것이고, 632행 규칙도 그 경로를 위한 것이었다. |
| 영향 | 돌림판은 "핀 아래 조각이 누구인지"를 눈으로 확인하는 게임인데 그걸 못 한다. 결과 발표 텍스트가 없으면 누가 걸렸는지 알 수 없다. v9.72 의 **G6** 와 v9.81 의 **R6-3** 이 같은 자리로 회귀했다. |
| 케이스 | **F4 · F6** |
| **조치** | 스타일시트의 `@supports (transform-box: fill-box){ .wlab{…} }` 블록과 쓰이지 않는 `@keyframes wspin`·`wlabspin` 을 지웠다. SVG 의 `rotate(a cx cy)` 는 회전축을 인자로 직접 받으므로 `transform-box`·`transform-origin` 도 필요 없다. 지운 자리에 **왜 여기에 CSS transform 을 두면 안 되는지**를 적어 뒀다 — 지우기만 하면 다음 사람이 같은 규칙을 다시 넣는다. |
| **검증** | 판이 화면 각도 131.8°·-19.2°·163.2° 로 멈춘 세 판 모두 라벨 화면 각도가 **0.0°**. 24명 번호도 전부 똑바로. |

### F-2 · 직접입력이 두 글자 이름을 엉뚱한 기존 메뉴로 가로챈다 (높음) — ✅ v10.10 수정

| | |
|---|---|
| 증상 | `🥤 직접 입력`에 `우유`를 치면 주 버튼이 `✋ 이미 있는 메뉴예요 — 🥜 우도땅콩라떼로 주문` 으로 **통째로 바뀐다**. 그 이름으로 커스텀 메뉴를 담을 길이 화면에서 사라진다(우회 버튼이 없다). |
| 실측 | 실제로 나올 법한 이름 31개 중 **10개가 가로채인다** — `우유→우도땅콩라떼` · `두유→작두콩차` · `생수→홍삼생강차` · `녹차→제주말차라떼` · `말차→제주말차라떼` · `레몬→에스프레소` · `밀크티→콜드브루 밀크 블렌디드` · `아이스티→복숭아 아이스티` · `포도주스→청포도에이드` · `흑임자라떼→홍삼진자라떼` |
| 원인 | `openCustomSheet` 의 `exactMatch`(1854행)가 `qn.length>=2` 에서 `fuzzyDist ≤ typoThreshold` 를 쓴다. `fuzzyDist` 는 긴 이름 안을 **질의 길이만큼 슬라이딩**하므로 `생수` 는 `홍삼생강차` 의 `생강` 창과 편집거리 1 이다. 바로 위 `matchesName`(1810행)은 **이미 이 함정을 알고** `qn.length>=3` 으로 막아 놨고 주석(1811–1812행)에 근거까지 적혀 있는데, 직접입력 경로만 그 규칙 밖에 있다. |
| 영향 | 이 앱에서 `우유`·`생수`는 실제로 자주 나오는 요청이다. 판정이 두 곳으로 갈려 있다는 게 더 큰 문제 — 검색창은 맞게 동작하고 직접입력만 틀린다. |
| 케이스 | **D7** |
| **조치 ①** | 오타 허용을 3글자부터로 — `matchesName` 이 이미 정해 둔 규칙에 이 경로를 맞췄다. |
| **조치 ②** | 비교를 `fuzzyDist`(질의 길이만큼 슬라이딩) 에서 `editDistance`(이름 전체 대 이름 전체) + **길이 차 1 이내** 로 바꿨다. '오타'는 길이가 비슷하고, '남의 이름 속 조각'은 그렇지 않다. 느슨한 후보는 아래 제안 목록이 그대로 보여 준다 — 막는 게 아니라 옮긴 것이다. |
| **조치 ③** | *고치다 드러난 선행 버그.* `pool.find` 로 한 번에 훑어서 **메뉴판에 먼저 적힌 것이 이겼다** — `돌체라떼` 를 치면 이름이 똑같은 메뉴를 두고 앞줄의 `카페라떼`(거리 2)가 잡혔고, `바닐라라때` 는 거리 1인 `바닐라라떼` 대신 거리 2인 `바닐라빈라떼` 가 잡혔다. 완전 일치를 먼저 훑고 오타 후보 중에서는 최근접을 고르는 `bestExisting()` 으로 갈랐다. |
| **검증** | 실제 시트에 20개를 쳐서 가로채임 **0건**, 진짜 오타 6종(`아메리키노`·`카폐라떼`·`바닐라라때` 등)은 그대로 잡는다. `우유` 는 커스텀으로 담기고, `밀크티` 는 버튼이 `추가하고 주문` 이면서 제안에 `콜드브루 밀크 블렌디드`·`로얄밀크티`·`밀크쉐이크` 가 뜬다. |

### F-3 · 구슬 레이스에서 자기 구슬을 못 찾는다 (높음) — ✅ v10.10 수정

| | |
|---|---|
| 증상 | 출발 화면이 `준비됐어요 — 내 번호를 찾아두세요` 라고 말하는데 **구슬 어디에도 번호가 없다.** 아래 범례와 HUD 상위 3명은 `5. 박다온` 처럼 번호로 말하므로, 두 표시가 서로 안 이어진다. |
| 실측 | 구슬 지름 **6.5 CSS px**(390px 화면). `draw()` 는 원을 채우고 테두리만 긋는다 — `fillText` 로 번호를 그리는 코드가 없다. |
| 딸린 것 ① | 이름표를 **전원에게** 그린다. 4명에서도 `김요셉 정하은이다니'박사무엘` 처럼 붙어서 잘린다. `shortName(players[i].name)`(4354행)에 **두 번째 인자 `k` 가 빠져** `n.length > undefined` → 항상 false → 절삭이 죽어 있다. |
| 딸린 것 ② | 미니맵이 캔버스 340px 중 **오른쪽 54px(15.9%)** 를 덮는다. 코스는 `base = CW / RACE_W` 로 **캔버스 전체 폭**에 그려지므로, 미니맵 폭을 빼 두겠다던 주석(4116행 "코스는 그 왼쪽에만 그린다")과 실제가 어긋난다. 오른쪽 벽 근처 구슬이 미니맵 뒤로 들어간다. |
| 영향 | v9.98 의 레퍼런스 방식 교체에서 빠진 것으로 보인다. 앞 판 TC110·TC119·TC179(구슬 흰 번호)와 TC247·TC248(10명 이하 이름표 / 11명 이상 번호)이 전부 지금 코드와 안 맞는다. |
| 케이스 | **F15** |
| **조치 ①** | 캔버스 라벨을 **인원과 무관하게 번호**로 통일하고 범례를 항상 노출한다(`gameLegendHtml(…, true)`). 사다리·돌림판처럼 `GAME_LABEL_MAX` 로 이름/번호를 가르지 않은 이유는 이 게임만 출발선에서 구슬이 한 덩어리라서다 — 자기 것을 찾아야 하는 바로 그 순간에 이름표가 가장 심하게 겹쳤다(네 명에서도 `이다니김요셉박사무최에스`). 이제 캔버스·HUD·범례가 같은 번호를 쓴다. |
| **조치 ②** | 코스 폭을 `COURSE_W = CW − 미니맵폭 − 여백`(286px)으로 잡아 주석의 약속을 실제로 지키게 하고, 화면 좌표를 쓰는 세 곳(카메라 중심·이름표·결승선 글자)을 함께 옮겼다. 더해서 **코스 그리기를 `COURSE_W` 로 클립**한다 — 결승 줌(camZ>1)이 들어가면 폭을 나눠 두는 것만으로는 코스가 다시 미니맵 밑으로 비집고 들어간다. |
| **부작용(수용)** | 코스가 340→286px 로 좁아져 구슬 지름이 6.5→**5.5 CSS px** 가 됐다. 구슬 크기를 키우려면 물리 상수 `RACE_R` 을 건드려야 하고 그러면 맵 여유·통과 폭·소요 시간이 함께 흔들린다. 식별은 이제 구슬이 아니라 번호가 지므로 이 교환을 받았다. |
| **검증** | 4명·12명·24명 모두 번호가 붙고 범례가 이어진다. 코스가 미니맵을 침범하지 않는다. 콘솔 오류 0. |

### U-1 · 헤더 이름 칩이 36px (중)

합친 화면에서 44px 미만인 탭 타겟은 `.me-chip`(`한사랑 · 변경`) **하나뿐**이었다 — 36×82px. 나머지 버튼·타일·칩은 전부 44px 이상으로 v9.53 의 B9 처방이 잘 지켜지고 있다. 이 칩만 그때 목록에 없었다.

**조치** — `min-height:44px`. `.nav-link` 처럼 음수 마진으로 줄높이를 되돌릴 필요가 없었다: `.h-row` 는 왼쪽의 두 줄(셀 이름 + 날짜) 때문에 **이미 45px** 이고 ⚙️ 도 44px 이라, 실측으로 헤더 높이(68px)와 본문 시작 위치(84px)가 그대로였다.
`gap:4px` 을 함께 넣은 것은 장식이 아니다 — flex 는 자식 사이의 공백 텍스트를 렌더하지 않아 마크업의 `이름 <small>· 변경</small>` 띄어쓰기가 사라지고 `한사랑· 변경` 이 된다.
**검증** — 칩 44×83px, 헤더 68px·본문 84px 불변, 칩 글자 `한사랑 · 변경`. 케이스 **I1**

### U-2 · 사다리 10명 라벨이 320px 에서 7.1px, 그런데 범례가 없다 (중) — ⏳ 열림

앞 판 **TC154 가 "미측정"으로 남겨 둔 값**이다. 이번에 쟀다.

| 인원 | 라벨 | 390px 화면 | 320px 화면 | 범례 |
|---|---|---|---|---|
| 10명 | 이름 2글자 | 8.9px | **7.1px** | **없음** |
| 16명 | 번호 | 10.5px | 8.4px | 있음 |

`GAME_LABEL_MAX = 10` 이라 **10명이 최악이면서 유일하게 구제 수단이 없다** — 11명부터는 번호로 바뀌고 아래 범례가 이름을 이어 주는데, 10명은 2글자로 잘린 7.1px 이름만 있다. 코드 주석(3647–3652행)이 예측한 값과 실측이 일치한다. 주석이 적어 둔 처방("안 읽히면 크기가 아니라 `GAME_LABEL_MAX` 를 9 로 내려 번호+범례 경로로 보낸다")이 그대로 유효하다.
**안 고친 이유** — 처방이 둘로 갈리는데(`GAME_LABEL_MAX` 를 9 로 내려 번호+범례로 보내기 / 라벨 크기를 올리고 `GAP`·`BOX_W` 를 다시 잡기) 어느 쪽인지는 **실기기에서 7.1px 이 읽히는지**에 달려 있다. 숫자만으로 정할 일이 아니라 남겨 둔다. 케이스 **M-14**

### U-3 · 320px 에서 참가자 고르기 일괄 버튼이 2줄로 접힌다 (소) — ⏳ 열림(동작 지장 없음)

`전원 참가 / 음료 고른 분 / 직전 섬긴 분 제외 / 전부 해제` 네 개가 320px 에서 3+1 로 접힌다(각 44px). 접혀도 동작에는 지장이 없고 시트가 한 줄만큼 길어진다. 390px 에서는 한 줄이다.
케이스 **I7**

### X-1 · 게임 네 종의 시작 규약이 갈린다 (중) — ✅ v10.10 수정

| 게임 | 출발 게이트 | 첫 문구 |
|---|---|---|
| 🪜 사다리타기 | `[▶ 시작!]` | 준비됐어요 — 내 **이름**을 찾아두세요 |
| 🏁 구슬 레이스 | `[▶ 출발!]` | 준비됐어요 — 내 **번호**를 찾아두세요 |
| 🎡 돌림판 | ~~없음 — 열자마자 돈다~~ → `[▶ 돌리기!]` | 준비됐어요 — 내 **이름**을 찾아두세요 |
| ☝️ 손가락 룰렛 | 해당 없음(손가락이 게이트) | 폰을 가운데 놓고 다 같이 손가락을 올려주세요 |

게이트를 넣은 근거는 두 게임의 주석에 같은 말로 적혀 있다 — *"열자마자 움직이면 자기 것을 찾기도 전에 전부 움직여서 누구를 보고 있었는지 놓친다."* 돌림판은 조각이 24명에서 15°까지 좁아져 그 이유가 **더** 강한데 혼자 게이트가 없었다.

**조치** — 돌림판에 `[▶ 돌리기!]` 게이트를 붙였다. 누르기 전에는 `rotate(0deg)` 로 서 있고(라벨도 rotate(0) 이라 어떤 각도도 답을 말하지 않는다 — R6 불변식 유지), 누르면 안내가 `돌고 있어요 — [그만 돌리기]를 누르면 멈춰요` 로 바뀌며 푸터가 `[⏹ 그만 돌리기]` 로 교체된다. `[한 판 더]` 도 같은 게이트에서 시작한다.
**검증** — 게이트에서 1.8초 방치해도 `rotate(0deg)`·결과 없음. 누르면 회전 시작, `[⏹ 그만]` 으로 멈추고 라벨 각도 0°. 케이스 **F16**

### X-2 · 돌림판 부제가 지금 동작과 어긋난다 (소) — ✅ v10.10 수정

게임 고르기 시트의 돌림판 부제가 `가장 빠르게 · 한 번에 끝` 이었다. v10.09 에서 돌림판은 **누를 때까지 무한히 돈다** — 가장 빨리 끝나는 게임이 아니라 사용자가 끝을 정하는 게임이 됐다.

**조치** — `직접 멈춰서 정해요 · 조각이 곧 확률` 로 바꿨다. 뒷 절반은 이 게임만 당첨자를 미리 뽑지 않는다는 사실(조각 각도가 곧 추첨)을 고르는 자리에서 알려 준다.

### X-3 · 동작 줄이기에서 돌림판만 조작 없이 결과가 튀어나온다 (소) — ✅ v10.10 수정

`prefers-reduced-motion` 에서 셋 다 연출을 건너뛰는 것은 확인했다(F14). 다만 사다리·레이스는 `[▶]` 를 눌러야 결과가 나오는 반면 **돌림판은 여는 즉시 결과**였고, 그 사이 300ms 동안 `[⏹ 그만 돌리기]` 가 깜빡였다.

**조치** — X-1 의 게이트가 이것도 함께 닫았다. `lessMotion` 갈래를 `startSpin()` 안으로 옮겨, 동작 줄이기에서도 `[▶ 돌리기!]` 를 지나야 결과가 나온다. 셋 다 '누르면 시작'으로 같아졌다.
**검증** — 동작 줄이기 켜고 열면 게이트에 서 있고 결과 없음. 누르면 0.5초 안에 결과.

---

## 3. 자동 검증 — 순수 로직 (`node --test tests/*.test.mjs`) · 44/44

한 명령으로 돈다. 아래는 무엇을 지키는지의 요약이고, 개별 단언은 테스트 파일에 있다.

- **사다리** — 가로선이 안 닿는 세로줄이 없다 · 같은 층 이웃 가로선이 안 붙는다 · 층 수 공식이 `playLadder` 와 한 곳에서 나온다 · 기하 불변식(`GAP≥18`·`BOX_W≥14` → 상한 16 의 근거)
- **추첨** — 가중 추첨 수렴(`확률⬇️` 가 다른 분의 1/3, 0 아님) · 명단 파생(`안 마심` 하드 제외, `on`/`off` 우선) · 색 배정 2~60명 중복 0 · 정책 적용 후 1명이 될 수 있음(경계)
- **돌림판** — 조각 각도가 가중치에 비례하고 360 을 빈틈없이 채운다 · 가중치가 전부 0 이면 균등(0 나눗셈 없음) · 멈추는 각도가 360도에 고르게 흩어진다 · **당첨률이 조각 각도와 맞는다(각도가 곧 확률)** · 압정을 실제로 지나고 기록 길이가 프레임 수와 맞는다 · 판이 오래 안 끈다
- **구슬 레이스** — 어떤 인원·어떤 시드로도 1등이 나온다 · 소요가 볼 만한 구간 · 구슬이 벽 밖으로 안 샌다 · 벽 기울기 하한 · **1등이 특정 번호로 안 쏠린다** · 버블이 실제로 터진다 · 순위는 y 가 아니라 도착 순
- **기간** — `weekBackDays` 가 월~일 전부에서 0~6, 시작일이 항상 월요일, 길이 1~7일 · `q3Start` 가 1·2월에 전년으로 넘어가고 길이 92일 이하

---

## 4. 자동 검증 — 화면·플로우 · 84/84

`✔` 통과. 괄호 안은 §2 의 어느 결함을 지키는 가드인지다.

**A. 입장 · 본인 선택**
- ✔ **A1** 부팅 — 푸터·콘솔 버전이 `v10.09`, 첫 화면이 본인선택
- ✔ **A2** 구성원이 ㄱㄴㄷ순, `내 이름이 목록에 없나요? / 추가하고 시작` 있음
- ✔ **A3** 이름 고르기는 **2단계** — 행을 누르면 `✓ 선택됨`, `🙋 '○○님'으로 시작하기` 를 눌러야 확정. 섬김 없는 화면에 `섬길 사람은 주문을 받은 뒤 🎲 게임으로 정할 수도 있어요` 한 줄이 있다

**B. 합친 화면 (`[주문 · 현황]`)**
- ✔ **B1** 탭이 `[주문 · 현황]`·`[카페 주문서]` 둘뿐
- ✔ **B2** 순서가 배너 → 탭 → `☕ 내 주문` → 메뉴 그리드 → `음료를 안 드신다면` → 게임 → `👀 현황` → `다른 분들`
- ✔ **B3** 390 / 360 / 320px 전부 가로 넘침 0
- ✔ **B4** 머리말 카드가 상태(주문 없음/주문함/안 마심)와 무관하게 104px — 탭 줄 top 이 세 경우 모두 202px
- ✔ **B5** `[카페 주문서]` 탭도 같은 머리말 — 탭 줄 세로 위치 동일(202px)
- ✔ **B6** `다른 분들` 로스터에 내가 없다(내 주문 카드와 중복 0)
- ✔ **B7** 주문한 남의 행에 `[나도]` — 누르면 그 메뉴로 내 주문이 바뀐다

**C. 주문**
- ✔ **C1** 타일 → 주문 시트 → `[이걸로 주문]` 저장
- ✔ **C2** 주문 후 `[🔄 다른 메뉴로][🙅 안 마실게요]` 두 버튼, 각 49px
- ✔ **C3** `안 마실게요` → 확인창(`취소` / `네, 안 마실게요`) → 상태 카드가 `안 마시기로 하셨어요`
- ✔ **C4** 안 마심 상태에서도 메뉴 그리드가 열려 있어 되돌릴 수 있다
- ✔ **C5** `지난번엔 이렇게 드셨어요` 줄이 메뉴 그리드 **위**에 뜨고 메뉴·온도·옵션·메모가 그대로
- ✔ **C6** `[이걸로]` → 그 값으로 채워진 주문 시트(옵션 칩 `덜달게` 선택된 채)
- ✔ **C7** 지금 담긴 주문과 같으면 그 줄이 안 뜬다
- ✔ **C8** 메뉴판에 없는 지난 메뉴면 줄 자체가 안 뜬다(빈 카드·비활성 버튼 없음)

**D. 전체 메뉴판 · 직접입력**
- ✔ **D1** 시트 하단이 `[✍️ 직접 입력][닫기]` 한 줄
- ✔ **D2** 검색창이 `type=search` + `autocomplete/autocorrect/autocapitalize=off` + `spellcheck=false` + `enterkeyhint=search`
- ✔ **D3** 초성 검색 `ㅇㅁㄹ` → 아메리카노 1개
- ✔ **D4** 무결과 안내(`검색 결과가 없어요`) → 지우면 76개 복원
- ✔ **D5** 직접입력으로 커스텀 주문 저장(`c:흑임자수정과`)
- ✔ **D6** 커스텀 메뉴가 카페 주문서에 들어간다
- ✔ **D7** 20개를 실제로 쳐서 가로채임 0건 · 진짜 오타 6종은 그대로 잡는다 (F-2)

**E. 게임 고르기 시트**
- ✔ **E1** 합친 화면에 진입점 `#gamepick` **정확히 하나**. 라벨이 상황에 따라 셋으로 갈린다 — 호스트 `섬길 사람을 다시 정하려면` / 호스트 없음 `누가 섬길지 아직 안 정했다면` / 남이 호스트 `게임이 궁금하면 (연습)`
- ✔ **E2** 게임 4종이 `🎯 확률 조정 가능`(사다리·돌림판) / `🎲 완전 랜덤`(손가락·레이스) 두 무리로 갈리고, 두 번째 무리 위에 "확률이 적용되지 않아요" 안내
- ✔ **E3** 정책 카드 기본값이 `평등`, 결과 줄이 `모두 33.3%로 똑같이`, 슬라이더는 안 보임
- ✔ **E4** `참가자 N명` = 음료를 주문한 사람 수
- ✔ **E5** `[확률⬇️]` → 슬라이더(10~90, `aria-valuetext`) 등장. 끄는 동안 **결과 줄과 참가자 카드의 사람별 % 가 동시에** 움직인다 (`42.9/14.3 → 37.7/24.5`)
- ✔ **E6** 참가자 시트 푸터가 `position:sticky`, 스위치 6개, 일괄 버튼 4종
- ✔ **E7** 토글이 `role="switch"` + `aria-checked` + `aria-labelledby`
- ✔ **E8** 맨 아래 행을 토글해도 스크롤이 안 튀고(0→0) 상단 인원수가 그 자리에서 바뀐다(3→2)

**F. 게임 실행**
- ✔ **F1** 사다리 — 출발 전 멈춰 있고 `준비됐어요 — 내 이름을 찾아두세요` / `[▶ 시작!][그만두기]`
- ✔ **F2** `[▶ 시작!]` → `.lpath` 의 `stroke-dashoffset` 이 596→173 으로 단조 감소(선이 실제로 내려간다)
- ✔ **F3** 사다리 결과 — ☕ 가 도착 칸에 있고 `[확정하기][🎲 한 판 더][그만두기]`
- ✔ **F4** 돌림판 — 도는 동안 이름이 똑바로 선다(라벨 화면 각도 0.0°) (F-1)
- ✔ **F5** 돌림판이 `[그만 돌리기]` 전까지 계속 돈다(10.8초 관찰, 저절로 안 멈춤)
- ✔ **F6** 멈춘 뒤 이름이 똑바로 읽히고 판이 뒤로 안 튄다(각도 단조 증가) (F-1)
- ✔ **F7** 레이스 — 출발 전 정렬, `[▶ 출발!][그만두기]`(아직 `건너뛰기` 없음)
- ✔ **F8** `[▶ 출발!]` → 푸터가 `[⏭ 건너뛰기]` 로 바뀌고 HUD 진행률 10%→34%, 상위 3명이 갱신
- ✔ **F9** 1등이 결승선을 지나는 순간 결과 발표, **화면은 계속 돈다**(캔버스 픽셀이 계속 바뀜)
- ✔ **F10** `[⏭ 건너뛰기]` → 즉시 결과, 오버레이는 안 닫힘
- ✔ **F11** 손가락 룰렛 — `손가락 0개 — 두 개 이상 올려주세요`, 인원 상한 없음
- ✔ **F12** 오버레이 포커스 트랩(`aria-modal`, `gameTrap`)
- ✔ **F13** 상한 — 사다리 16 · 돌림판 24 · 레이스 24 · 손가락 ∞. 17명·25명에서 `gameFits` false
- ✔ **F14** 동작 줄이기 — 셋 다 `[▶]` 를 누른 뒤 연출 없이 결과로(사다리 0.25s · 돌림판 0.5s · 레이스 0.25s) (X-3)
- ✔ **F15** 구슬에 번호가 있고 범례·HUD 와 같은 표를 쓴다 · 코스가 미니맵을 침범하지 않는다 (F-3)
- ✔ **F16** 돌림판도 `[▶ 돌리기!]` 게이트가 있다 — 1.8초 방치해도 `rotate(0deg)`, 누르면 회전 (X-1)

**G. 뒤로가기 · 히스토리**
- ✔ **G1** 게임 오버레이 → 뒤로가기 1회 → 오버레이만 닫히고 앱은 그대로
- ✔ **G2** `카페 주문서` 탭 → 뒤로가기 1회 → 합친 화면(`cafe`→`order`)
- ✔ **G3** 확인창 → 뒤로가기 → `false` 로 닫히고 **동작은 실행 안 됨**, 이후 뒤로가기가 앱 안에서 계속 동작

**H. 마감 · 대시보드**
- ✔ **H1** 마감 화면 — 나 포함 전원이 나오고 `[나도]` 버튼은 0개
- ✔ **H2** 주문 0건 마감 — 콘솔 오류 0, 빈 상태 정상
- ✔ **H3** 기간 프리셋 7종이 `오늘 · 이번주 · 지난달 · 이번달 · 3개월 · 올해 · 작년`, **기본값이 `오늘`**
- ✔ **H4** 구간 계산 — `이번주` 가 월요일 시작·오늘 끝 · `지난달` 이 1일~말일 · `3개월` 첫날이 1일 · `작년` 이 1/1~12/31
- ✔ **H5** 320px 에서 프리셋 7개가 **가로 스크롤 없이 한 줄**(scrollWidth 320 = clientWidth 320, 칩 31~41px)

**I. 접근성 · 레이아웃**
- ✔ **I1** 누를 수 있는 요소가 전부 44px 이상(`.me-chip` 44×83px) (U-1)
- ✔ **I2** 헤더 `⚙️` 44px
- ✔ **I3** 헤더에 `공유중`·초록 점·`마지막 갱신`·`N초 전` 이 없다(`#syncAgo` 도 없음)
- ✔ **I4** 이 기기 전용 모드 경고는 헤더에 그대로 남아 있다
- ✔ **I5** 호스트가 아니면 하단바 없음 + `body.no-bottom` 으로 아래 여백 0
- ✔ **I6** 호스트면 하단바가 `[섬김 취소][주문 마감하기]` 로 고정 — 탭을 바꿔도 같다
- ✔ **I7** 320px 참가자 시트 일괄 버튼 4개가 2줄로 접힌다(각 44px 유지) → **U-3** *(열림 · 동작 지장 없음, 기록용)*
- ✔ **I8** 긴 이름(6자) 호스트 — 320px 에서 머리말이 **한 줄** 유지, 가로 넘침 0, 머리말 높이 104px 유지

**K. 사용성 검토 대응 (REVIEW-USABILITY.md · v10.11)**
- ✔ **K1** 섬김 전 화면에 갈림길이 둘 — `📝 메뉴 고르기`가 주(brand), `☕ 제가 살게요`가 부(soft), 둘 다 44px 이상
- ✔ **K2** `[📝 메뉴 고르기]` → 호스트 없이 주문이 열리고(`hostId=null`·`state=open`), 주문해도 호스트가 안 생긴다
- ✔ **K3** `[제가 살게요]`에 확인창 — 취소하면 호스트도 섬김도 안 만들어진다
- ✔ **K4** 참가자에게는 게임 진입점이 없고 `다른 분들`이 접힌다(요약 44px) — `[나도]` 3개는 살아 있다
- ✔ **K5** 호스트에게는 게임 진입점이 있고 로스터가 펼쳐져 있다
- ✔ **K6** 연습 게임은 ⚙️ 설정의 `🎲 게임 구경하기 (연습)` 로 옮겨졌다 (참가자에게만)
- ✔ **K7** 주문 시트 — 옵션이 서랍에 접히고 안내 한 줄이 있으며 확정 버튼은 스크롤 없이 보인다
- ✔ **K8** 옵션을 고르면 접힌 서랍 요약이 그 자리에서 바뀐다(`기본 그대로`→`1개 선택됨`, 리렌더 없이)
- ✔ **K9** 제목 위계 — `h1` 하나(셀 이름만, 배지는 밖) → `h2` 구역 → `h3` 소제목
- ✔ **K10** 글자 크기가 rem 이라 사용자 설정을 따른다(루트 130% → 본문 15→20px)
- ✔ **K11** `N번째 섬김` 배지는 2회차부터만 나온다
- ✔ **K12** 호스트 없는 섬김 — 배너가 `아직 섬기실 분이 안 정해졌어요` 라고 말한다(잘림 없음, 머리말 104px 유지)
- ✔ **K13** 카페 주문서 — 섬기실 분이 없으면 경고하되 `📋 주문서 복사`는 막지 않는다
- ✔ **K14** `[✅ 주문 마감]` → `☕ 누가 섬기셨어요?` → 고르면 **호스트가 채워지고 마감**된다(주문 4건 보존) — 기록 누락 차단
- ✔ **K15** 마감했으니 그날 다음 섬김(seq 2)을 열 수 있다 — 막다른 골목 해소
- ✔ **K16** 주문 0건인 호스트 없는 섬김은 `[이 섬김 접기]`로 사라진다

**L. 시트 푸터 · 옵션 (v10.13)**
- ✔ **L1** 주문 시트 푸터가 한 줄 — `[닫기][이걸로 주문]` 가로 배치, 각 44px 이상, 가로 넘침 0. 푸터 **155 → 100px**
- ✔ **L2** 샷 선택지가 `기본 · 1/4샷 · 1/2샷 · **1샷** · **2샷** · 1샷 추가 · 2샷 추가` 7종이고, 샷이 붙는 **모든 메뉴가 같은 목록**을 쓴다(`OPT_SHOT` 한 곳이 출처)
- ✔ **L3** `[2샷]`을 골라 주문하면 카페 주문서에 `아메리카노 (ICE) [2샷]` 로 찍힌다
- ✔ **L4** 짧은 `[부][주]` 쌍을 가로로 둔 시트 3종(구성원 편집·셀 만들기·메뉴 추가)이 320px 에서 안 접힌다

**J. 콘솔**
- ✔ **J1** 위 전 구간에서 콘솔 오류 0건

---

## 5. 실기기 체크리스트

자동화로 못 보는 것만 남겼다. **준비**: ① 구성원 4명 이상·그중 3명 주문·1명 `안 마실게요`·1명 무응답인 셀, ② 구성원 10명·16명·24명 셀 각 한 벌, ③ 폰 2대(또는 창 2개).

**M-1 ~ M-5 · 손가락 룰렛 (전부 실기기 전용 — 멀티터치)**
- [ ] **M-1** 손가락 3~5개를 동시에 올려도 **화면이 확대되지 않고** 원이 손가락마다 따라붙는다 (카톡 인앱 안드로이드 / iOS 사파리 / 삼성인터넷 각각)
- [ ] **M-2** 참가자 5명인데 손가락 3개만 올린다 → 세지 않고 `손가락 3개 · 참가자 5명` / `아직 안 올린 분이 있어요?` / `[이대로 시작]`. 6개를 올리면 **다른 문구**(`한 분이 두 개 올렸거나…`)
- [ ] **M-3** `[이대로 시작]` 을 다른 손가락이 화면에 있는 채로 눌러도 **한 번에** 눌리고, 그 터치로 점이 새로 생기지 않는다. 승인 뒤 손가락을 더하거나 빼면 질문이 **다시** 뜬다
- [ ] **M-4** 손가락을 게임 본문 가장자리 밖으로 밀면 점이 경계에 붙어 남고 카운트다운이 리셋되지 않는다. 실제로 떼면 그때 사라진다
- [ ] **M-5** 폰 가운데에 손가락을 몰아 겹쳐도 안내 문구와 `3·2·1` 이 읽힌다. 당첨이 정해지면 판이 사라져 당첨 점을 안 가린다

**M-6 ~ M-9 · 실시간(폴링) · 두 기기**
- [ ] **M-6** A가 게임 결과를 열어 둔 채 B가 새 주문 → A에 `게임 도중 참가자가 바뀌었어요 — 한 판 더 돌리시겠어요?`, `한 판 더` 로 사라진다
- [ ] **M-7** B가 당첨자를 명단에서 빼면 → A에 `당첨되신 분이 방금 명단에서 빠졌어요`, **`확정하기` 가 사라지고** `한 판 더`·`그만두기` 는 남는다
- [ ] **M-8** A가 확정하는 찰나에 B가 먼저 이어받는다 → A에 `앗, 방금 다른 분이 먼저 이어받으셨어요` 토스트, **오버레이는 안 닫힌다**
- [ ] **M-9** 헤더에서 30초 동안 바뀌는 글자가 한 군데도 없다. 그 뒤 B가 주문을 바꾸면 **본문 현황**은 10초 안에 따라온다(헤더 표시는 지웠지만 폴링은 살아 있다)
- [ ] **M-10** 게임 확정 후 다른 폰에서 10초 안에 호스트가 바뀐 게 보이고, `N번째 섬김` 번호와 주문이 그대로다

**M-11 ~ M-13 · 데이터 안전 (자동화가 못 만드는 경합)**
- [ ] **M-11** 호스트가 있는 섬김에서 주문을 전부 취소해 0건으로 만들고 `전원 참가` 로 참가자 2명을 채운 뒤 확정 → `아직 주문이 없어서 지금 넘기면 이 섬김이 사라져요` 로 막히고 **`N번째 섬김` 번호가 그대로**다. 번호가 오르면 섬김이 지워졌다 다시 만들어진 것이라 실패
- [ ] **M-12** `확정하기` 를 누른 직후 버튼이 흐려지며 `확정하는 중…`. 한 번 더 눌러도 토스트가 한 번, 호스트도 한 번만 바뀐다
- [ ] **M-13** `db-migration-last-host.sql` 미적용 셀에서 게임을 연다 → 오류 토스트 없이 시트가 뜨고 정책 카드만 안 보인다. 실행부터 확정까지 정상

**M-14 ~ M-18 · 눈으로만 판정되는 것**
- [ ] **M-14** 사다리 **10명**을 320px 기기에서 연다 → 세로줄 위 2글자 이름(**실측 7.1px**)이 읽히는지 정직하게 본다. 안 읽히면 크기가 아니라 `GAME_LABEL_MAX` 를 9 로 내려 번호+범례 경로로 보낸다 → **U-2**
- [ ] **M-15** 사다리 **16명** — 번호(8.4px @320px)와 아래 범례로 누가 어느 줄인지 알 수 있다. 선마다 색 동그라미가 따라 내려오고 속도가 미묘하게 다르다(0.9~1.3배)
- [ ] **M-16** 구슬 레이스 **24명** — 코스가 끼는 곳 없이 끝나고 한 판이 30초를 안 넘는다. 회전체·버블·결승 통로가 다 나온다
- [ ] **M-17** 레이스 도중 홈 버튼으로 나갔다 10초 뒤 돌아온다 → 구슬이 순간이동하지 않고 떠날 때 자리에서 이어진다(전체 시간이 늘어나는 것은 정상)
- [ ] **M-18** ✕ / 뒤로가기 / `그만두기` 세 경로로 각각 게임을 나간 뒤 1분간 폰을 둔다 → 화면이 뜨겁거나 배터리가 계속 줄면 프레임이 남은 것이라 실패
- [ ] **M-19** iOS 사파리 여러 세대(dpr 2·3)에서 레이스 코스 벽·페그·결승선이 **실제로 그려진다**(캔버스 한 변 4096px 제한)

**M-20 ~ M-23 · 모바일 키보드 · 대시보드**
- [ ] **M-20** 전체 메뉴판 검색창을 탭 → 키보드 위로 결과 목록과 `닫기` 가 계속 보이고, 시트 하단과 키보드 사이에 **딤 안 된 띠가 없다**. 주소창 축소/확장·화면 회전 후에도 같다. (`?vvdebug` 로 `inner · vv · top · kb · sab` 실측값을 캡처해 붙인다)
- [ ] **M-21** 검색창 탭 시 비밀번호·카드 자동완성 줄이 안 뜬다. 관리자 로그인 화면에서는 자동완성이 여전히 동작한다
- [ ] **M-22** 관리자 대시보드 검색창에 `하성` → 자모 분리 없이 들어간다(IME). 셀 카드를 눌러도 검색어·포커스 유지
- [ ] **M-23** 셀 링크 대시보드(`?cell=…&view=dashboard`)가 기간 컨트롤과 함께 렌더되고, 프리셋을 바꾸면 헤더 `from ~ to` 가 따라온다. 데이터 없는 셀에서 크래시 없이 빈 상태

---

## 6. 지켜야 할 설계 판단 (회귀 가드)

케이스가 아니라 **바꾸면 안 되는 이유**다. 위 TC 중 여럿이 이것들의 가드다.

**게임 3원칙**
1. **게임은 저절로 안 뜬다.** 자진해서 섬기려던 분의 결정을 가로채지 않는다 — 버튼을 직접 눌렀을 때만 시작한다. (마감을 눌러도 게임이 안 뜬다)
2. **주문이 모인 뒤에 돌린다.** 사는 사람을 먼저 정하면 그 뒤 주문이 전부 눈치 게임이 된다.
3. **결과에 구속력이 없다.** `[확정하기]` 전에는 서버에 아무것도 안 쓴다. `한 판 더`·`그만두기`가 항상 있고, 당첨자 본인은 메뉴를 다시 고를 수 있다.

**확률은 한 지점에만 얹힌다** — `startGame` 에서 당첨자를 먼저 뽑고 그림이 그 결과를 향한다. 예외 둘: 돌림판은 조각 각도가 곧 확률이라 멈춘 자리가 추첨이고(그래서 `wheelSimulate` 단위 테스트가 각도=확률을 지킨다), 구슬 레이스와 손가락 룰렛은 물리·손가락이 정해서 가중치를 걸 자리가 없다.

**탭·히스토리 불변식** — 합친 화면이 키 `order` 를 유지하고 `cafe` 가 유일한 비기본 탭이라, "기본 탭을 떠날 때만 히스토리 1칸"이 성립한다. 게임 오버레이는 `pushSub()` 을 쓰지 않고 **자기 항목 1칸을 직접 소유**한다 — 그래야 뒤로가기 한 번에 게임만 닫히고 탭의 한 칸이 살아 있다. `renderBottom()` 은 `openTab` 을 읽지 않는다(하단바는 탭과 무관하게 고정). → **G1·G2·I6**

**날짜는 KST 문자열 + UTC 로만** — 기준일은 `todayKey()`, 계산은 `YYYY-MM-DD` 문자열과 UTC. 시간대 밀림이 없다. → **H4**

**정상 상태는 조용히, 문제 상태만 알린다** — `공유중`·연결 점·`마지막 갱신` 은 지웠고 `이 기기 전용` 경고만 남겼다. → **I3·I4**

**중복을 그리지 않는다** — 합친 화면에서 내 주문은 위 액션 카드에 한 번, 로스터에는 내가 없다. → **B6**

---

## 7. 앞 판에서 지운 케이스와 이유

| 지운 것 | 이유 |
|---|---|
| TC1~TC23 (기능 TC) | 전부 A·B·C·D·H 로 옮겨 자동화됐다. 목록만 있고 실행 근거가 없던 항목이라 남길 이유가 없다 |
| TC24~TC41 (v9.52 키보드·대시보드) | 키보드 4건은 **M-20·M-21** 로 합쳤다. 나머지는 그 뒤 회차에서 화면이 통째로 바뀌어 대상이 없다 |
| TC42~TC50, TC51~TC64 | IME·프리셋은 **M-22·H3·H4** 로, 게임 진입점·탭은 **E1·B1·B2** 로 대체 |
| TC65~TC104 (game-picker R1~R18) | 참가자 시트·정책 카드·확률은 **E3~E8** 이 자동으로 지킨다. 실시간 경합만 **M-6~M-13** 에 남겼다 |
| TC105~TC121 (구슬 레이스 R19) | v9.98 에서 레이스가 레퍼런스 방식으로 **통째로 교체**됐다. 카메라·HUD·역전 문턱·소요 시간이 전부 다른 구현이라 케이스가 코드를 안 가리킨다. 지금 것에 맞는 F7~F10·F15·M-16~M-19 로 새로 썼다 |
| TC122~TC128 (헤더 연결 표시 제거) | **I3·I4·M-9** 로 압축. "없어졌는지"를 여섯 케이스로 볼 이유가 없다 |
| TC129~TC232 중복 번호대 | TC219~TC221·TC222~TC232 가 **각각 두 벌씩** 있었다(v9.92/v9.93/v9.94 가 같은 번호를 다시 씀). 번호로 가리킬 수 없는 목록이라 통째로 다시 매겼다 |
| TC233~TC255 (v9.96·v9.97 장애물·미니맵) | 위와 같음 — v9.98 교체로 대상 소멸 |
| TC153~TC155 (미측정 항목) | **쟀다.** 사다리 10명 = 7.1px(→ **U-2**·M-14), 사다리 24명 범례는 상한 16 이라 성립 불가, 레이스 구슬 번호는 **애초에 없다**(→ **F-3**) |

**번호 체계**: 이제 `A~J` 는 자동 검증(회차와 무관하게 유지·확장), `M` 은 실기기, `F-n/U-n/X-n` 은 이번 회차에 연 결함이다. 다음 회차에는 결함 번호만 새로 열고 A~J 는 늘리거나 고친다 — 회차마다 번호대를 새로 따지 않는다.

---

## 8. 계측 (PostHog · v10.06)

제품 이벤트 16종이 걸려 있다. 로컬 사본에서는 스니펫을 빼므로 검증 대상이 아니고, 실기기에서 PostHog 대시보드로 확인한다.

`cell_entered` · `serving_started` · `serving_closed` · `order_submitted` · `order_skipped` · `order_removed` · `game_sheet_opened` · `game_started` · `game_finished` · `game_replayed` · `game_abandoned` · `host_committed` · `dashboard_opened` · `dash_preset_used` · `copy_used` · `write_failed`

**규약**(코드 주석 1160~1164행): ① 사람을 알아볼 수 있는 값은 안 보낸다 — 이름·메모·직접입력 문구는 **유무와 길이만**. member id 는 앱이 만든 가명이라 보낸다. ② `track` 은 절대 던지지 않는다. ③ 속성 이름은 어느 사건에서나 같은 뜻(`cafe_id` 는 늘 카페, `kind` 는 늘 게임 종류).

- [ ] **M-24** 주문·게임·마감을 한 바퀴 밟은 뒤 PostHog 에서 16종이 다 들어오는지, 그리고 **이름·메모 원문이 섞여 들어간 속성이 없는지** 본다

---

## 9. 알려진 한계 (의도 — 신뢰그룹 전제)

- 자기신고 actor — 이름 도용 시 위조 가능. 수정내역은 로그된다
- 링크 = 권한 — 링크를 아는 사람은 편집 가능
- 직접입력 메뉴는 디카페인 옵션 없음 (길카페는 애초에 디카페인 미제공)
- 섬김은 당일(KST) 기준 — 자정 전 마감 권장
- `visualViewport`·`min()`·`overscroll-behavior` 미지원 브라우저는 패치 전 동작으로 떨어질 뿐 깨지지 않는다

## 10. 백엔드 (라이브 · 읽기 전용)

- [ ] anon 직접 조회 차단: `orders`/`sessions`/`cells`/`member_logs` → 401 또는 permission denied
- [ ] RPC 정상: `get_cell(demo)` 200, `get_room_day(demo)` 200
- [ ] 감사로그: `set_members` 3-인자 존재, `member_diff` 서버 diff 정상
- [ ] 회원가입 비활성: `/auth/v1/signup` 422
