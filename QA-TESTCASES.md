# QA 테스트케이스 & 결과 (v9.33)

검증 방법: 스크래치패드 로컬 사본(키 blank=LocalSync, 백엔드 쓰기 없음)에서 Chrome 자동화로 플로우 실행 + 라이브는 읽기 전용 curl. 실데모/실셀에는 절대 쓰기 안 함.

---

## 모바일 키보드 패치 회차 (v9.52) — ⏳ 실기기 확인 대기

아래는 **아직 검증 안 된 항목**이다. 데스크톱 자동화로는 모바일 키보드를 재현할 수 없어 실기기 확인이 필요하다.

| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| B4 | 전체 메뉴판에서 타이핑하면 걸러진 메뉴가 안 보임 | `.scrim{inset:0}`·`.sheet{max-height:88vh}` 가 **레이아웃** 뷰포트 기준 → 키보드가 올라와도 시트가 안 줄어 결과 목록이 키보드 뒤로 밀림 | `visualViewport` 높이를 `--vvh`/`--vv-top` CSS 변수로 전달 + 검색 시트를 헤더/스크롤/푸터 3분할(`.sheet.split`) |
| B5 | 주문 후 '안 마실게요'가 13px 텍스트 링크로만 남아 누르기 어려움 | `showGrid` 가 false 라 버튼이 통째로 숨겨지고 `.nav-link` 만 노출 | 주문 후에도 `🔄 다른 메뉴로` / `🙅 안 마실게요` 2버튼(`.row-2`, 높이 ≥47px)으로 나란히 노출 |
| B6 | 배포했는데 반영됐는지 판별 불가 | 이전 회차가 `APP_VERSION` 을 안 올려 캐시본과 신규본 구분 불가 | v9.51 로 범프. **배포마다 반드시 올린다** |
| B7 | 키보드 올라온 상태에서 시트 하단과 키보드 사이에 **딤 안 된 띠**가 남음 (Galaxy/Chrome 실기기) | scrim 바닥을 `top:--vv-top + height:--vvh` 로 **계산**해서 만든 탓에 측정 오차가 그대로 구멍이 됨. 삼성 키보드의 자동완성·추천 줄이 키보드가 뜬 뒤에 붙어 키보드가 더 높아지는데 `resize` 를 한 번만 받아 그 증가분을 놓침 | scrim 을 `inset:0` 로 **항상 화면 전체**를 덮게 하고 `padding-bottom: var(--kb)` 로 시트만 밀어올림 → 측정이 틀려도 구멍이 구조적으로 안 생김. `--kb = innerHeight - (vv.height + vv.offsetTop)` + 이벤트 후 60·160·320·600ms 재측정 + `interactive-widget=resizes-content` |

**TC24~TC32 (실기기 체크리스트)**
- [ ] TC24 iPhone Safari — 전체 메뉴판 → 검색창 탭 → 키보드 위로 결과 목록과 '닫기' 바가 계속 보임
- [ ] TC25 타이핑 중 검색창 아래 "검색 결과 N개" 갱신
- [ ] TC26 초성 검색 — `ㅇㅁㄹ` → 아메리카노 계열 노출 (자음만 입력했을 때만 초성 모드)
- [ ] TC27 Android Chrome — 주소창 축소/확장·화면 회전 후에도 시트 하단 안 잘림
- [ ] TC28 키보드 닫으면 시트가 원래 높이(88vh)로 복귀
- [ ] TC29 목록 끝에서 계속 스크롤해도 배경 페이지가 안 따라 움직임(`overscroll-behavior`)
- [ ] TC30 B1·B2 회귀 없음 — 헤더 위로 목록 안 비침 / 시트 닫고 다시 열면 목록 맨 위부터
- [ ] TC31 음료 주문 후 메인 화면에 '안 마실게요' 버튼이 '다른 메뉴로' 옆에 노출·동작 (B5)
- [ ] TC32 키보드 올린 상태에서 시트 하단과 키보드 사이에 딤 안 된 띠 없음. 자동완성/추천 줄이 뜬 뒤에도 없음 (B7)

**계측**: 주소 뒤에 `?vvdebug` 를 붙이면 화면 하단에 `inner · vv · top · kb · sab · 버전` 실측값이 표시된다. 키보드 관련 증상이 남으면 이 값을 캡처해 붙인다 — 추측 대신 숫자로 잡는다.

**확인 순서**: 푸터의 버전이 `v9.52` 인지 먼저 본다. 낮으면 캐시본이므로 재접속(카톡 인앱 브라우저는 앱 재시작) 후 다시 확인한다. GitHub Pages 는 `cache-control: max-age=600` 을 강제하므로 배포 후 최대 10분 지연이 정상이다. `<meta http-equiv="Cache-Control">` 3줄은 문서 자신에는 효과가 없다(참고용으로만 유지).

**미지원 폴백**: `visualViewport` 미지원 브라우저는 리스너를 안 걸고 CSS 폴백값(`100%`/`88vh`)으로 패치 전과 동일하게 동작한다. `min()` 은 Chrome 79+/Safari 11.1+, `overscroll-behavior` 는 iOS Safari 16+ 필요 — 둘 다 미지원 시 패치 전 동작으로 떨어질 뿐이다.

---

## 이번 회차에 발견·수정한 버그
| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| B1 | 전체 메뉴판 스크롤 시 헤더 위로 메뉴가 살짝 비침 | `.sheet padding-top:8px` 때문에 sticky 헤더가 8px 아래에 붙어 그 틈으로 목록이 보임 | `.sheet` 상단 패딩 제거(그립 마진이 여백 유지) + 헤더 좌우 bleed·z-index↑ → 빈틈 0 |
| B2 | 스크롤된 시트를 닫고 다른 시트를 열면 새 시트가 스크롤된 채로 뜸 | `openSheet`가 `scrollTop`을 리셋 안 함 | `openSheet`에 `scrollTop=0` 추가 |
| B3(경미) | `member_diff` 함수가 anon에서도 실행됨 | Supabase 기본권한이 함수 생성 시 anon에 EXECUTE 부여 → `revoke from public`만으론 부족 | `revoke ... from public, anon, authenticated` (순수 함수라 무해하나 최소권한 정리) |

## 기능 TC (로컬 자동검증 — 전부 PASS)
**입장/이름**
- TC1 이름 미선택 → 본인선택 화면 / 선택 → 메인
- TC14 본인 이름 변경 → 숨은 id 유지·표시 갱신·주문 보존
- TC15 본인이 명단에서 삭제됨 → 본인선택 화면으로 안전 복귀(크래시 없음)

**섬김(호스트)/세션**
- TC2 호스트 자처 → open, host 지정
- TC8 마감 → closed / 마감취소 → open
- TC16 호스트가 명단에서 빠지면 → 세션 자동 해제(idle, 잠김 방지)

**주문**
- TC3 음료 주문 저장·표시
- TC7 주문 취소
- TC9 기존 주문 수정 시 옵션 상태 복원 + 배반(샷추가↔연하게)
- TC10 직접입력(커스텀) 주문 — 디카페인 옵션 없음(알려진 한계)
- TC11 디카페인은 커피계열만(아메리카노 O, 제주말차라떼 X)
- TC12 HOT 전용 메뉴(에스프레소) 온도 자동·고정
- TC17 "나도" — 남의 주문(옵션 포함) 복사

**현황/주문서**
- TC4 합계(잔 수) 정확
- TC5 카페 주문서 텍스트(음료·온도·옵션·합계) 생성
- TC6 분류(주문/안마심/대기) 정확
- TC18 주문 0건 마감 화면 크래시 없음
- TC22 주문서에 커스텀 메뉴 + 디카페인 포함, 크래시 없음
- TC23 커스텀 메뉴 이모지 폴백(🥤)

**전체 메뉴판**
- TC13 검색 필터 / 무결과 안내 / 검색어 지우면 복원
- B1 스크롤 시 헤더 위 비침 없음(수정 확인)

**다이얼로그/안내**
- TC20 커스텀 확인창 표시 / 뒤로가기=취소 / 확인=true
- TC21 호스트가 명단에서 빠져도 "안내 문구" 생성 크래시 없음

**기타**
- TC19 셀 전환 시 forceGrid 리셋
- 7글자 이름: 헤더/타이틀/목록/셀명 깨짐 없음, 가로 스크롤 0 (이전 회차)
- 이름 선택 화면 12초 폴링에 안 닫힘 (이전 회차)

## 백엔드 TC (라이브, 읽기 전용 — 전부 PASS)
- anon 직접 조회 차단: `orders/sessions/cells/member_logs` → 401/permission denied
- RPC 정상: `get_cell(demo)` 200, `get_room_day(demo)` 200
- 감사로그 적용 확인: `set_members` 3-인자 존재, `member_diff` 서버 diff 정상
- 회원가입 비활성: `/auth/v1/signup` 422

## 알려진 한계(의도 — 신뢰그룹 전제)
- 자기신고 actor(이름 도용 시 위조 가능), 로그 자동삭제 미도입
- 직접입력 메뉴는 디카페인 옵션 없음
- 세션은 당일(KST) 기준 — 자정 전 마감 권장
- 링크=권한(링크 아는 사람은 편집 가능, 단 수정내역은 로그됨)
