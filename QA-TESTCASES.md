# QA 테스트케이스 & 결과 (v9.33)

검증 방법: 스크래치패드 로컬 사본(키 blank=LocalSync, 백엔드 쓰기 없음)에서 Chrome 자동화로 플로우 실행 + 라이브는 읽기 전용 curl. 실데모/실셀에는 절대 쓰기 안 함.

---

## 모바일 키보드 패치 회차 (v9.52) — ⏳ 실기기 확인 대기

아래는 **아직 검증 안 된 항목**이다. 데스크톱 자동화로는 모바일 키보드를 재현할 수 없어 실기기 확인이 필요하다.

| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| B4 | 전체 메뉴판에서 타이핑하면 걸러진 메뉴가 안 보임 | `.scrim{inset:0}`·`.sheet{max-height:88vh}` 가 **레이아웃** 뷰포트 기준 → 키보드가 올라와도 시트가 안 줄어 결과 목록이 키보드 뒤로 밀림 | `visualViewport` 높이를 `--vvh`/`--vv-top` CSS 변수로 전달 + 검색 시트를 헤더/스크롤/푸터 3분할(`.sheet.split`) |
| B5 | 주문 후 '안 마실게요'가 13px 텍스트 링크로만 남아 누르기 어려움 | `showGrid` 가 false 라 버튼이 통째로 숨겨지고 `.nav-link` 만 노출 | 주문 후에도 `🔄 다른 메뉴로` / `🙅 안 마실게요` 2버튼(`.row-2`, 높이 ≥47px)으로 나란히 노출 |
| B6 | 배포했는데 반영됐는지 판별 불가 | 이전 회차가 `APP_VERSION` 을 안 올려 캐시본과 신규본 구분 불가 | v9.51 로 범프. **배포마다 반드시 올린다** |
| B7 | 키보드 올라온 상태에서 시트 하단과 키보드 사이에 **딤 안 된 띠**가 남음 (Galaxy/Chrome 실기기) | scrim 바닥을 `top:--vv-top + height:--vvh` 로 **계산**해서 만든 탓에 측정 오차가 그대로 구멍이 됨. 삼성 키보드의 자동완성·추천 줄이 키보드가 뜬 뒤에 붙어 키보드가 더 높아지는데 `resize` 를 한 번만 받아 그 증가분을 놓침 | scrim 을 `inset:0` 로 **항상 화면 전체**를 덮게 하고 `padding-bottom: var(--kb)` 로 시트만 밀어올림 → 측정이 틀려도 구멍이 구조적으로 안 생김. `--kb = innerHeight - (vv.height + vv.offsetTop)` + 이벤트 후 60·160·320·600ms 재측정 + `interactive-widget=resizes-content` |

| B8 | 메뉴 검색창을 누르면 비밀번호·카드 자동완성 아이콘 줄이 떠서 보이는 영역이 더 줄어듦 | `<input type="text">` 에 `autocomplete` 지정이 없어 Chrome/Android 자동완성 휴리스틱이 저장된 자격증명(관리자 로그인)을 이 도메인 필드에 제안 | 검색창을 `type="search"` + `autocomplete/autocorrect/autocapitalize=off` + `spellcheck=false` + `enterkeyhint=search` 로 변경. 나머지 비자격증명 input 11개에도 `autocomplete="off"` 일괄 적용(관리자 email·password 2개는 자동완성 유지) |

| B9 | 탭 영역이 44px 미만인 요소 8종 | `.chip` 36 · `.btn-sm` 35(좁은 화면 32) · `.nav-link` 16 · `#gear` 22 · `.sheet-x` 35 · `.tab` 36 | 전부 `min-height:44px`. 헤더 안 요소(`.nav-link`·`.sheet-x`)는 음수 마진으로 줄높이·시각적 위치 보존 |
| B10 | 사용 빈도 막대의 마감/진행중 두 색이 구분 안 됨 | `--brand #6f4e37` ↔ `--accent-ink #96631f` 가 **정상 시야 ΔE 10.2**(기준 15 미달). v9.54 에서 `--accent` 를 글자 대비 때문에 어둡게 하며 악화됨. 또 `--brand` 는 채도 0.057 이라 데이터 마크로는 회색으로 읽힘 | 상태를 색으로 나누지 않는다 — 막대는 `--data-1 #94520c`(채도 0.104, 검증 통과) 한 색, **마감=채움 / 진행중=윤곽** + 라벨 범례 |
| B11 | 대시보드가 모바일에서 안 읽힘 | 4개 섹션이 같은 무게로 전부 펼쳐짐 · 가로 스크롤 2개 중첩 · 모든 막대에 숫자 라벨 · 정보가 `title=`(터치에서 안 뜸)에만 있음 · 기간 필터가 표시용 pill | 히어로+KPI → 추세 → 사람 → 메뉴 4단 위계. 매트릭스·사람별 메뉴는 드릴다운 시트로. 값 라벨은 최댓값·마지막만. 탭 툴팁 도입. 기간 필터 7/30/90 복구 |

**TC24~TC33 (실기기 체크리스트)**
- [ ] TC24 iPhone Safari — 전체 메뉴판 → 검색창 탭 → 키보드 위로 결과 목록과 '닫기' 바가 계속 보임
- [ ] TC25 타이핑 중 검색창 아래 "검색 결과 N개" 갱신
- [ ] TC26 초성 검색 — `ㅇㅁㄹ` → 아메리카노 계열 노출 (자음만 입력했을 때만 초성 모드)
- [ ] TC27 Android Chrome — 주소창 축소/확장·화면 회전 후에도 시트 하단 안 잘림
- [ ] TC28 키보드 닫으면 시트가 원래 높이(88vh)로 복귀
- [ ] TC29 목록 끝에서 계속 스크롤해도 배경 페이지가 안 따라 움직임(`overscroll-behavior`)
- [ ] TC30 B1·B2 회귀 없음 — 헤더 위로 목록 안 비침 / 시트 닫고 다시 열면 목록 맨 위부터
- [ ] TC31 음료 주문 후 메인 화면에 '안 마실게요' 버튼이 '다른 메뉴로' 옆에 노출·동작 (B5)
- [x] TC32 키보드 올린 상태에서 시트 하단과 키보드 사이에 딤 안 된 띠 없음 (B7) — **v9.52 Galaxy/Chrome 확인 완료**, 계측값 `inner 726 · vv 726 · top 0 · kb 0 · sab 0` (레이아웃 뷰포트가 함께 줄어드는 경로로 동작)
- [ ] TC33 메뉴 검색창 탭 → 비밀번호·카드 자동완성 아이콘 줄이 안 뜸 (B8). 관리자 로그인 화면에서는 여전히 자동완성 동작
- [ ] TC34 검색창에서 키보드의 `검색` 키 → 키보드만 내려가고 목록은 필터된 상태 유지
- [ ] TC35 온도·옵션 칩과 수정/나도 버튼이 44px 이상이고 레이아웃이 안 깨짐 (B9)
- [ ] TC36 헤더 높이 변화 없음 — ⚙️ 설정·뒤로가기는 탭 영역만 넓혔다 (B9)
- [ ] TC37 관리자 📊 대시보드 — 기간 필터 7/30/90 전환 시 재집계됨 (B11)
- [ ] TC38 사용 빈도 막대: 마감=채움 / 진행중=윤곽으로 구분되고 범례에 라벨이 있음 (B10)
- [ ] TC39 막대·KPI·사람 행을 탭하면 툴팁이 뜸 (모바일에서 `title=` 은 안 뜬다) (B11)
- [ ] TC40 `날짜별 보기` / `사람별로 보기` 드릴다운 시트가 열리고 닫힘 (B11)
- [ ] TC41 데이터 없는 셀에서 대시보드가 크래시 없이 빈 상태 문구를 보여줌

**계측**: 주소 뒤에 `?vvdebug` 를 붙이면 화면 하단에 `inner · vv · top · kb · sab · 버전` 실측값이 표시된다. 키보드 관련 증상이 남으면 이 값을 캡처해 붙인다 — 추측 대신 숫자로 잡는다.

**확인 순서**: 푸터의 버전이 `v9.52` 인지 먼저 본다. 낮으면 캐시본이므로 재접속(카톡 인앱 브라우저는 앱 재시작) 후 다시 확인한다. GitHub Pages 는 `cache-control: max-age=600` 을 강제하므로 배포 후 최대 10분 지연이 정상이다. `<meta http-equiv="Cache-Control">` 3줄은 문서 자신에는 효과가 없다(참고용으로만 유지).

**미지원 폴백**: `visualViewport` 미지원 브라우저는 리스너를 안 걸고 CSS 폴백값(`100%`/`88vh`)으로 패치 전과 동일하게 동작한다. `min()` 은 Chrome 79+/Safari 11.1+, `overscroll-behavior` 는 iOS Safari 16+ 필요 — 둘 다 미지원 시 패치 전 동작으로 떨어질 뿐이다.

---

## 대시보드 기간·검색 회차 (v9.72) — ⏳ 실기기 확인 대기

| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| B12 | 관리자 대시보드에서 사람 이름을 치면 `하성` 이 `ㅎㅏㅅㅓㅇ` 으로 다 분리됨 | `input` 마다 `paintAdminDash()` 가 `$app.innerHTML` 을 통째로 갈아엎어 검색 `<input>` 자체가 새로 만들어짐 → 조합 중이던 IME 가 매 글자 끊기고 자모가 그대로 확정됨. 포커스·캐럿 복원 코드로는 조합 상태를 못 살린다 | 컨트롤 바 아래를 `#dashBody` 로 묶고 `paintAdminDash(true)` 가 그 안만 교체. 검색·셀 선택은 컨트롤 바를 안 바꾸므로 입력창이 살아 있고 IME 가 안 끊긴다. 포커스 복원 해킹 제거 |
| B13 | 셀 단위 대시보드(`?cell=…&view=dashboard`)가 `집계하는 중…` 에서 멈춤 | 정의된 적 없는 `dashPeriodHtml()` 을 호출 → 템플릿 평가 중 ReferenceError 로 렌더가 통째로 중단 | `dashDatesHtml`·`dashPresetsHtml`·`dashWarnHtml` 로 쪼개고 `dashPeriodHtml`(검색 없는 기간 전용)을 실제로 정의 |
| B14 | 기간 필터가 `7일/30일/90일/1년` 이라 "이번달 얼마나 썼나"를 바로 못 봄 | 프리셋이 달력이 아니라 '오늘부터 N일' 롤링 | 달력 기준 6종으로 교체 — `이번주`(주일 시작)·`지난달`·`이번달`·`최근3개월`(90일 롤링 유지)·`올해`·`작년`. 한 줄 가로 스크롤 + 선택 버튼 자동 센터링. 기본값도 최근 30일 → `이번달` |
| B15 | `오늘은 안 마실게요` 버튼이 있는 줄 사람들이 모름 → 미응답으로 남아 섬기는 분이 일일이 물어야 함 | 위치가 아니라 **무게** 문제. `.btn-ghost`(투명 배경 + `--sub` 회색)라 베이지 배경에 묻혀 `닫기` 같은 부속으로 읽힘. 바로 위 메뉴 타일이 흰 카드로 시선을 다 가져감 | ① 배너 첫 줄을 `드시고 싶은 메뉴를 골라주세요` → `메뉴를 고르거나, 안 드실 땐 알려주세요` 로 — 맨 위라 반드시 읽힌다. ② 버튼을 `.label`(`음료를 안 드신다면`)로 감싸 `메뉴 선택`과 같은 급의 **섹션**으로 승격. ③ 스타일을 `.btn-choice`(흰 카드 + `--ink` 글자)로 — 이 앱에서 누를 수 있는 건 전부 흰 카드다. 이모지를 앞으로 빼 스캔되게 함 |

**TC42~TC47 (실기기 체크리스트)**
- [ ] TC42 관리자 대시보드 검색창에 `하성` 입력 → 자모 분리 없이 그대로 들어가고 결과가 즉시 좁혀짐 (B12)
- [ ] TC43 같은 창에 `ㅎㅅ` 초성만 입력 → 초성 매칭 동작, 지웠을 때 전체 복원
- [ ] TC44 검색 중 셀 카드를 눌러도 검색어·포커스가 유지되고 아래 상세만 바뀜 (B12)
- [ ] TC45 셀 링크 대시보드가 기간 컨트롤과 함께 정상 렌더 (B13)
- [ ] TC46 프리셋 6개가 한 줄에 있고, 좁은 화면에서 가로로 밀린다. 누른 버튼이 화면 안으로 당겨짐 (B14)
- [ ] TC47 프리셋별 구간이 헤더 `from ~ to` 와 일치 — 특히 `지난달` 말일, 1월의 `지난달`(작년 12월), `작년` 1/1~12/31 (B14)
- [ ] TC48 주문 전 화면에서 `음료를 안 드신다면` 섹션과 흰 버튼이 스크롤 없이 보임 (B15) — 실기기에서 **접히는지**가 핵심
- [ ] TC49 `🙅 오늘은 안 마실게요` → `안 마시기로 했어요` 카드로 바뀌고, 그 뒤 메뉴를 고르면 주문으로 되돌아감(되돌릴 수 있는 선택)
- [ ] TC50 주문 후 `🔄 다른 메뉴로` 를 누른 상태에서 `안 마실게요` 버튼이 **한 개만** 보임(중복 없음)

**날짜 계산**: 기준일은 `todayKey()`(KST 고정), 계산은 `YYYY-MM-DD` 문자열 + UTC 로만 한다. 윤년 2월·1월 롤오버·월말은 node 로 검증했다(2026-07-31 / 2026-01-01 / 2026-03-01 / 2026-08-02 / 2024-02-29 / 2026-12-31).

---

## 랜덤 게임 + 탭 내비게이션 회차 (v9.72) — ⏳ 실기기 확인 대기

**설계 판단 3가지** (구현보다 이게 먼저다)

1. **게임은 절대 저절로 안 뜬다.** 자진해서 섬기려던 분의 결정을 게임이 가로채면 안 되므로, `[🎲 게임으로 정하기]`를 직접 눌렀을 때만 시작한다. 호스트가 있는 섬김은 **그 호스트만** 열 수 있다(남의 섬김을 뺏는 경로가 없다).
2. **주문이 모인 뒤에 돌린다.** 사는 사람을 먼저 정하면 그 뒤 주문이 전부 눈치 게임이 된다. 주문 후면 ①아무도 사는 사람을 모른 채 고르고 ②`안 마실게요` 한 분이 후보에서 자동으로 빠지고 ③늦게 온 분도 주문만 하면 후보에 들어온다.
3. **결과에 구속력이 없다.** `[확정하기]` 전에는 서버에 아무것도 안 쓴다. `한 판 더`·`그만두기`가 항상 있고, 당첨자 본인에게는 메뉴를 다시 고를 기회를 준다(사는 줄 모르고 고른 메뉴를 바꿀 수 있어야 한다).

| # | 항목 | 판단 | 조치 |
|---|---|---|---|
| G1 | 게임 3종 선정 | 카톡 인앱·사파리·삼성인터넷에서 **똑같이 동작하는 것만** 넣는다 | 🪜사다리타기 · ☝️손가락 룰렛 · 🎡돌림판. 전부 단일 기기, 디바이스 센서 0개 |
| G2 | 흔들기(DeviceMotion) 채택 안 함 | iOS 13+ 는 사용자 제스처 안에서 `requestPermission()` 이 필요한데 카톡 인앱에서 프롬프트가 안 뜨는 사례가 많고, 안드로이드 인앱 웹뷰도 센서 이벤트가 안 오는 기기가 있다 | 후보에서 제외. 진동은 `navigator.vibrate` 가 있을 때만 보조로 쓰고(iOS 는 API 자체가 없음) 시각 효과를 본체로 |
| G3 | 멀티터치가 핀치 줌으로 먹힘 | 뷰포트 확대는 접근성상 열어둬야 한다(a2f6297) → 전역으로 못 막는다 | 게임 오버레이에만 `touch-action:pan-y`(손가락 룰렛 진행 중엔 `none`) + `user-select/touch-callout:none` + `touchmove`·`gesturestart`·`gesturechange` preventDefault |
| G4 | 호스트 교체가 섬김을 지움 | `unclaim_serving` 은 **주문이 없으면 섬김 자체를 delete** 한다 | 게임 진입점을 주문 1건 이상일 때만 노출. 그러면 `unclaim → claim_host` 2콜로 **DB 마이그레이션 없이** 호스트만 갈아끼운다(seq·주문·카페 전부 보존) |
| G5 | 사다리 확률이 그림에 좌우됨 | 사다리 구조에 따라 도착 칸 분포가 치우칠 수 있다 | **당첨자를 먼저 균등 추첨**하고 ☕를 그 사람 도착 칸에 놓는다. 그림은 결과를 향해 그릴 뿐. 매핑이 전단사인지 33,000회 검증(위반 0) |
| G6 | 돌림판 이름이 거꾸로 섬 | 레이블이 회전 그룹 안에 있어 판과 같이 돌아 멈춘 각도 그대로 뒤집힘 | 최종 회전각을 먼저 구해 각 `<text>` 에 `rotate(-spin)` 을 제자리에 걸어둔다 → 멈춘 순간 항상 똑바로 |
| G7 | 인원 많을 때 사다리 이름 겹침 | 세로줄 간격(292/(n-1))보다 이름이 넓다 | 인원에 맞춰 글자수·크기 축소(≤7명 4자/11px · ≤9명 3자/10px · 그 이상 2자/8.5px) + 전체 이름은 `<title>` 과 결과 발표에 |
| G8 | 결과 발표가 화면 밖으로 밀림 | 12명·568px 화면에서 사다리+발표가 본문 높이를 넘음 | `.game-body` 를 스크롤 가능하게(+`::before/::after{margin:auto}` 로 여유 있으면 가운데) 하고, **푸터 버튼이 들어간 다음 프레임에** 끝까지 스크롤 |
| N1 | `[현황 보기] ↔ [내 주문]` 왕복이 나쁨 | 둘이 **별개 페이지**라 볼 때마다 떠났다 돌아와야 했다. 게다가 현황 안에 또 `[사람별 \| 카페 주문서]` 탭이 있어 2단 | 계층을 한 줄로 펴서 **`[내 주문 \| 현황 N잔 \| 주문서]` 3탭**. 페이지 이동이 아니라 탭 전환이라 헤더·하단바가 그대로다 |
| N2 | 하단바 버튼이 화면마다 바뀜 | 주문화면 `[현황][마감]` ↔ 현황화면 `[섬김취소][주문 마감하기]` | 현황이 탭으로 올라갔으므로 하단바는 호스트 `[섬김 취소][주문 마감하기]` 하나로 고정. 호스트가 아니면 하단바 없음(+`body.no-bottom` 으로 아래 여백도 걷음) |
| N3 | 뒤로가기 동작 보존 | 탭은 보통 히스토리를 안 쌓지만, 기존엔 현황이 페이지라 뒤로가기로 돌아왔다 | 기본 탭을 **떠날 때만** `pushSub()` 1칸 → 뒤로가기 한 번이면 `내 주문` 탭. 앱 밖으로 안 튕긴다 |
| **G9** | **데모에서 게임이 아예 안 보임** (실사용 제보) | 호스트용 진입점을 `현황` 탭 **안에만** 뒀는데 기본 탭이 `내 주문` 이다. 사용자 여정 5단계를 재현하니 **탭을 직접 옮긴 1곳에서만** 보였다. 나머지 진입점(인계 대기 하단바)은 호스트가 `섬김 취소`를 눌러야 생기는 희귀 상태라 실질적으로 안 뜬다 | ① 기본 탭(`내 주문`)에도 같은 진입점을 둔다 — B15와 같은 처방으로 `.label`(`누가 섬길지 아직 안 정했다면`) + `.btn-choice` 흰 카드. ② 섬김이 없는 화면에 안내 한 줄(`섬길 사람은 주문을 받은 뒤 🎲 게임으로 정할 수도 있어요`) — 진입점이 아니라 **언제 생기는지**를 알려준다. ③ 여정 재현 테스트를 회귀 가드로 고정 |

> **G9 교훈**: 기능이 "있다"와 "보인다"는 다르다. 단위 테스트는 `버튼이 DOM 에 있는가`만 물었고 전부 통과했다 — **기본 탭에서 보이는가**를 안 물었기 때문이다. 이후 진입점을 만들 땐 사용자 여정을 처음부터 밟아 각 단계에서 보이는지를 테스트한다.

**자동 검증 (스크래치패드 로컬 사본 · CDP 디바이스 에뮬레이션)** — 44/44 통과
- 게임 상태 전이 17종: 자진 호스트/인계 대기 양쪽에서 호스트 교체 후 seq·주문·카페·open 상태 보존, `안 마심`/`오늘 섬김` 후보 제외, 2명 미만이면 전원 참가 복귀, 뒤로가기로 오버레이만 닫힘, 닫을 때 타이머 정리
- 진입점·탭 16종: **기본 탭에서 게임 버튼이 보이는지**(G9 회귀 가드) · 주문 0→1건 전환 · 섬김 없는 화면 안내 · 주문 0건이면 게임 버튼 없음, 비호스트면 없음, 마감이면 없음, **마감을 눌러도 게임이 저절로 안 뜸**, 탭 전환 시 헤더·하단바 동일, 뒤로가기로 `내 주문` 복귀
- 클릭 전 경로 11종: 현황 탭 → 게임 → 사다리 → 확정까지 실제 DOM 클릭만으로
- 사용자 여정 5단계: 이름 선택 → 섬김 시작 → 주문 → 탭 이동 → 다인 주문, 각 단계에서 게임이 보이는지 (G9 를 잡은 테스트)
- 수학: 사다리 전단사 33,000회 위반 0 · 추첨 균등성 12만회(6명, 16.57~16.82%) · 돌림판 핀 정확도 4만4천회 오차 0

**TC51~TC60 (실기기 체크리스트)**
- [ ] TC51 카톡 인앱(안드로이드) — 손가락 룰렛에서 손가락 3~5개를 동시에 올렸을 때 **화면이 확대되지 않고** 원이 손가락마다 따라붙음 (G3)
- [ ] TC52 iOS 사파리 — 같은 동작. 손가락을 떼면 카운트다운이 리셋되고, 3초 유지하면 하나가 뽑힘
- [ ] TC53 삼성인터넷 — 사다리 애니메이션이 끊기지 않고, 12명에서도 이름이 안 겹침 (G7)
- [ ] TC54 돌림판이 멈춘 뒤 이름이 **똑바로** 읽힘 (G6) — 여러 번 돌려 각도를 바꿔가며
- [ ] TC55 좁은 화면(≤360px)에서 결과 발표(☕+이름)가 잘리지 않고 보임 (G8)
- [ ] TC56 자진해서 섬기기로 한 호스트가 `마감하기`를 눌렀을 때 게임이 **안 뜬다** (확인창만)
- [ ] TC57 게임 확정 후 다른 사람 폰에서도 10초 안에 호스트가 바뀐 게 보임(폴링) · 섬김 번호와 주문이 그대로
- [ ] TC58 당첨자가 본인이면 `내 주문` 탭으로 이동하고 메뉴를 다시 고를 수 있음
- [ ] TC59 `[내 주문 | 현황 | 주문서]` 탭 전환 시 화면이 깜빡이거나 스크롤이 튀지 않음. 뒤로가기 한 번에 `내 주문`
- [ ] TC60 호스트가 아닌 사람 화면에서 하단바가 없고 본문 아래 빈 여백도 없음 (N2)
- [ ] TC61 데모 셀에서 처음부터 밟기 — 섬김 시작 → 메뉴 하나 고름 → **탭을 안 옮겨도** `내 주문` 화면을 내리면 `🎲 게임으로 섬길 사람 정하기`가 보임 (G9)
- [ ] TC62 섬김이 없는 화면에 `주문을 받은 뒤 🎲 게임으로 정할 수도 있어요` 안내가 보임 (G9)

**움직임 최소화**: `prefers-reduced-motion` 이 켜져 있으면 사다리·돌림판 모두 애니메이션 없이 결과로 직행한다(전역 `!important` 규칙에 더해 JS 타이머도 짧게 잡는다).

---

## 이번 회차에 발견·수정한 버그
| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| B1 | 전체 메뉴판 스크롤 시 헤더 위로 메뉴가 살짝 비침 | `.sheet padding-top:8px` 때문에 sticky 헤더가 8px 아래에 붙어 그 틈으로 목록이 보임 | `.sheet` 상단 패딩 제거(그립 마진이 여백 유지) + 헤더 좌우 bleed·z-index↑ → 빈틈 0 |
| B2 | 스크롤된 시트를 닫고 다른 시트를 열면 새 시트가 스크롤된 채로 뜸 | `openSheet`가 `scrollTop`을 리셋 안 함 | `openSheet`에 `scrollTop=0` 추가 |
| B3(경미) | `member_diff` 함수가 anon에서도 실행됨 | Supabase 기본권한이 함수 생성 시 anon에 EXECUTE 부여 → `revoke from public`만으론 부족 | `revoke ... from public, anon, authenticated` (순수 함수라 무해하나 최소권한 정리) |

## 기능 TC (로컬 자동검증 — 전부 PASS)
**입장/이름**
- TC1 이름 미선택 → 본인선택 화면 / 선택 → 메인
- TC14 본인 이름 변경 → 숨은 id 유지·표시 갱신·주문 보존
- TC15 본인이 명단에서 삭제됨 → 본인선택 화면으로 안전 복귀(크래시 없음)

**섬김(호스트)/세션**
- TC2 호스트 자처 → open, host 지정
- TC8 마감 → closed / 마감취소 → open
- TC16 호스트가 명단에서 빠지면 → 세션 자동 해제(idle, 잠김 방지)

**주문**
- TC3 음료 주문 저장·표시
- TC7 주문 취소
- TC9 기존 주문 수정 시 옵션 상태 복원 + 배반(샷추가↔연하게)
- TC10 직접입력(커스텀) 주문 — 디카페인 옵션 없음(알려진 한계)
- TC11 디카페인은 커피계열만(아메리카노 O, 제주말차라떼 X)
- TC12 HOT 전용 메뉴(에스프레소) 온도 자동·고정
- TC17 "나도" — 남의 주문(옵션 포함) 복사

**현황/주문서**
- TC4 합계(잔 수) 정확
- TC5 카페 주문서 텍스트(음료·온도·옵션·합계) 생성
- TC6 분류(주문/안마심/대기) 정확
- TC18 주문 0건 마감 화면 크래시 없음
- TC22 주문서에 커스텀 메뉴 + 디카페인 포함, 크래시 없음
- TC23 커스텀 메뉴 이모지 폴백(🥤)

**전체 메뉴판**
- TC13 검색 필터 / 무결과 안내 / 검색어 지우면 복원
- B1 스크롤 시 헤더 위 비침 없음(수정 확인)

**다이얼로그/안내**
- TC20 커스텀 확인창 표시 / 뒤로가기=취소 / 확인=true
- TC21 호스트가 명단에서 빠져도 "안내 문구" 생성 크래시 없음

**기타**
- TC19 셀 전환 시 forceGrid 리셋
- 7글자 이름: 헤더/타이틀/목록/셀명 깨짐 없음, 가로 스크롤 0 (이전 회차)
- 이름 선택 화면 12초 폴링에 안 닫힘 (이전 회차)

## 백엔드 TC (라이브, 읽기 전용 — 전부 PASS)
- anon 직접 조회 차단: `orders/sessions/cells/member_logs` → 401/permission denied
- RPC 정상: `get_cell(demo)` 200, `get_room_day(demo)` 200
- 감사로그 적용 확인: `set_members` 3-인자 존재, `member_diff` 서버 diff 정상
- 회원가입 비활성: `/auth/v1/signup` 422

## 알려진 한계(의도 — 신뢰그룹 전제)
- 자기신고 actor(이름 도용 시 위조 가능), 로그 자동삭제 미도입
- 직접입력 메뉴는 디카페인 옵션 없음
- 세션은 당일(KST) 기준 — 자정 전 마감 권장
- 링크=권한(링크 아는 사람은 편집 가능, 단 수정내역은 로그됨)
