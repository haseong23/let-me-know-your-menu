# QA 테스트케이스 & 결과 (v9.33)

검증 방법: 스크래치패드 로컬 사본(키 blank=LocalSync, 백엔드 쓰기 없음)에서 Chrome 자동화로 플로우 실행 + 라이브는 읽기 전용 curl. 실데모/실셀에는 절대 쓰기 안 함.

---

## 모바일 키보드 패치 회차 (v9.52) — ⏳ 실기기 확인 대기

아래는 **아직 검증 안 된 항목**이다. 데스크톱 자동화로는 모바일 키보드를 재현할 수 없어 실기기 확인이 필요하다.

| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| B4 | 전체 메뉴판에서 타이핑하면 걸러진 메뉴가 안 보임 | `.scrim{inset:0}`·`.sheet{max-height:88vh}` 가 **레이아웃** 뷰포트 기준 → 키보드가 올라와도 시트가 안 줄어 결과 목록이 키보드 뒤로 밀림 | `visualViewport` 높이를 `--vvh`/`--vv-top` CSS 변수로 전달 + 검색 시트를 헤더/스크롤/푸터 3분할(`.sheet.split`) |
| B5 | 주문 후 '안 마실게요'가 13px 텍스트 링크로만 남아 누르기 어려움 | `showGrid` 가 false 라 버튼이 통째로 숨겨지고 `.nav-link` 만 노출 | 주문 후에도 `🔄 다른 메뉴로` / `🙅 안 마실게요` 2버튼(`.row-2`, 높이 ≥47px)으로 나란히 노출 |
| B6 | 배포했는데 반영됐는지 판별 불가 | 이전 회차가 `APP_VERSION` 을 안 올려 캐시본과 신규본 구분 불가 | v9.51 로 범프. **배포마다 반드시 올린다** |
| B7 | 키보드 올라온 상태에서 시트 하단과 키보드 사이에 **딤 안 된 띠**가 남음 (Galaxy/Chrome 실기기) | scrim 바닥을 `top:--vv-top + height:--vvh` 로 **계산**해서 만든 탓에 측정 오차가 그대로 구멍이 됨. 삼성 키보드의 자동완성·추천 줄이 키보드가 뜬 뒤에 붙어 키보드가 더 높아지는데 `resize` 를 한 번만 받아 그 증가분을 놓침 | scrim 을 `inset:0` 로 **항상 화면 전체**를 덮게 하고 `padding-bottom: var(--kb)` 로 시트만 밀어올림 → 측정이 틀려도 구멍이 구조적으로 안 생김. `--kb = innerHeight - (vv.height + vv.offsetTop)` + 이벤트 후 60·160·320·600ms 재측정 + `interactive-widget=resizes-content` |

| B8 | 메뉴 검색창을 누르면 비밀번호·카드 자동완성 아이콘 줄이 떠서 보이는 영역이 더 줄어듦 | `<input type="text">` 에 `autocomplete` 지정이 없어 Chrome/Android 자동완성 휴리스틱이 저장된 자격증명(관리자 로그인)을 이 도메인 필드에 제안 | 검색창을 `type="search"` + `autocomplete/autocorrect/autocapitalize=off` + `spellcheck=false` + `enterkeyhint=search` 로 변경. 나머지 비자격증명 input 11개에도 `autocomplete="off"` 일괄 적용(관리자 email·password 2개는 자동완성 유지) |

| B9 | 탭 영역이 44px 미만인 요소 8종 | `.chip` 36 · `.btn-sm` 35(좁은 화면 32) · `.nav-link` 16 · `#gear` 22 · `.sheet-x` 35 · `.tab` 36 | 전부 `min-height:44px`. 헤더 안 요소(`.nav-link`·`.sheet-x`)는 음수 마진으로 줄높이·시각적 위치 보존 |
| B10 | 사용 빈도 막대의 마감/진행중 두 색이 구분 안 됨 | `--brand #6f4e37` ↔ `--accent-ink #96631f` 가 **정상 시야 ΔE 10.2**(기준 15 미달). v9.54 에서 `--accent` 를 글자 대비 때문에 어둡게 하며 악화됨. 또 `--brand` 는 채도 0.057 이라 데이터 마크로는 회색으로 읽힘 | 상태를 색으로 나누지 않는다 — 막대는 `--data-1 #94520c`(채도 0.104, 검증 통과) 한 색, **마감=채움 / 진행중=윤곽** + 라벨 범례 |
| B11 | 대시보드가 모바일에서 안 읽힘 | 4개 섹션이 같은 무게로 전부 펼쳐짐 · 가로 스크롤 2개 중첩 · 모든 막대에 숫자 라벨 · 정보가 `title=`(터치에서 안 뜸)에만 있음 · 기간 필터가 표시용 pill | 히어로+KPI → 추세 → 사람 → 메뉴 4단 위계. 매트릭스·사람별 메뉴는 드릴다운 시트로. 값 라벨은 최댓값·마지막만. 탭 툴팁 도입. 기간 필터 7/30/90 복구 |

**TC24~TC33 (실기기 체크리스트)**
- [ ] TC24 iPhone Safari — 전체 메뉴판 → 검색창 탭 → 키보드 위로 결과 목록과 '닫기' 바가 계속 보임
- [ ] TC25 타이핑 중 검색창 아래 "검색 결과 N개" 갱신
- [ ] TC26 초성 검색 — `ㅇㅁㄹ` → 아메리카노 계열 노출 (자음만 입력했을 때만 초성 모드)
- [ ] TC27 Android Chrome — 주소창 축소/확장·화면 회전 후에도 시트 하단 안 잘림
- [ ] TC28 키보드 닫으면 시트가 원래 높이(88vh)로 복귀
- [ ] TC29 목록 끝에서 계속 스크롤해도 배경 페이지가 안 따라 움직임(`overscroll-behavior`)
- [ ] TC30 B1·B2 회귀 없음 — 헤더 위로 목록 안 비침 / 시트 닫고 다시 열면 목록 맨 위부터
- [ ] TC31 음료 주문 후 메인 화면에 '안 마실게요' 버튼이 '다른 메뉴로' 옆에 노출·동작 (B5)
- [x] TC32 키보드 올린 상태에서 시트 하단과 키보드 사이에 딤 안 된 띠 없음 (B7) — **v9.52 Galaxy/Chrome 확인 완료**, 계측값 `inner 726 · vv 726 · top 0 · kb 0 · sab 0` (레이아웃 뷰포트가 함께 줄어드는 경로로 동작)
- [ ] TC33 메뉴 검색창 탭 → 비밀번호·카드 자동완성 아이콘 줄이 안 뜸 (B8). 관리자 로그인 화면에서는 여전히 자동완성 동작
- [ ] TC34 검색창에서 키보드의 `검색` 키 → 키보드만 내려가고 목록은 필터된 상태 유지
- [ ] TC35 온도·옵션 칩과 수정/나도 버튼이 44px 이상이고 레이아웃이 안 깨짐 (B9)
- [ ] TC36 헤더 높이 변화 없음 — ⚙️ 설정·뒤로가기는 탭 영역만 넓혔다 (B9)
- [ ] TC37 관리자 📊 대시보드 — 기간 필터 7/30/90 전환 시 재집계됨 (B11)
- [ ] TC38 사용 빈도 막대: 마감=채움 / 진행중=윤곽으로 구분되고 범례에 라벨이 있음 (B10)
- [ ] TC39 막대·KPI·사람 행을 탭하면 툴팁이 뜸 (모바일에서 `title=` 은 안 뜬다) (B11)
- [ ] TC40 `날짜별 보기` / `사람별로 보기` 드릴다운 시트가 열리고 닫힘 (B11)
- [ ] TC41 데이터 없는 셀에서 대시보드가 크래시 없이 빈 상태 문구를 보여줌

**계측**: 주소 뒤에 `?vvdebug` 를 붙이면 화면 하단에 `inner · vv · top · kb · sab · 버전` 실측값이 표시된다. 키보드 관련 증상이 남으면 이 값을 캡처해 붙인다 — 추측 대신 숫자로 잡는다.

**확인 순서**: 푸터의 버전이 `v9.52` 인지 먼저 본다. 낮으면 캐시본이므로 재접속(카톡 인앱 브라우저는 앱 재시작) 후 다시 확인한다. GitHub Pages 는 `cache-control: max-age=600` 을 강제하므로 배포 후 최대 10분 지연이 정상이다. `<meta http-equiv="Cache-Control">` 3줄은 문서 자신에는 효과가 없다(참고용으로만 유지).

**미지원 폴백**: `visualViewport` 미지원 브라우저는 리스너를 안 걸고 CSS 폴백값(`100%`/`88vh`)으로 패치 전과 동일하게 동작한다. `min()` 은 Chrome 79+/Safari 11.1+, `overscroll-behavior` 는 iOS Safari 16+ 필요 — 둘 다 미지원 시 패치 전 동작으로 떨어질 뿐이다.

---

## 대시보드 기간·검색 회차 (v9.72) — ⏳ 실기기 확인 대기

| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| B12 | 관리자 대시보드에서 사람 이름을 치면 `하성` 이 `ㅎㅏㅅㅓㅇ` 으로 다 분리됨 | `input` 마다 `paintAdminDash()` 가 `$app.innerHTML` 을 통째로 갈아엎어 검색 `<input>` 자체가 새로 만들어짐 → 조합 중이던 IME 가 매 글자 끊기고 자모가 그대로 확정됨. 포커스·캐럿 복원 코드로는 조합 상태를 못 살린다 | 컨트롤 바 아래를 `#dashBody` 로 묶고 `paintAdminDash(true)` 가 그 안만 교체. 검색·셀 선택은 컨트롤 바를 안 바꾸므로 입력창이 살아 있고 IME 가 안 끊긴다. 포커스 복원 해킹 제거 |
| B13 | 셀 단위 대시보드(`?cell=…&view=dashboard`)가 `집계하는 중…` 에서 멈춤 | 정의된 적 없는 `dashPeriodHtml()` 을 호출 → 템플릿 평가 중 ReferenceError 로 렌더가 통째로 중단 | `dashDatesHtml`·`dashPresetsHtml`·`dashWarnHtml` 로 쪼개고 `dashPeriodHtml`(검색 없는 기간 전용)을 실제로 정의 |
| B14 | 기간 필터가 `7일/30일/90일/1년` 이라 "이번달 얼마나 썼나"를 바로 못 봄 | 프리셋이 달력이 아니라 '오늘부터 N일' 롤링 | 달력 기준 6종으로 교체 — `이번주`(주일 시작)·`지난달`·`이번달`·`최근3개월`(90일 롤링 유지)·`올해`·`작년`. 한 줄 가로 스크롤 + 선택 버튼 자동 센터링. 기본값도 최근 30일 → `이번달` |
| B15 | `오늘은 안 마실게요` 버튼이 있는 줄 사람들이 모름 → 미응답으로 남아 섬기는 분이 일일이 물어야 함 | 위치가 아니라 **무게** 문제. `.btn-ghost`(투명 배경 + `--sub` 회색)라 베이지 배경에 묻혀 `닫기` 같은 부속으로 읽힘. 바로 위 메뉴 타일이 흰 카드로 시선을 다 가져감 | ① 배너 첫 줄을 `드시고 싶은 메뉴를 골라주세요` → `메뉴를 고르거나, 안 드실 땐 알려주세요` 로 — 맨 위라 반드시 읽힌다. ② 버튼을 `.label`(`음료를 안 드신다면`)로 감싸 `메뉴 선택`과 같은 급의 **섹션**으로 승격. ③ 스타일을 `.btn-choice`(흰 카드 + `--ink` 글자)로 — 이 앱에서 누를 수 있는 건 전부 흰 카드다. 이모지를 앞으로 빼 스캔되게 함 |

**TC42~TC47 (실기기 체크리스트)**
- [ ] TC42 관리자 대시보드 검색창에 `하성` 입력 → 자모 분리 없이 그대로 들어가고 결과가 즉시 좁혀짐 (B12)
- [ ] TC43 같은 창에 `ㅎㅅ` 초성만 입력 → 초성 매칭 동작, 지웠을 때 전체 복원
- [ ] TC44 검색 중 셀 카드를 눌러도 검색어·포커스가 유지되고 아래 상세만 바뀜 (B12)
- [ ] TC45 셀 링크 대시보드가 기간 컨트롤과 함께 정상 렌더 (B13)
- [ ] TC46 프리셋 6개가 한 줄에 있고, 좁은 화면에서 가로로 밀린다. 누른 버튼이 화면 안으로 당겨짐 (B14)
- [ ] TC47 프리셋별 구간이 헤더 `from ~ to` 와 일치 — 특히 `지난달` 말일, 1월의 `지난달`(작년 12월), `작년` 1/1~12/31 (B14)
- [ ] TC48 주문 전 화면에서 `음료를 안 드신다면` 섹션과 흰 버튼이 스크롤 없이 보임 (B15) — 실기기에서 **접히는지**가 핵심
- [ ] TC49 `🙅 안 마실게요, 감사합니다` → `안 마시기로 했어요` 카드로 바뀌고, 그 뒤 메뉴를 고르면 주문으로 되돌아감(되돌릴 수 있는 선택)
- [ ] TC50 주문 후 `🔄 다른 메뉴로` 를 누른 상태에서 `안 마실게요` 버튼이 **한 개만** 보임(중복 없음)

**날짜 계산**: 기준일은 `todayKey()`(KST 고정), 계산은 `YYYY-MM-DD` 문자열 + UTC 로만 한다. 윤년 2월·1월 롤오버·월말은 node 로 검증했다(2026-07-31 / 2026-01-01 / 2026-03-01 / 2026-08-02 / 2024-02-29 / 2026-12-31).

---

## 랜덤 게임 + 탭 내비게이션 회차 (v9.72) — ⏳ 실기기 확인 대기

**설계 판단 3가지** (구현보다 이게 먼저다)

1. **게임은 절대 저절로 안 뜬다.** 자진해서 섬기려던 분의 결정을 게임이 가로채면 안 되므로, `[🎲 게임으로 정하기]`를 직접 눌렀을 때만 시작한다. 호스트가 있는 섬김은 **그 호스트만** 열 수 있다(남의 섬김을 뺏는 경로가 없다).
2. **주문이 모인 뒤에 돌린다.** 사는 사람을 먼저 정하면 그 뒤 주문이 전부 눈치 게임이 된다. 주문 후면 ①아무도 사는 사람을 모른 채 고르고 ②`안 마실게요` 한 분이 후보에서 자동으로 빠지고 ③늦게 온 분도 주문만 하면 후보에 들어온다.
3. **결과에 구속력이 없다.** `[확정하기]` 전에는 서버에 아무것도 안 쓴다. `한 판 더`·`그만두기`가 항상 있고, 당첨자 본인에게는 메뉴를 다시 고를 기회를 준다(사는 줄 모르고 고른 메뉴를 바꿀 수 있어야 한다).

| # | 항목 | 판단 | 조치 |
|---|---|---|---|
| G1 | 게임 3종 선정 | 카톡 인앱·사파리·삼성인터넷에서 **똑같이 동작하는 것만** 넣는다 | 🪜사다리타기 · ☝️손가락 룰렛 · 🎡돌림판. 전부 단일 기기, 디바이스 센서 0개 |
| G2 | 흔들기(DeviceMotion) 채택 안 함 | iOS 13+ 는 사용자 제스처 안에서 `requestPermission()` 이 필요한데 카톡 인앱에서 프롬프트가 안 뜨는 사례가 많고, 안드로이드 인앱 웹뷰도 센서 이벤트가 안 오는 기기가 있다 | 후보에서 제외. 진동은 `navigator.vibrate` 가 있을 때만 보조로 쓰고(iOS 는 API 자체가 없음) 시각 효과를 본체로 |
| G3 | 멀티터치가 핀치 줌으로 먹힘 | 뷰포트 확대는 접근성상 열어둬야 한다(a2f6297) → 전역으로 못 막는다 | 게임 오버레이에만 `touch-action:pan-y`(손가락 룰렛 진행 중엔 `none`) + `user-select/touch-callout:none` + `touchmove`·`gesturestart`·`gesturechange` preventDefault |
| G4 | 호스트 교체가 섬김을 지움 | `unclaim_serving` 은 **주문이 없으면 섬김 자체를 delete** 한다 | 게임 진입점을 주문 1건 이상일 때만 노출. 그러면 `unclaim → claim_host` 2콜로 **DB 마이그레이션 없이** 호스트만 갈아끼운다(seq·주문·카페 전부 보존) |
| G5 | 사다리 확률이 그림에 좌우됨 | 사다리 구조에 따라 도착 칸 분포가 치우칠 수 있다 | **당첨자를 먼저 균등 추첨**하고 ☕를 그 사람 도착 칸에 놓는다. 그림은 결과를 향해 그릴 뿐. 매핑이 전단사인지 33,000회 검증(위반 0) |
| G6 | 돌림판 이름이 거꾸로 섬 | 레이블이 회전 그룹 안에 있어 판과 같이 돌아 멈춘 각도 그대로 뒤집힘 | 최종 회전각을 먼저 구해 각 `<text>` 에 `rotate(-spin)` 을 제자리에 걸어둔다 → 멈춘 순간 항상 똑바로 |
| G7 | 인원 많을 때 사다리 이름 겹침 | 세로줄 간격(292/(n-1))보다 이름이 넓다 | 인원에 맞춰 글자수·크기 축소(≤7명 4자/11px · ≤9명 3자/10px · 그 이상 2자/8.5px) + 전체 이름은 `<title>` 과 결과 발표에 |
| G8 | 결과 발표가 화면 밖으로 밀림 | 12명·568px 화면에서 사다리+발표가 본문 높이를 넘음 | `.game-body` 를 스크롤 가능하게(+`::before/::after{margin:auto}` 로 여유 있으면 가운데) 하고, **푸터 버튼이 들어간 다음 프레임에** 끝까지 스크롤 |
| N1 | `[현황 보기] ↔ [내 주문]` 왕복이 나쁨 | 둘이 **별개 페이지**라 볼 때마다 떠났다 돌아와야 했다. 게다가 현황 안에 또 `[사람별 \| 카페 주문서]` 탭이 있어 2단 | 계층을 한 줄로 펴서 **`[내 주문 \| 현황 N잔 \| 주문서]` 3탭**. 페이지 이동이 아니라 탭 전환이라 헤더·하단바가 그대로다 |
| N2 | 하단바 버튼이 화면마다 바뀜 | 주문화면 `[현황][마감]` ↔ 현황화면 `[섬김취소][주문 마감하기]` | 현황이 탭으로 올라갔으므로 하단바는 호스트 `[섬김 취소][주문 마감하기]` 하나로 고정. 호스트가 아니면 하단바 없음(+`body.no-bottom` 으로 아래 여백도 걷음) |
| N3 | 뒤로가기 동작 보존 | 탭은 보통 히스토리를 안 쌓지만, 기존엔 현황이 페이지라 뒤로가기로 돌아왔다 | 기본 탭을 **떠날 때만** `pushSub()` 1칸 → 뒤로가기 한 번이면 `내 주문` 탭. 앱 밖으로 안 튕긴다 |
| **G9** | **데모에서 게임이 아예 안 보임** (실사용 제보) | 호스트용 진입점을 `현황` 탭 **안에만** 뒀는데 기본 탭이 `내 주문` 이다. 사용자 여정 5단계를 재현하니 **탭을 직접 옮긴 1곳에서만** 보였다. 나머지 진입점(인계 대기 하단바)은 호스트가 `섬김 취소`를 눌러야 생기는 희귀 상태라 실질적으로 안 뜬다 | ① 기본 탭(`내 주문`)에도 같은 진입점을 둔다 — B15와 같은 처방으로 `.label`(`누가 섬길지 아직 안 정했다면`) + `.btn-choice` 흰 카드. ② 섬김이 없는 화면에 안내 한 줄(`섬길 사람은 주문을 받은 뒤 🎲 게임으로 정할 수도 있어요`) — 진입점이 아니라 **언제 생기는지**를 알려준다. ③ 여정 재현 테스트를 회귀 가드로 고정 |

| **G10** | **사다리에 내려가는 애니메이션이 없음** (실사용 제보) | 두 가지가 겹쳤다. ① `stroke-dashoffset` 에 단위를 안 붙여(`663.4`) CSS `<length>` 로 안 먹고 computed 가 `0px` 로 떨어졌다. ② 시작값을 설정한 직후 `requestAnimationFrame` 한 번만 두고 끝값을 줬는데, 그 사이 스타일이 커밋되지 않아 두 값이 같은 재계산에 묶였다 — 보간할 구간 자체가 없다 | 길이에 `px` 를 붙이고, 시작값 설정 → `void offsetHeight`(리플로우 1회로 커밋) → 끝값 순서로 바꾼다. 계측으로 확인: dashoffset 605→487→374→250→94→27→0 |

> **G10 참고**: 돌림판은 같은 rAF 패턴인데 **정상 동작**했다. 시작 transform 이 `innerHTML` 안에 인라인으로 들어가 첫 렌더에 이미 커밋되기 때문이다. 같은 코드 모양이라고 같은 버그가 아니므로 하나씩 계측했다.

> **G9·G10 교훈**: 기능이 "있다"와 "보인다"·"움직인다"는 다르다. 단위 테스트는 `버튼이 DOM 에 있는가`만 물었고 전부 통과했다 — **기본 탭에서 보이는가**를 안 물었기 때문이다. 이후 진입점을 만들 땐 사용자 여정을 처음부터 밟아 각 단계에서 보이는지를 테스트한다.


**이번 통합(R12)이 위 세 결정을 어떻게 지키는가**

- **N1** — 3탭을 2탭으로 더 줄였다. `내 주문`과 `현황 n잔`이 한 스크롤 뷰가 되어 왕복 자체가 사라졌고, 같은 사람을 두 번 그리던 중복(`다른 분 주문` 블록 vs 로스터의 `주문` 그룹)도 함께 없앴다. `[사람별 | 카페 주문서]` 2단 탭을 열린 화면에 들여오지 않는다는 원칙은 그대로다.
- **N2** — `renderBottom()` 은 여전히 `openTab` 을 읽지 않는다. 탭이 둘로 줄어도 하단바 구성은 안 바뀐다. **TC60 은 이 결정의 회귀 가드라 재작성하지 않는다.**
- **N3** — `pushSub()`·`subActive`·`closeSheet()`·`popstate`·`clearSubEntry()`·`setOpenTab()`·`commitGameWinner` 의 `openTab="order"` 대입까지 **일곱 지점 모두 변경 0**이다. 합친 탭이 키 `order` 를 유지하고 `cafe` 가 유일한 비기본 탭으로 남기 때문에 "기본 탭을 떠날 때만 1칸" 이 그대로 성립한다. 합친 화면 안에 접기 같은 화면 내 상태를 넣지 않은 것도 이 결정을 지키기 위해서다 — 기본 탭에서 `pushState` 를 하는 순간 그 불변식이 깨진다.

**자동 검증 (스크래치패드 로컬 사본 · CDP 디바이스 에뮬레이션)** — 47/47 통과
- 게임 상태 전이 17종: 자진 호스트/인계 대기 양쪽에서 호스트 교체 후 seq·주문·카페·open 상태 보존, `안 마심`/`오늘 섬김` 후보 제외, 2명 미만이면 전원 참가 복귀, 뒤로가기로 오버레이만 닫힘, 닫을 때 타이머 정리
- 진입점·탭 16종: **기본 탭에서 게임 버튼이 보이는지**(G9 회귀 가드) · 주문 0→1건 전환 · 섬김 없는 화면 안내 · 주문 0건이면 게임 버튼 없음, 비호스트면 없음, 마감이면 없음, **마감을 눌러도 게임이 저절로 안 뜸**, 탭 전환 시 헤더·하단바 동일, 뒤로가기로 `내 주문` 복귀
- 클릭 전 경로 11종: 현황 탭 → 게임 → 사다리 → 확정까지 실제 DOM 클릭만으로
- 애니메이션 3종: 사다리 dashoffset 이 단조 감소하는지 · 시작 시 완성돼 있지 않은지 · 돌림판 행렬이 계속 바뀌는지 (G10 을 잡은 테스트)
- 사용자 여정 5단계: 이름 선택 → 섬김 시작 → 주문 → 탭 이동 → 다인 주문, 각 단계에서 게임이 보이는지 (G9 를 잡은 테스트)
- 수학: 사다리 전단사 33,000회 위반 0 · 추첨 균등성 12만회(6명, 16.57~16.82%) · 돌림판 핀 정확도 4만4천회 오차 0

**TC51~TC60 (실기기 체크리스트)**
- [ ] TC51 카톡 인앱(안드로이드) — 손가락 룰렛에서 손가락 3~5개를 동시에 올렸을 때 **화면이 확대되지 않고** 원이 손가락마다 따라붙음 (G3)
- [ ] TC52 iOS 사파리 — 같은 동작. 손가락을 떼면 카운트다운이 리셋되고, 3초 유지하면 하나가 뽑힘
- [ ] TC53 삼성인터넷 — 사다리 애니메이션이 끊기지 않고, 12명에서도 이름이 안 겹침 (G7)
- [ ] TC54 돌림판이 멈춘 뒤 이름이 **똑바로** 읽힘 (G6) — 여러 번 돌려 각도를 바꿔가며
- [ ] TC55 좁은 화면(≤360px)에서 결과 발표(☕+이름)가 잘리지 않고 보임 (G8)
- [ ] TC56 자진해서 섬기기로 한 호스트가 `마감하기`를 눌렀을 때 게임이 **안 뜬다** (확인창만)
- [ ] TC57 게임 확정 후 다른 사람 폰에서도 10초 안에 호스트가 바뀐 게 보임(폴링) · 섬김 번호와 주문이 그대로
- [ ] TC58 당첨자가 본인이면 합친 화면에서 메뉴를 다시 고를 수 있음
- [ ] TC59 탭이 `[주문 · 현황 | 카페 주문서]` **둘뿐**이다. 전환 시 화면이 깜빡이지 않고, 합친 화면(≈1,100px)에서 `카페 주문서`(≈370px)로 갔다가 돌아오면 스크롤이 맨 위로 간다(높이 차가 커서 이전 위치를 복원하지 않는 게 정상). 뒤로가기 한 번에 합친 화면 (R12-1, R12-19)
- [ ] TC60 호스트가 아닌 사람 화면에서 하단바가 없고 본문 아래 빈 여백도 없음 (N2)
- [ ] TC61 데모 셀에서 처음부터 밟기 — 섬김 시작 → 메뉴 하나 고름 → 합친 화면을 **한 번 플릭**하면 `🎲 게임으로 섬길 사람 정하기`가 보인다. 게임 섹션은 `지금 현황`·`다른 분들` 로스터보다 **위**에 있어야 한다 — 로스터 아래로 내려가 있으면 실패 (G9, R12-11)
- [ ] TC62 섬김이 없는 화면에 `주문을 받은 뒤 🎲 게임으로 정할 수도 있어요` 안내가 보임 (G9)
- [ ] TC63 사다리타기 — 색선 6개가 **위에서 아래로 그려지며 내려가는 게 보임**(약 4.8초). 시작하자마자 완성돼 있으면 실패. 선이 갈라지는 지점에서 "누가 어디로 갔는지"를 눈으로 좇을 수 있어야 한다 (G10)
- [ ] TC64 `설정 > 손쉬운 사용 > 동작 줄이기`를 켜면 사다리·돌림판이 애니메이션 없이 결과로 직행

**움직임 최소화**: `prefers-reduced-motion` 이 켜져 있으면 사다리·돌림판 모두 애니메이션 없이 결과로 직행한다(전역 `!important` 규칙에 더해 JS 타이머도 짧게 잡는다).

---

## 게임 참가자·확률·히스토리 개선 회차 (v9.72 → 배포 시 범프) — ⏳ 실기기 확인 대기

스펙 `game-picker-ui-fixes`(R1~R18)로 바꾼 것들의 회귀 케이스다. 순수 로직은 `node --test` 20개가 지키므로, 아래에는 **눈이나 손으로만 확인되는 것**만 남겼다.

**준비**: 데모 셀에서 구성원 4명 이상 · 그중 2명 주문 · 1명 `안 마실게요, 감사합니다` · 1명 무응답 상태를 만든다. 인원 케이스(TC90·TC91·TC103·TC104)는 구성원 25명 이상인 셀이 따로 필요하다. 실시간 케이스(TC96~TC99)는 폰 2대(또는 창 2개)로 A·B를 나눈다.

**TC65~TC72 (참가자 고르기 — 토글 스위치와 기본값)**
- [ ] TC65 참가자 고르기 시트 — 주문한 사람 행은 **초록 트랙 + 손잡이 오른쪽**, 무응답 행은 **회색 트랙 + 손잡이 왼쪽**. 화면을 흑백으로 봐도 손잡이 위치만으로 구분된다 (R1-2, R1-3)
- [ ] TC66 스위치의 보이는 트랙 바로 위·아래 6px 지점(트랙 밖)을 탭 → 토글이 바뀐다. 그런데도 행 높이는 스위치가 없던 때와 같아 목록이 늘어나지 않는다 (R1-4)
- [ ] TC67 스위치를 끈 행의 이름·부제가 흐려지되 밝은 실외 밝기에서도 글자가 읽힌다. 아바타 원은 여전히 갈색(`--brand`)이고 회색으로 바뀌지 않는다 (R1-9)
- [ ] TC68 구성원 12명 이상인 셀에서 목록을 끝까지 내려 마지막 행의 스위치를 누른다 → 목록이 맨 위로 튀지 않고 그 자리에 머문다 (R7-1)
- [ ] TC69 같은 스위치를 연달아 3번 누른다 → 매번 포커스 링이 그 스위치에 남고, 상단 `현재 N명` 숫자가 누를 때마다 1씩 오르내린다 (R7-2, R7-3)
- [ ] TC70 목록을 내린 상태에서 `전원 참가` → 모든 행의 스위치가 켜지고 스크롤 위치는 그대로다. `오늘 섬긴 분 빼기` 도 같다 (R7-4)
- [ ] TC71 게임 진입점 → 시트의 `참가자 N명` 이 **음료를 주문한 사람 수**와 같다. 무응답자·`안 마실게요` 한 사람은 세어지지 않고, `메뉴를 고른 분이 기본으로 들어가 있어요` 안내가 보인다 (R2-1, R2-2, R2-3, R2-6)
- [ ] TC72 무응답 행의 부제가 `아직 주문 전 — 넣으려면 켜주세요` 이고 스위치를 켜면 `현재 N명` 이 1 오른다. `안 마실게요` 행에는 스위치 대신 `안마심` 배지만 있어 켤 방법이 없다 (R2-4, R2-5)

**TC73~TC79 (명단·정책 연동)**
- [ ] TC73 한 명을 끄고 `완료` → 사다리타기의 세로줄 수를 센다 → `그만두기` 후 돌림판을 고른다 → 돌림판 조각 수가 사다리와 같다 (R3-1)
- [ ] TC74 결과 화면에서 `🎲 한 판 더` → 참가자 수와 명단이 직전 판과 같다 (R3-2)
- [ ] TC75 게임을 ✕로 닫고 같은 섬김에서 진입점을 다시 누른다 → 껐던 사람이 여전히 꺼져 있고 정책 칩 선택도 그대로다. 다른 `N번째 섬김` 으로 옮겨 열면 기본값으로 돌아와 있다 (R3-3, R3-4, R6-8)
- [ ] TC76 사다리타기 **시작 순간** 도착 칸 하나에 ☕ 가 이미 있고 나머지는 `·` 다. `?` 칸은 하나도 없고, ☕ 칸만 배경·테두리가 다르다 (R4-1, R4-2, R4-8)
- [ ] TC77 같은 판에서 색선이 위에서 아래로 약 2.4초 동안 그려지는 게 보인다. 다 그려진 뒤 ☕ 칸에 닿은 선의 색이 결과 발표에 나온 사람의 색과 같고, 칸을 하나씩 열어 보이는 연출은 없다 (R4-3, R4-4, R4-5)
- [ ] TC78 정책 카드에서 `제외` 를 누른다 → 아래 `참가자 N명` 이 그 자리에서 1 줄어든다. `확률⬇️`/`평등` 으로 되돌리면 숫자도 되돌아오고 시트 스크롤은 안 튄다 (R6-6, R7-6)
- [ ] TC79 정책 카드에 `직전에 섬긴 분: OOO님` 으로 **실제 이름**이 나오고, 처음 열었을 때 선택된 칩은 `확률⬇️` 다. 직전에 섬긴 분이 없는 셀에서는 카드 자체가 안 보인다 (R6-1, R6-2, R6-3, R6-12)

**TC80~TC86 (손가락 룰렛 스크롤 · 뒤로가기)**
- [ ] TC80 손가락 룰렛을 끝까지 진행해 당첨자를 지목한 뒤 결과 화면을 손가락으로 쓸어올린다 → 화면이 스크롤되고 `확정하기`·`한 판 더`·`그만두기` 세 버튼 모두에 닿는다. 좁은 화면(≤360px)에서 본다 (R8-1, R8-2)
- [ ] TC81 합친 화면 → `카페 주문서` 탭 → 뒤로가기 1회 → 합친 화면. 2회 → 앱 밖. 1회에서 앱이 닫히거나 2회에서 아무 일도 안 일어나면 실패 (R11-1, R11-2, R12-2)
- [ ] TC82 합친 화면 → 게임 실행 → 뒤로가기 1회 → 게임만 닫히고 합친 화면. 2회 → **화면에 아무 변화가 없다**(시트가 남긴 항목을 조용히 소비한다). 3회 → 앱 밖. 2회에서 앱이 닫히면 실패 (R11-3, R12-2)
- [ ] TC83 게임 고르기 시트에서 게임을 실행한 뒤 뒤로가기 1회 → 오버레이와 시트가 **함께** 닫히고 게임을 열기 전 탭이 보인다 (R11-4)
- [ ] TC84 게임을 ✕ 또는 `그만두기` 로 닫은 뒤 뒤로가기 → 게임을 안 열었을 때와 같은 곳으로 간다. 진입점이 합친 화면 하나로 줄어 목적지도 하나다 — 시트를 지나왔으면 그 항목 1칸을 소비하고, 아니면 앱 밖 (R11-5, R12-10)
- [ ] TC85 참가자 고르기 → `완료` → 게임 고르기 시트로 돌아온 뒤 뒤로가기 1회 → 시트가 닫히고 원래 탭이 보인다. 두 번 눌러야 나가면 실패 (R11-7)
- [ ] TC86 주문 마감처럼 확인창이 뜨는 동작에서 뒤로가기 → 확인창만 닫히고 **마감이 실행되지 않는다**. 이어서 뒤로가기가 앱 안에서 계속 동작한다 (`ignorePop` 카운터화 회귀) (R11-5)

**TC87~TC92 (확정 · 돌림판 · 인원 · 폴백)**
- [ ] TC87 합친 화면에서 게임을 열어 내가 당첨된 판을 만들고 `✅ 이분으로 확정하기` → 합친 화면으로 돌아오고 메뉴 그리드가 **펼쳐진 채로** 보인다. 그리드가 접혀 있으면 실패 (R12-1, R12-2)
- [ ] TC88 같은 것을 `카페 주문서` 탭을 들렀다 온 뒤(히스토리 1칸이 남은 상태) 확정 → 합친 화면에 그리드가 펼쳐지고, 이어지는 뒤로가기가 앱 밖으로 튀지 않는다 (R12-3, R12-2)
- [ ] TC89 돌림판이 약 4.4초 동안 돌아가는 게 보인다 — 시작하자마자 결과 각도로 서 있으면 실패(TC63 사다리 케이스와 짝). 멈춘 뒤 모든 이름이 똑바로 읽히고 핀(12시) 아래 조각이 결과 발표와 같은 사람이며, `한 판 더` 로 다시 돌려도 매번 회전이 보인다 (R14-1, R14-3, R14-4, R14-5)
- [ ] TC90 참가자 11명으로 사다리타기와 돌림판을 돌린다 → 라벨이 이름 대신 번호(1~11)로 바뀌고 그림 아래에 `번호 · 색 · 전체 이름` 범례가 보인다. 라벨끼리 겹치지 않고 범례의 이름은 잘리지 않는다 (R9-5, R10-1, R10-2)
- [ ] TC91 참가자 17명 → `사다리타기` 부제가 `16명까지 — 지금 17명이라 화면에 안 들어가요` 로 바뀐다. 25명이면 `돌림판` 에도 같은 형태의 문구가 붙고, `손가락 룰렛` 은 항상 열려 있다 (R10-4)
- [ ] TC92 `db-migration-last-host.sql` 을 적용하지 않은 셀에서 게임을 연다 → 오류 토스트 없이 시트가 뜨고, 정책 카드는 오늘 섬김 기록이 있을 때만 나온다. 게임 실행부터 확정까지 정상 동작한다 (R5-6, R5-8)

**TC93~TC95 (손가락 룰렛 안정화 — 구현 중 드러난 것)**
- [ ] TC93 손가락 2개를 올리고 가만히 둔다 → `손가락 2개 — 다 올리셨으면 곧 시작해요` 가 약 0.9초 유지된 뒤 `3·2·1` 이 시작된다. 2개를 올린 즉시 3부터 세면 실패 (R15-3, R15-4)
- [ ] TC94 `3` 이 뜨기 전에 세 번째 손가락을 올린다 → 카운트다운이 멈추고 `손가락 3개 — …` 대기 문구로 돌아간다. 그대로 두면 다시 3부터 센다 (R15-3)
- [ ] TC95 손가락 하나를 게임 본문 가장자리(헤더·푸터 쪽) 밖으로 밀어 넘긴다 → 그 점이 경계에 붙어 **남아 있고** 카운트다운이 리셋되지 않는다. 화면에서 실제로 떼면 그때 점이 사라진다 (R15-1, R15-2)

**TC96~TC100 (게임 중 실시간 갱신과 확정 안전 — 구현 중 드러난 것)**
- [ ] TC96 A에서 게임을 시작한 뒤 B에서 새 주문을 넣는다 → A의 결과 위에 `게임 도중 참가자가 바뀌었어요 — 한 판 더 돌리시겠어요?` 배너가 뜨고, `한 판 더` 를 누르면 배너가 사라진다 (R17-1)
- [ ] TC97 A의 결과가 뜬 뒤 B에서 당첨자를 명단에서 빼거나 `안 마실게요` 로 바꾼다 → A에 `당첨되신 분이 방금 명단에서 빠졌어요 — 한 판 더 돌려주세요` 가 뜨고 `확정하기` 버튼이 **사라진다**. `한 판 더`·`그만두기` 는 남는다 (R17-2)
- [ ] TC98 A가 확정을 누르는 찰나에 B가 먼저 섬김을 이어받게 만든다 → A에 `앗, 방금 다른 분이 먼저 이어받으셨어요 — [한 판 더]로 다시 돌려보세요` 토스트가 뜨고 **오버레이가 닫히지 않으며** `한 판 더` 가 그대로 있다 (R17-4)
- [ ] TC99 **데이터 안전 핵심.** 호스트가 있는 섬김에서 주문을 전부 취소해 0건으로 만들고 `전원 참가` 로 참가자 2명을 채운 뒤 확정을 누른다 → `아직 주문이 없어서 지금 넘기면 이 섬김이 사라져요 — 주문이 모인 뒤에 확정해주세요` 로 막히고, 헤더의 `N번째 섬김` 번호가 그대로다. 번호가 올라가면 섬김이 지워졌다 다시 만들어진 것이므로 실패 (R16-5)
- [ ] TC100 `확정하기` 를 누른 직후 버튼이 흐려지며 `확정하는 중…` 으로 바뀐다. 그 상태에서 한 번 더 눌러도 토스트가 한 번만 뜨고 호스트도 한 번만 바뀐다 (R18-1, R18-2)

**TC101~TC104 (오버레이 포커스 · 상한 — 구현 중 드러난 것)**
- [ ] TC101 게임 오버레이가 열린 상태에서 Tab 을 계속 누른다 → 포커스가 ✕ → 푸터 버튼들 → 다시 ✕ 로 돌고 아래 화면·하단바로 나가지 않는다. Shift+Tab 은 반대로 순환한다 (R13-1, R13-2)
- [ ] TC102 게임을 ✕ 또는 뒤로가기로 닫는다 → 포커스 링이 **게임을 열었던 진입점 버튼**에 돌아와 있다(Tab 을 한 번 누르면 그 다음 요소로 간다). 진입점이 합친 화면의 `#gamepick` 하나와 하단바 `#gamepickBar` 둘뿐이라 어느 쪽에서 열었는지가 그대로 드러난다 (R13-3, R12-10)
- [ ] TC103 상한을 넘긴 게임 버튼은 흐리게 보이고 눌러도 아무 일이 없다 — 시트가 닫히거나 게임이 시작되면 실패. 셋 중 최소 하나(`손가락 룰렛`)는 항상 누를 수 있다 (R10-4)
- [ ] TC104 참가자 15명으로 사다리타기를 돌린 뒤 결과 화면을 열어둔 채 B에서 주문을 늘려 17명이 되게 한다 → A에서 `한 판 더` 를 누르면 사다리가 시작되지 않고 `16명까지 — 지금 17명이라 화면에 안 들어가요` 토스트가 뜬다. `한 판 더` 는 시트를 지나지 않으므로 실행 지점에서 막는다 (R10-4)

**자동 검증**: `node --test` 20개 — 가중 추첨 수렴(`확률⬇️` = 다른 분의 1/3, 0 아님) · 명단 파생(`안 마심` 하드 제외, `on`/`off` 우선) · 색 배정(2~60명 중복 0) · 사다리 기하 불변식(GAP≥18·BOX_W≥14, 상한 16의 근거). 위 실기기 케이스는 이 20개가 못 보는 것만 남긴 목록이다.

---

## 🏁 구슬 레이스 회차 (v9.72 → 배포 시 범프) — ⏳ 실기기 확인 대기

앞 절과 절을 나눈 이유: 앞 절은 스스로 범위를 `R1~R18 회귀`로 못박았고 준비 조건(구성원 4명·주문 2건)도 그 케이스들에 맞춰져 있다. 구슬 레이스는 회귀가 아니라 **새로 얹힌 네 번째 게임**(R19)이고 필요한 준비물도 다르다 — 20초를 재는 스톱워치, 25명 셀, `동작 줄이기` 설정, iOS 사파리 실기기. 한 절에 섞으면 준비물이 두 겹이 되어 밟는 사람이 어느 쪽 조건인지 헷갈린다.

**준비**: 참가자 3~6명(대부분의 케이스) · 24명(TC119) · 25명(TC106) 세 벌을 만든다. 시간은 `출발!`이 뜬 순간부터 결과 발표가 나올 때까지 스톱워치로 잰다. 캔버스 그림은 자동화로 못 보므로 아래는 전부 눈으로 확인한다.

**TC105~TC107 (진입점 · 상한 · 소요 시간)**
- [ ] TC105 게임 고르기 시트를 연다 → `🏁 구슬 레이스` 가 `사다리타기`·`손가락 룰렛`·`돌림판` 다음 **네 번째**로 있고, 부제에 `다 같이 20초 · 제일 오래 즐겨요` 가 보인다. 순서가 다르거나 부제에 소요 시간이 없으면 실패 (R19-1, R19-2)
- [ ] TC106 참가자 25명인 셀에서 시트를 연다 → `구슬 레이스` 버튼이 흐려지고 부제가 `24명까지 — 지금 25명이라 화면에 안 들어가요` 로 바뀐다. 눌러도 시트가 닫히지 않고 게임도 시작되지 않는다. 24명으로 한 명 줄이면 다시 눌린다 (R19-12)
- [ ] TC107 참가자 4~6명으로 레이스를 돌린다 → `출발!` 부터 결과 발표까지 **18~22초**(설계 목표 약 20초) 안에 끝난다. 그리고 결승선 체커 무늬를 **가장 먼저 통과한 구슬의 번호·색**이 결과 발표에 나온 사람의 번호·색과 같다. 2위가 먼저 들어오는 그림이 한 번이라도 보이면 실패 (R19-3, R19-4)

**TC108~TC111 (레이스 연출 — 역전 · 카메라 · 구슬 · HUD)**
- [ ] TC108 참가자 3명 이상으로 3판을 돌린다 → 매 판 선두(어두운 테두리 구슬)가 **최소 3번** 바뀌는 게 눈에 보인다. 참가자 2명일 때는 문턱이 1회이므로 **최소 1번** 바뀌면 통과다(두 구슬의 도착 시간 차가 고정이라 4번 교차가 구조적으로 드물다 — R19-5 참고). 처음부터 끝까지 같은 구슬이 앞서 있으면 실패 (R19-5)
- [ ] TC109 레이스가 진행되는 동안 화면을 본다 → 코스가 위로 흘러가며 화면이 선두를 따라 내려가고, 선두 구슬이 **처음·중간·결승선 도착 순간 모두** 캔버스 안에 온전히 보인다(위아래 테두리에 반쯤 잘리면 실패). 코스 끝을 지나 빈 배경만 보이는 구간이 없다 (R19-10)
- [ ] TC110 구슬을 하나씩 본다 → 참가자마다 색이 다르고 흰 번호(1~N)가 박혀 있다. 그 순간 1위인 구슬만 **어두운 테두리**로 굵게 강조되고, 선두가 바뀌면 강조도 따라 옮겨간다. 강조가 두 구슬에 동시에 걸리면 실패 (R19-6)
- [ ] TC111 캔버스 아래 HUD를 본다 → 진행률 바가 왼쪽부터 차오르고, 상위 3명 줄(`1·2·3` + 색 조각 + 번호 + 이름)이 레이스 중 계속 갱신된다. 순위가 뒤집힐 때 HUD도 같이 뒤집힌다. 바가 멈춰 있거나 상위 3명이 처음 값에서 고정이면 실패 (R19-9)

**TC112~TC115 (건너뛰기 · 움직임 최소화 · 중단 · 백그라운드)**
- [ ] TC112 레이스 도중 `⏭ 건너뛰기` 를 누른다 → 그 자리에서 결승선 통과 상태가 한 번 그려지고 **즉시** 결과 발표로 넘어간다. 남은 시간을 기다리게 하거나 오버레이가 닫혀버리면 실패 (R19-7)
- [ ] TC113 `설정 > 손쉬운 사용 > 동작 줄이기`(iOS) / `애니메이션 제거`(Android)를 켜고 구슬 레이스를 실행한다 → 구슬이 내려가는 연출 없이 결과가 바로 뜬다. 20초를 기다리면 실패 (R19-8)
- [ ] TC114 레이스 도중 ✕ 로 나간다 → 오버레이가 닫히고 아래 화면이 멈춘 상태로 정상 동작한다. `뒤로가기`·`그만두기` 로도 같다. 세 경로 각각에서 나간 뒤 1분간 폰을 그대로 두고 본다 → 화면이 뜨겁거나 배터리가 계속 줄면 프레임이 남은 것이므로 실패 (R19-13)
- [ ] TC115 레이스 도중 홈 버튼으로 앱을 백그라운드로 보내 10초쯤 두었다가 돌아온다 → 구슬이 화면 절반을 순간이동하지 않고 **떠날 때 있던 자리에서 이어서** 내려간다. 숨어 있던 만큼 전체 시간이 늘어나는 것은 정상이다(벽시계보다 이어 보이는 쪽을 골랐다). 돌아온 순간 이미 결과가 떠 있거나 구슬 순서가 뒤바뀌어 있으면 실패 (R19-14)

**TC116~TC118 (한 판 더 · 정책 연동 · 결과 흐름)**
- [ ] TC116 결과 화면에서 `🎲 한 판 더` 를 누른다 → 새 레이스의 **벽 모양(사행 곡선과 페그 위치)이 직전 판과 다르다**. 두세 번 반복해 매번 다른지 본다. 같은 코스가 다시 나오면 실패 (R19-16)
- [ ] TC117 정책 카드에서 `제외` 를 켜고 구슬 레이스를 돌린다 → 구슬 수가 참가자 수와 같고, 직전에 섬긴 그분의 구슬이 **아예 없다**(범례에도 이름이 없다). 끝까지 달리다 마지막에 지는 게 아니라 처음부터 없어야 한다 (R19-15)
- [ ] TC118 레이스가 끝난 뒤 결과 화면을 본다 → 발표 형식과 `✅ 이분으로 확정하기`·`🎲 한 판 더`·`그만두기` 세 버튼이 사다리·돌림판·손가락 룰렛과 **같은 모양·같은 순서**로 있고, 확정까지 밟았을 때 호스트 교체 결과도 같다 (R19-16)

**TC119~TC121 (가독성 · iOS 렌더링 · 포커스 — 구현 중 드러난 것)**
- [ ] TC119 참가자 24명으로 레이스를 돌린다 → 달리는 중에 구슬의 두 자리 번호가 **읽히는지** 정직하게 본다. 구슬이 겹쳐 번호가 가려지는 순간이 있어도, 아래 범례(`번호 · 색 · 전체 이름`)와 HUD 상위 3명으로 **누가 누군지 알 수 있으면** 통과다. 번호가 원을 넘쳐 뭉개지거나 범례가 잘려 이름을 못 읽으면 실패 (R19-6)
- [ ] TC120 iOS 사파리 실기기에서 구슬 레이스를 실행한다 → 코스 벽 두 줄·페그(점)·결승선 체커 무늬가 **실제로 그려진다**. 구슬만 떠 있고 배경이 텅 비어 있으면 오프스크린 벽 캐시가 사파리의 캔버스 한 변 4096px 제한에 다시 걸린 것이므로 실패(캐시 dpr을 1.5로 묶어 3600px로 맞춰 둔 부분이다). 아이폰 여러 세대(dpr 2·3)에서 각각 본다
- [ ] TC121 레이스 오버레이가 열린 상태에서 Tab 을 계속 누른다 → 포커스가 ✕ → `⏭ 건너뛰기` → `그만두기` → 다시 ✕ 로 돌고 아래 화면으로 나가지 않는다. Shift+Tab 은 반대로 순환한다. 새로 생긴 `건너뛰기` 가 순환에서 빠지면 실패 (R13-2)

**자동 검증**: `node --test` 27개 — 앞 절의 20개에 구슬 레이스 순수 함수 7개가 더해졌다(마커 블록 심볼 존재 · `order[0]` 도착 시간 최소 · 도착 시간이 `duration*0.75`~`duration` 안 · `raceProgress` 단조 증가와 양끝 값 · `raceXY` 의 x 가 코스 안 · `raceLeadChanges` 문턱과 재생성 루프가 죽은 코드가 아님 · `gameFits("race")` 24/25 경계). 위 실기기 케이스는 이 27개가 못 보는 것 — 캔버스 그림·카메라·HUD·백그라운드 복귀·실제 소요 시간 — 만 남긴 목록이다.

---

## 헤더 연결 상태 표시 제거 회차 (v9.72 → 배포 시 범프) — ⏳ 실기기 확인 대기

앞 절들과 절을 나눈 이유: 게임과 코드가 겹치지 않는 헤더 한 줄(R20)만 다루고, 준비물도 다르다 — 공유 셀과 이 기기 전용 모드 **두 벌**, 그리고 폴링을 확인할 두 번째 기기(또는 창). 없앤 것이 아니라 **없어졌는지**를 보는 케이스라 대부분 "안 보이면 통과"다.

**준비**: ① 공유 셀(`?cell=demo` 등 정상 링크)에서 한 벌, ② 이 기기 전용 모드(공유 키 없이 로컬 사본으로 접속)에서 한 벌을 밟는다. 폴링 케이스(TC125)는 폰 2대(또는 창 2개)로 A·B를 나눈다. TC128 은 데스크톱 Chrome 개발자도구 콘솔을 띄운 채로 본다.

**TC122~TC125 (날짜 줄 — 무엇이 없고 무엇이 남는가)**
- [ ] TC122 공유 모드로 접속해 헤더의 날짜 줄을 본다 → `11월 5일 (수)` 처럼 **날짜만** 있다. `공유중`·연결 점·`마지막 갱신 14:32:07`·`N초 전` 중 하나라도 보이면 실패. 날짜 뒤에 `·` 나 공백 구분자가 매달려 있어도 실패다 (R20-1, R20-6)
- [ ] TC123 이 기기 전용 모드로 접속해 같은 자리를 본다 → `11월 5일 (수) · ●이 기기 전용` 로 경고가 그대로 있고, 앞의 점이 **주황색**으로 보인다. 경고가 사라졌거나 점이 안 보이면 실패 — 이 상태를 모르고 주문하면 아무도 그 주문을 못 본다 (R20-2)
- [ ] TC124 두 모드 헤더를 나란히 본다 → **초록 점이 어디에도 없다**. 공유 모드에 점이 하나라도 남아 있으면 `.dot.on` 규칙이 안 지워진 것이므로 실패 (R20-1, R20-5)
- [ ] TC125 공유 모드에서 A를 헤더가 보이는 채로 두고 **30초 이상** 아무것도 누르지 않는다 → 헤더에서 숫자나 글자가 바뀌는 곳이 한 군데도 없다(10초마다 갱신되던 표시가 사라졌다). 이어서 B에서 주문을 하나 넣거나 바꾼다 → A의 본문 현황에 그 변화가 **약 10초 안에** 나타난다. 헤더 글자가 여전히 바뀌면 타이머가 남은 것이고, 본문이 갱신되지 않으면 폴링까지 함께 지워진 것이므로 둘 다 실패 (R20-3, R20-7)

**TC126~TC128 (남겨야 하는 것 · 콘솔)**
- [ ] TC126 데모·공용 셀에서 헤더를 본다 → `데모·공용` 배지가 그대로 있다 (R20-8)
- [ ] TC127 섬김이 시작된 셀에서 헤더를 본다 → `N번째 섬김` 배지가 그대로 있고 번호도 맞다. 두 번째 섬김을 만들어 숫자가 오르는지도 본다 (R20-8)
- [ ] TC128 콘솔을 열어 둔 채 새로 접속한다 → 로드 직후와 **1분 방치 후** 모두 빨간 오류가 0건이다. `Cannot set properties of null` 같은 오류가 뜨면 지워진 `#syncAgo` 를 아직 참조하는 코드가 남은 것이므로 실패 (R20-3, R20-4)

**남긴 판단**: 정상 상태는 조용히, 문제 상태만 알린다. `공유중`은 잘 되고 있다는 말을 매초 반복하며 폰에서 한 줄을 다 차지했고, `이 기기 전용`은 모르면 손해를 보는 경고다. 그래서 앞의 것만 지웠다. 설정 시트의 `⚠️ 이 기기 전용 모드 (공유 안 됨)` 안내는 그대로 남아 있어 두 곳에서 같은 사실을 말한다.

---

## 이번 회차에 발견·수정한 버그
| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| B1 | 전체 메뉴판 스크롤 시 헤더 위로 메뉴가 살짝 비침 | `.sheet padding-top:8px` 때문에 sticky 헤더가 8px 아래에 붙어 그 틈으로 목록이 보임 | `.sheet` 상단 패딩 제거(그립 마진이 여백 유지) + 헤더 좌우 bleed·z-index↑ → 빈틈 0 |
| B2 | 스크롤된 시트를 닫고 다른 시트를 열면 새 시트가 스크롤된 채로 뜸 | `openSheet`가 `scrollTop`을 리셋 안 함 | `openSheet`에 `scrollTop=0` 추가 |
| B3(경미) | `member_diff` 함수가 anon에서도 실행됨 | Supabase 기본권한이 함수 생성 시 anon에 EXECUTE 부여 → `revoke from public`만으론 부족 | `revoke ... from public, anon, authenticated` (순수 함수라 무해하나 최소권한 정리) |

## 기능 TC (로컬 자동검증 — 전부 PASS)
**입장/이름**
- TC1 이름 미선택 → 본인선택 화면 / 선택 → 메인
- TC14 본인 이름 변경 → 숨은 id 유지·표시 갱신·주문 보존
- TC15 본인이 명단에서 삭제됨 → 본인선택 화면으로 안전 복귀(크래시 없음)

**섬김(호스트)/세션**
- TC2 호스트 자처 → open, host 지정
- TC8 마감 → closed / 마감취소 → open
- TC16 호스트가 명단에서 빠지면 → 세션 자동 해제(idle, 잠김 방지)

**주문**
- TC3 음료 주문 저장·표시
- TC7 주문 취소
- TC9 기존 주문 수정 시 옵션 상태 복원 + 배반(샷추가↔연하게)
- TC10 직접입력(커스텀) 주문 — 디카페인 옵션 없음(알려진 한계)
- TC11 디카페인은 커피계열만(아메리카노 O, 제주말차라떼 X)
- TC12 HOT 전용 메뉴(에스프레소) 온도 자동·고정
- TC17 "나도" — 남의 주문(옵션 포함) 복사

**현황/주문서**
- TC4 합계(잔 수) 정확
- TC5 카페 주문서 텍스트(음료·온도·옵션·합계) 생성
- TC6 분류(주문/안마심/대기) 정확
- TC18 주문 0건 마감 화면 크래시 없음
- TC22 주문서에 커스텀 메뉴 + 디카페인 포함, 크래시 없음
- TC23 커스텀 메뉴 이모지 폴백(🥤)

**전체 메뉴판**
- TC13 검색 필터 / 무결과 안내 / 검색어 지우면 복원
- B1 스크롤 시 헤더 위 비침 없음(수정 확인)

**다이얼로그/안내**
- TC20 커스텀 확인창 표시 / 뒤로가기=취소 / 확인=true
- TC21 호스트가 명단에서 빠져도 "안내 문구" 생성 크래시 없음

**기타**
- TC19 셀 전환 시 forceGrid 리셋
- 7글자 이름: 헤더/타이틀/목록/셀명 깨짐 없음, 가로 스크롤 0 (이전 회차)
- 이름 선택 화면 12초 폴링에 안 닫힘 (이전 회차)

## 백엔드 TC (라이브, 읽기 전용 — 전부 PASS)
- anon 직접 조회 차단: `orders/sessions/cells/member_logs` → 401/permission denied
- RPC 정상: `get_cell(demo)` 200, `get_room_day(demo)` 200
- 감사로그 적용 확인: `set_members` 3-인자 존재, `member_diff` 서버 diff 정상
- 회원가입 비활성: `/auth/v1/signup` 422

## 알려진 한계(의도 — 신뢰그룹 전제)
- 자기신고 actor(이름 도용 시 위조 가능), 로그 자동삭제 미도입
- 직접입력 메뉴는 디카페인 옵션 없음
- 세션은 당일(KST) 기준 — 자정 전 마감 권장
- 링크=권한(링크 아는 사람은 편집 가능, 단 수정내역은 로그됨)

---

## TC129~TC152 — 기간 프리셋 정리와 게임 마무리 다듬기 (v9.81)

**TC129~TC134 (대시보드 기간 프리셋 · R1~R3)**
- [ ] TC129 오늘이 **월요일**인 날 대시보드에서 `이번주` → 시작일과 종료일이 **같은 날**(오늘)이다. 일요일로 시작하면 실패 (R1-1, R1-3)
- [ ] TC130 오늘이 **일요일**인 날 `이번주` → 범위가 **직전 월요일 ~ 오늘(7일)**이다. 기기 날짜를 바꿔 확인해도 된다 (R1-4)
- [ ] TC131 주중 아무 날 `이번주` → 시작일이 그 주 월요일, 종료일이 오늘이다. 종료일이 이번 주 일요일(미래)로 잡히면 실패 (R1-2)
- [ ] TC132 `3개월` → 시작일이 **(이번 달 − 2)월 1일**, 종료일이 오늘이다. 1월이면 전년 11월 1일, 2월이면 전년 12월 1일 (R2-1 ~ R2-5)
- [ ] TC133 **320px** 폭(iPhone SE / 개발자도구 320×568)에서 대시보드를 연다 → 프리셋 6개(`이번주 지난달 이번달 3개월 올해 작년`)가 **가로 스크롤 없이 한 줄에 전부** 보인다. 밀어야 `작년`이 나오면 실패. 셀 대시보드와 관리자 대시보드 **둘 다** 확인한다 (R3-1, R3-11)
- [ ] TC134 브라우저 글자 크기를 **120%**로 키운 채 320px에서 본다 → 칩이 잘리거나 겹치지 않고, 한 줄에 안 들어가면 가로로 밀 수 있으며 **선택된 칩이 화면 안에 보이게** 자동으로 당겨진다. `3개월` 칩을 길게 눌러(또는 데스크톱에서 호버) `이번 달 포함 최근 3개월` 설명이 뜬다 (R3-4, R3-8, R3-9)

**TC135~TC139 (손가락 룰렛 인원 불일치 · 가운데 가독성 · R4~R5)**
- [ ] TC135 참가자 **5명**인 상태에서 손가락 룰렛을 열고 손가락 **3개**만 올린다 → 3·2·1이 **시작되지 않고** `손가락 3개 · 참가자 5명` / `아직 안 올린 분이 있어요?` / `[이대로 시작]`이 뜬다. 그냥 세기 시작하면 실패 (R4-1 ~ R4-4)
- [ ] TC136 같은 상태에서 손가락 **6개**를 올린다 → `손가락 6개 · 참가자 5명` / `한 분이 두 개 올렸거나, 참가자가 아닌 분이 올렸을 수 있어요`가 뜬다. TC135와 문구가 같으면 실패 (R4-5)
- [ ] TC137 `[이대로 시작]`을 누른다 → 그대로 진행되어 안정화 뒤 3·2·1이 돈다. 누른 자리에 **손가락 점이 새로 생기지 않는다**(점 개수가 그대로) — 생기면 실패 (R4-6, R5-4)
- [ ] TC138 `[이대로 시작]`으로 승인한 뒤 손가락을 **하나 더 올리거나 하나 뗀다** → 승인이 무효가 되어 불일치 질문이 **다시** 뜬다 (R4-9)
- [ ] TC139 참가자 수와 손가락 수를 맞춘 뒤 폰 **가운데에 손가락을 몰아** 겹치게 둔다 → 안내 문구와 3·2·1 숫자가 색 원 위에 묻히지 않고 읽힌다. 판이 화면 가운데를 넓게 덮어 어떤 손가락이 어디 있는지 모르게 되면 실패. 당첨이 정해지면 판이 **사라져** 당첨 점을 가리지 않는다 (R5-1, R5-2, R5-7, R5-9)

**TC140~TC143 (돌림판 정보 누출 · R6)**
- [ ] TC140 **참가자 2명**으로 돌림판을 연다 → 판이 돌기 **전** 정지 상태에서 두 이름이 모두 **똑바로**(기울지 않고) 서 있다. 한쪽으로 기울어 있으면 그 각도가 곧 당첨자라 실패다. 여러 판 반복해도 시작 모습이 매번 같아야 한다 (R6-1, R6-2)
- [ ] TC141 판이 멈춘 뒤 모든 이름이 똑바로 읽히고, 12시 핀(🔻) 아래 조각이 결과 발표와 **같은 사람**이다 (R6-3, R6-6)
- [ ] TC142 참가자 **11명**으로 돌림판을 연다 → 라벨이 번호로 바뀌어도 정지 상태에서 번호가 기울어 있지 않다. 아래 범례가 번호·색·이름을 잇는다 (R6-4)
- [ ] TC143 `설정 > 손쉬운 사용 > 동작 줄이기`를 켜고 돌림판을 연다 → 회전 없이 바로 결과로 가되, 그 짧은 순간에도 이름이 기울어 보이지 않는다 (R6-5)
- [ ] TC143b **구형 브라우저 폴백** — 개발자도구 콘솔에서 `CSS.supports = () => false` 로 덮어쓴 뒤 돌림판을 연다(`transform-box: fill-box` 미지원 흉내) → ① 정지 상태에서 이름이 여전히 **똑바로** 서 있다(기울어 있으면 폴백이 누출을 막지 못한 것) ② 라벨이 판 밖으로 날아가지 않는다 ③ 멈춘 뒤 이름이 똑바로 읽힌다 ④ 회전 도중 라벨 각도가 한 번 바뀌지만 눈에 띄지 않는다 (R6-1, R6-3)

**TC144~TC149 (열린 섬김 화면 통합 · R12)**
- [ ] TC144 열린 섬김에서 탭이 `[주문 · 현황]`과 `[카페 주문서]` **둘뿐**이다. 330px 폭에서 두 라벨이 **두 줄로 접히지 않는다**(접히면 탭 바 높이가 두 배가 되어 실패) (R12-1, R12-19)
- [ ] TC145 나 포함 여러 명이 주문한 상태에서 합친 화면을 끝까지 내린다 → **같은 사람이 두 번 나오지 않는다.** 내 주문은 위쪽 카드에 한 번, `다른 분들` 로스터에는 **내가 없다** (R12-5, R12-6, R12-7)
- [ ] TC146 `다른 분들` 로스터에서 이미 주문한 사람 행의 오른쪽이 `주문` 알약이 아니라 `[나도]` 버튼이다. 눌러 보면 그 사람과 같은 메뉴로 내 주문이 바뀐다. `안마심`·`미응답` 알약은 그대로 남아 있다 (R12-9)
- [ ] TC147 미응답자가 있는 상태에서 합친 화면 맨 아래 `📢 미응답 n명 안내 문구 복사` 버튼이 보이고, 눌렀을 때 실제로 복사된다 (R12-13)
- [ ] TC148 **주문을 마감한 뒤**(닫힌 화면) 사람 목록을 본다 → 예전처럼 **나를 포함한 전원**이 나오고 `주문` 알약이 그대로이며 `[나도]` 버튼은 **없다**. 닫힌 화면이 달라졌으면 실패 (R12-12)
- [ ] TC149 **360px 실기기**에서 합친 화면 전체 높이를 잰다(개발자도구 또는 스크롤 끝까지 세어보기) → 기록해 둔다. 목표는 ≈1,080~1,120px이며, 크게 벗어나면 접기 도입을 다시 검토한다. **이 값은 아직 실측된 적이 없다** (R12-21)

**TC150~TC152 (타이포그래피 · R13)**
- [ ] TC150 눈에 보이는 여섯 가지 크기 변화를 확인한다 — ① 대시보드 경고 줄과 보조문구 4곳이 12px로 통일 ② 시트 상단 개수 표시 12px ③ 셀 카드 이름 13px ④ 시트 제목 18px(좁은 화면 17px) ⑤ 시트 닫기 ✕ 18px ⑥ 어디서도 글자가 겹치거나 잘리지 않는다 (R13-6, R13-10)
- [ ] TC151 대시보드 KPI 큰 숫자(`.stat .num`)와 게임 결과 당첨자 이름(`.game-win .wn`)의 위아래 여백이 예전보다 **줄었다**. 글자가 잘리면 실패 (R13-17)
- [ ] TC152 관리자 화면의 **분류 드롭다운(`<select>`)** 글꼴이 옆의 입력창·버튼과 같다. 혼자 다른 서체(UA 기본)로 보이면 실패 (R13-2)

**미측정으로 남긴 것 (R13-4, R13-5 · 다음 스펙의 입력)**
- [ ] TC153 참가자 **24명**으로 사다리타기나 돌림판을 돌린다 → 아래 번호·이름 범례가 **몇 줄**이 되는지 세어 기록한다. 결과 블록이 화면 밖으로 밀리지 않으면 11px을 12px로 올릴 여지가 있다
- [ ] TC154 참가자 **10명**으로 사다리타기를 돌린다 → 세로줄 위 두 글자 이름이 읽히는지 기록한다(계산상 CSS 7.1px). 안 읽히면 크기가 아니라 `GAME_LABEL_MAX`를 9로 내려 번호+범례 경로로 보낸다
- [ ] TC155 참가자 **12명 이상**으로 구슬 레이스를 돌린다 → 10번 이후 구슬의 흰 숫자가 읽히는지 기록한다(계산상 CSS 7.5px). 안 읽히면 `RACE_R`을 키우고 `raceXY`의 벽 여유를 다시 확인한다
- [ ] TC156 합친 화면에 구분선이 둘 있다 — `☕ 내 주문`과 `👀 N번째 섬김 · 현황`. 320px에서 제목 양옆의 선이 사라지지 않고, 제목이 두 줄로 접히지 않는다 (R12-1)
- [ ] TC157 손가락 룰렛을 연다 → 위쪽 안내(`폰을 가운데 놓고…`)와 가운데 판(`손가락 n개 …`)이 **겹치지 않는다**. 화면 높이가 짧은 기기(가로 모드 포함)에서 특히 확인한다. 당첨자를 지목한 뒤 결과 블록은 다시 화면 가운데로 온다
- [ ] TC158 합친 화면에서 **탭 줄의 세로 위치가 상황에 상관없이 같다** — ① 호스트 없음 ② 호스트 있음 ③ 내가 주문함 ④ 내가 `안 마실게요` 네 경우를 오가며 확인한다. 탭이 위아래로 움직이면 실패. 머리말 카드는 항상 104px이고 안내 한 줄이 늘 있다
- [ ] TC159 이름이 긴 사람(6자 이상)이 호스트일 때 머리말 제목이 **한 줄로 유지**되고 이름만 말줄임된다. `님이 섬깁니다!` 부분이 잘리면 실패. 320px에서 확인
- [ ] TC160 `🙅 안 마실게요, 감사합니다` 버튼이 위쪽 메뉴 타일들과 **색으로 구분**된다(주황 테두리 + 연한 주황 배경). 주문 후 버튼줄의 `🙅 안 마실게요`도 같은 색이다
- [ ] TC161 `다른 분들` 로스터에서 `[나도]` 버튼이 `안마심`·`미응답` 알약과 **같은 오른쪽 끝 선**에 정렬된다. 이름·메뉴 길이가 다른 행을 여럿 놓고 버튼 왼쪽 모서리가 들쭉날쭉하면 실패
- [ ] TC162 구분선(`☕ 내 주문` / `👀 N번째 섬김 · 현황`)이 위아래 내용과 확실히 구분된다 — 선 2px, 제목 15px 갈색 굵은 글씨

**TC163~TC167 (사용자 요청 후속 · v9.82)**
- [ ] TC163 사다리타기 — 각 선의 **최전선에 그 사람의 색 동그라미**가 붙어 함께 내려온다. 10명 이하면 동그라미 안에 이름 두 글자(11명 이상은 번호)가 보이고, 16명이면 글자 없이 색 동그라미만 나온다(글자를 넣으면 원도 글자도 안 읽힌다)
- [ ] TC164 사다리타기 — 선마다 내려오는 **속도가 미묘하게 다르다**(0.9~1.3배). 여섯 줄이 한 덩어리로 움직이면 실패. 가장 느린 선이 도착한 뒤에 결과가 발표된다
- [ ] TC165 게임 진입점 세 자리(합친 화면 두 갈래 · 하단바) 어디에도 `[test]` 표시가 **없다**
- [ ] TC166 호스트가 `섬김 취소`를 누르면 확인창에 세 줄이 나온다 — 취소 여부 질문 · 주문이 있으면 섬김이 남고 이어받으면 메뉴가 그대로 돌아온다는 설명 · 주문이 없으면 사라진다는 설명
- [ ] TC167 `안 마실게요` 후 내 상태 카드가 `감사하지만, 안 마실게요 🙅` 로 보인다

> **R11-2 해제**: 이전 스펙은 실기기 확인 전까지 `[test]` 표시를 유지하기로 했으나, 사용자가 제거를 지시해 TC165 로 대체한다. 실기기 확인 자체는 여전히 남아 있다(TC149·TC153~TC155).

**TC168~TC173 (확률 표시·슬라이더·게임 분류 · v9.83)**
- [ ] TC168 게임 고르기 시트의 참가자 카드에 **사람별 당첨 확률 %**가 칩으로 보인다. 합이 100%에 가깝고, `[확률⬇️]`가 걸린 분은 주황 테두리로 갈라져 있다
- [ ] TC169 `[확률⬇️]`를 고르면 **강도 슬라이더**가 나온다. 끄는 동안 칩 아래 결과 줄의 %와 참가자 카드의 사람별 % 가 **동시에** 따라 움직인다(손을 뗀 뒤에 바뀌면 실패)
- [ ] TC170 슬라이더를 좌우 끝까지 민다 → 10%~90% 안에서만 움직이고, `[평등]`(100%)이나 `[제외]`(0%)와 값이 겹치지 않는다. `[평등]`·`[제외]`를 고르면 슬라이더가 사라진다
- [ ] TC171 게임 목록이 두 무리로 갈라져 있다 — `🎯 확률 조정 가능`(사다리타기·돌림판) / `🎲 완전 랜덤`(손가락 룰렛·구슬 레이스). 두 번째 무리 위에 "위에 적힌 확률이 적용되지 않아요" 안내가 있다
- [ ] TC172 `[제외]`를 고른 상태에서 **네 게임 모두** 그분이 참가자 목록·게임 화면·결과 어디에도 나오지 않는다(제외는 모든 게임에 적용된다)
- [ ] TC173 게임 진입점 라벨이 `🎲 어떤 분이 섬겨주실지 게임으로 정하기` 다

**TC174~TC181 (구슬 레이스 물리 엔진 · v9.84)**
- [ ] TC174 구슬 레이스를 돌린다 → 구슬들이 **서로 부딪혀 밀린다**. 겹친 채 통과하면 실패. 좁은 문에서 줄을 서고, 뚫고 나온 순서가 그때그때 다르다
- [ ] TC175 코스에 **네 종류 장애물**이 보인다 — 못밭 · 바람개비(도는 십자, 갈색) · 가운데 기둥 · 범퍼(주황 큰 원). 바람개비는 실제로 돌고, 맞은 구슬이 튕겨 나간다
- [ ] TC176 길이 세 가지로 이어진다 — 1자길 · 지그재그길 · 깔때기길. 매 판 **순서와 장애물이 달라진다**(연달아 두 판 돌려 비교)
- [ ] TC177 레이스가 **14~21초** 안에 끝난다. 30초를 넘거나 5초에 끝나면 실패
- [ ] TC178 구슬이 벽이나 장애물을 **뚫고 지나가지 않는다**. 코스 밖으로 새는 구슬이 하나도 없다
- [ ] TC179 구슬 번호가 **읽힌다**(320px 폰에서). 예전 7.5px 문제가 해소됐는지 — 10번 이후 두 자리 번호까지 확인
- [ ] TC180 시작 화면에 `물리로 굴러가요 — 확률 조정이 적용되지 않는 게임이에요` 안내가 있다. `[확률⬇️]`를 걸어도 레이스 결과 분포는 균등하다
- [ ] TC181 `[제외]`를 건 분은 구슬 레이스에도 **구슬이 없다**(참가자 수와 구슬 수가 같다). `⏭ 건너뛰기`·`그만두기`·뒤로가기로 중단하면 프레임이 남지 않는다(콘솔에 오류 0)

**TC182~TC187 (게임 시트 UI 재설계 · 바람개비 · v9.85)**
- [ ] TC182 참가자 고르기 시트에서 목록을 **끝까지 내려도** 아래 `참가 N명 / M명 빠짐 / [완료]` 줄이 화면에 **그대로 붙어 있다**. 맨 아래 사람을 토글하면 그 숫자가 **눈앞에서** 바뀐다(숫자를 보려고 위로 올려야 하면 실패)
- [ ] TC183 일괄 버튼이 셋이다 — `메뉴 고른 분만`(기본값 복귀) · `전원` · `오늘 섬긴 분 빼기`. 명단을 헝클어뜨린 뒤 `메뉴 고른 분만`을 누르면 처음 상태로 돌아온다
- [ ] TC184 `[확률⬇️]`를 고르면 슬라이더가 **왼쪽 세로선으로 그 칩에 묶여** 보인다. 칩 셋과 같은 층위로 나란히 있으면 실패
- [ ] TC185 슬라이더를 끄는 동안 **바로 아래에** `홍길동 7.7%` / `다른 분 23.1%` 두 숫자가 따라 움직인다. 참가자 카드까지 스크롤해야 결과를 알 수 있으면 실패
- [ ] TC186 구슬 레이스의 **바람개비가 눈에 띄게 돈다**. 날개 길이가 서로 달라(긴 것/짧은 것) 어느 순간에도 방향이 읽히고, 가운데 회전축 점이 보인다. 멈춰 보이면 실패
- [ ] TC187 게임 고르기에서 구슬 레이스 부제가 `시스템도 모르는 100% 랜덤이에요` 다

**TC188~TC194 (UI 후속 · v9.86)**
- [ ] TC188 전체 메뉴판 시트 아래 버튼이 `[✍️ 직접 입력][닫기]` **좌우 한 줄**이다. 목록이 세로로 한 줄만큼 더 보인다. 320px에서 두 버튼 글자가 접히지 않는다
- [ ] TC189 `[🙅 안 마실게요, 감사합니다]` 버튼이 **흰 배경 + 주황 테두리**다(연한 주황으로 채워져 있으면 실패 — 그건 '이미 고른 메뉴 타일'의 신호다)
- [ ] TC190 그 버튼을 누르면 상태 카드가 **꽉 찬 주황 배경 + 흰 글자 + ✓** 로 바뀐다. 누르기 전과 후의 모양이 확실히 다르고, 아래 메뉴 그리드로 언제든 되돌릴 수 있다
- [ ] TC191 `[주문 · 현황]` ↔ `[카페 주문서]` 를 오갈 때 **탭 줄이 위아래로 움직이지 않는다**. 두 탭 모두 같은 104px 머리말 카드를 쓴다
- [ ] TC192 참가자 고르기 일괄 버튼이 넷이다 — `전원 참가` · `음료 고른 분` · `직전 섬긴 분 빼기` · `전부 해제`. `전부 해제` 뒤 푸터가 `참가 0명`을 말하고, 그 상태로 게임을 시작하면 2명 이상 안내가 뜬다
- [ ] TC193 손가락 룰렛에서 **다른 손가락을 화면에 올려둔 채** `[이대로 시작]`을 누른다 → 한 번에 눌린다. 여러 번 눌러야 하거나 안 눌리면 실패. 그 터치로 손가락 점이 새로 생기지도 않는다
- [ ] TC194 구슬 레이스 — 고르기 화면 부제는 `시스템도 모르는 100% 랜덤이에요`, 진행 화면 안내는 `부딪히고 튕기며 내려가요 — 결과는 아무도 몰라요` 로 서로 다르다

**TC195~TC200 (지난번 주문 다시 담기 · v9.87)**
- [ ] TC195 옵션과 메모까지 넣어 주문한다(예: 콜드브루 라떼 · ICE · 샷 추가 · 📝 덜 달게). **다음 섬김**에서 메뉴 그리드 **위**에 `지난번엔 이렇게 드셨어요` 줄이 뜨고, 메뉴·온도·옵션·메모가 그대로 적혀 있다
- [ ] TC196 `[이걸로]`를 누르면 **주문 시트가 그 값으로 채워진 채** 열린다. 그대로 `[이걸로 주문]`을 누르면 지난번과 같은 주문이 담긴다. 옵션 하나를 빼고 담을 수도 있다
- [ ] TC197 지난번 주문이 **지금 메뉴판에 없는 메뉴**면 그 줄이 아예 안 뜬다(비활성 버튼이나 빈 카드가 남으면 실패)
- [ ] TC198 지난번 옵션 중 **지금은 없는 옵션**이 있으면 시트가 그것만 빼고 열린다. 온도도 그 메뉴가 지원하는 값으로 맞춰진다
- [ ] TC199 주문을 담은 직후 `[🔄 다른 메뉴로]`를 누른다 → `지난번엔…` 줄이 **안 뜬다**(방금 담은 것과 같아서). 다른 메뉴로 바꾼 뒤 다시 들어가면 뜬다
- [ ] TC200 한 폰을 여러 사람이 돌려 쓴다 — A로 주문하고 이름을 B로 바꾼다 → B에게 A의 지난 주문이 보이지 **않는다**

**TC201~TC208 (컨펌 · 게임 연습 · 선택색 · 복사 · v9.88)**
- [ ] TC201 `[🙅 안 마실게요, 감사합니다]`를 누르면 확인창이 먼저 뜬다. `취소`하면 아무 일도 안 일어나고, `네, 안 마실게요`를 눌러야 상태가 바뀐다
- [ ] TC202 메뉴를 고르면 그 타일이 **진한 갈색으로 꽉 차고 글자가 희게** 바뀐다. 타일 크기·배치·체크 위치는 그대로다(선택 때문에 그리드가 출렁이면 실패)
- [ ] TC203 **호스트가 아닌 일반 참가자**에게도 게임 버튼이 보인다 — `게임이 궁금하면 (연습)` / `🎲 게임 연습으로 돌려보기`
- [ ] TC204 그 상태로 게임을 끝까지 돌리면 결과와 이름은 그대로 나오고, 푸터에 `연습으로 돌려본 판이에요 — 확정은 섬기기로 하신 분이 합니다` 안내만 있고 **`[확정하기]`가 없다**. `[한 판 더]`는 된다
- [ ] TC205 호스트 본인은 예전처럼 `[✅ 이분으로 확정하기]`가 보이고 확정된다
- [ ] TC206 A가 결과 화면을 열어 둔 채 B가 `[제가 섬길게요]`로 섬김을 가져간다 → A가 `[확정하기]`를 눌러도 `그 사이 다른 분이 섬기기로 하셨어요` 토스트만 뜨고 호스트가 안 바뀐다
- [ ] TC207 `📢 미응답 n명 안내 문구 복사`를 누르면 복사된 문구 **마지막 줄에 셀 링크**가 있다. 그 링크를 열면 해당 셀로 바로 들어간다
- [ ] TC208 안드로이드에서 복사할 때 시스템 알림과 앱 토스트가 **겹치지 않는다** — 앱은 토스트 대신 **누른 버튼 글자가 `✅ 복사했어요`로 잠깐 바뀐다**. `📋 주문서 복사`도 같다

**TC209~TC213 (역방향 장애물 · 출발 게이트 · v9.89)**
- [ ] TC209 구슬 레이스를 열면 구슬들이 **출발선에 멈춰 있다**. 안내는 `준비됐어요 — 내 번호를 찾아두세요`, 푸터는 `[▶ 출발!][그만두기]` 뿐이다(아직 `건너뛰기`가 없어야 한다). 이 상태에서 자기 번호를 찾을 수 있다
- [ ] TC210 `[▶ 출발!]`을 누르면 그때 굴러가기 시작하고 푸터가 `[⏭ 건너뛰기][그만두기]`로 바뀐다. 멈춰 있던 시간만큼 구슬이 순간이동하지 **않는다**
- [ ] TC211 `[한 판 더]`도 마찬가지로 멈춘 상태에서 시작한다
- [ ] TC212 (v9.90 에서 삭제 — 힘장 구간을 걷어냈다. TC215·TC216 이 대신한다)
- [ ] TC213 (v9.90 에서 삭제 — 위와 같음)

**TC214~TC220 (사다리 가로선 보장 · 큰 날 · 탄성 못밭 · 결승 구간 · v9.90)**
- [ ] TC214 사다리를 **2명 · 3명 · 16명**으로 각각 여러 판 돌린다 → **가로선이 하나도 안 닿는 세로줄이 없다**(위에서 아래까지 곧게만 내려오는 줄이 하나라도 있으면 실패). 같은 높이에서 이웃한 가로선이 붙어 있지도 않다
- [ ] TC215 구슬 레이스에 **코스 폭을 거의 채우는 큰 날**이 돈다. 긴 팔과 짧은 팔의 **색이 다르고**(진한 쪽이 긴 팔) 가운데 축에 점이 찍혀 있어, 지나가는 1~2초 사이에도 **돌고 있는 게 보인다**. 긴 팔이 향한 쪽은 구슬이 못 빠져나가고 반대쪽으로만 흐른다
- [ ] TC216 **진한 주홍색 큰 못이 엇갈려 깔린 구간**에서 구슬이 **눈에 띄게 위로 되돌아 튄다**(한 줄 높이쯤 거슬러 오른다). 주황색 범퍼 구간과 색·크기·개수가 확실히 구분된다
- [ ] TC217 여러 판 돌려도 **어디에서도 구슬이 끼어 멈추지 않는다** — 특히 큰 날과 벽 사이, 큰 못과 벽 사이. 한 판이 20초를 크게 넘기지 않는다
- [ ] TC218 **2명**으로 돌려도 정상으로 끝나고, 두 구슬이 **번갈아 앞서는 장면**이 나온다(한 구슬이 처음부터 끝까지 앞서기만 하면 실패)
- [ ] TC219 사다리·레이스 모두 **결과와 확률은 그대로다** — 사다리는 ☕ 가 당첨자 도착 칸에 있고, 레이스는 [확정하기]로 그 사람이 호스트가 된다. `prefers-reduced-motion` 을 켜면 두 게임 다 연출을 건너뛰고 결과로 바로 간다
- [ ] TC220 **결승 직전이 좁은 문(깔때기)이 아니다** — 마지막 구간에도 장애물이 있고, 구슬들이 한 줄로 서서 들어오는 게 아니라 **뭉쳐서 자리다툼하며** 결승선을 넘는다. 여러 판 돌려 **마지막에 순위가 뒤집히는 장면**이 나오는지 본다(3/4 지점 선두가 그대로 이기기만 하면 실패)

**TC221~TC223 (바람개비 밭 · 결승 관문 · v9.90)**
- [ ] TC221 **작은 바람개비가 여럿 깔린 구간**이 있다 — 여덟 개가 3-2-3 으로 엇갈려 있고 **저마다 다른 방향·속도로 돈다**(전부 같은 각도로 나란히 돌면 실패). 구슬이 그냥 통과하지 못하고 날개가 비켜 줄 때 빠져나간다
- [ ] TC222 **결승 깔때기가 좁고 긴 통로로 이어진다** — 통로 안에서는 두 구슬이 나란히 못 가고 추월도 안 된다(들어간 순서 그대로 결승선). 그 입구를 가로지르는 **큰 막대**가 도는데, **회전축이 코스 밖**이라 팔이 완만한 호를 그리며 오래 걸쳐 있다. 수평에 가까울 때는 **양옆이 다 막혀 한 마리도 못 들어간다**
- [ ] TC223 그 문이 열리는 순간 **순위가 다시 정해진다**. 여러 판 돌려 **문 앞에서 1등이 바뀌는 장면**이 나오는지 본다. 어떤 구슬도 막대와 깔때기 벽 사이에 끼어 영영 못 들어가지 않고, 통로 안에서 구슬이 서로를 뚫고 지나가지 않는다. 한 판이 23초를 넘지 않는다

**TC224~TC226 (확률 정책 카드 정리 · v9.90)**
- [ ] TC224 정책 카드가 **네 층**이다 — 이름 줄 / 칩 세 개(`확률⬇️` `제외` `평등`) / **결과 한 줄** / 슬라이더. 예전의 설명 문장·슬라이더 라벨·결과 칩 두 개는 없다. 같은 숫자가 두 번 나오는 자리가 없어야 한다
- [ ] TC225 칩을 `확률⬇️`→`제외`→`평등`으로 옮기면 결과 줄이 **같은 자리에서** 바뀐다 — `한사랑R 10.0% · 다른 분 30.0%` / `이번 판에서 빠져요 · 참가자 3명` / `모두 25.0%로 똑같이`. 줄이 위아래로 튀어 방금 누른 칩에서 눈이 떨어지면 실패
- [ ] TC226 슬라이더를 끄는 동안 **결과 줄의 %와 참가자 카드의 사람별 %가 동시에** 따라 움직인다. 이름이 아주 긴 분이어도 320px 폭에서 가로 스크롤이 생기지 않고, 줄이 바뀔 때 가운뎃점이 다음 줄 맨 앞에 홀로 떨어지지 않는다

**TC227~TC228 (기본 정책 · v9.90)**
- [ ] TC227 게임 고르기 시트를 처음 열면 정책 칩이 **`평등`**에 선택돼 있고, 결과 줄이 `모두 N%로 똑같이` 다. 슬라이더는 안 보인다(`확률⬇️`를 골라야 나온다)
- [ ] TC228 참가자 카드의 사람별 %가 **전원 같다**(직전에 섬긴 분도 포함). `확률⬇️`를 누르면 그때 그분만 낮아진다

**TC229~TC232 (카메라 줌 · 사다리 시작 게이트 · 돌림판 끊기 · v9.91)**
- [ ] TC229 구슬 레이스를 **16명 이상**으로 돌린다 → 무리가 퍼지면 **화면이 자동으로 넓어져** 더 많은 구슬이 한 화면에 들어온다. 그때 코스가 가운데 좁은 띠가 되고 **양옆 여백이 한 톤 어둡게** 칠해져 '길이 어디까지인지' 보인다. 구슬 번호는 여전히 읽힌다(두 자리도)
- [ ] TC230 같은 판에서 **선두 구슬이 화면 위쪽이 아니라 가운데보다 조금 아래(약 55%)**에 있다. 그래서 뒤따르는 무리가 더 보이고, 선두 앞쪽 코스도 예전만큼은 보인다. 결승선을 넘는 순간도 화면 안에 있다
- [ ] TC231 사다리타기를 열면 선이 **멈춰 있다**. 안내는 `준비됐어요 — 내 이름을 찾아두세요`, 푸터는 `[▶ 시작!][그만두기]` 다. 몇 초 두어도 저절로 안 내려간다. `[▶ 시작!]`을 눌러야 내려가기 시작하고 푸터가 `[그만두기]`만 남는다. `[한 판 더]`도 마찬가지로 멈춘 채 시작한다
- [ ] TC232 돌림판이 도는 중에 `[⏹ 그만 돌리기]`를 누른다 → 판이 **뒤로 튀지 않고** 이어서 약 1초 만에 멈추고 결과가 나온다. **누르는 시점을 바꿔가며 여러 번** 해본다 — 일찍 누르든 늦게 누르든 멈춘 자리의 조각이 결과 발표와 같고, `확률⬇️`를 걸었을 때 그 확률대로 나온다(누르는 타이밍이 당첨자를 정하지 않는다). `동작 줄이기`를 켜면 이 버튼이 아예 없다

**TC219~TC221 (결승 줌인 · v9.92)**
- [ ] TC219 레이스 중반까지는 예전처럼 **무리가 퍼지면 화면이 넓어진다**(줌아웃). 선두 혼자 크게 보이지 않는다
- [ ] TC220 선두가 **결승 깔때기에 들어서는 순간부터 화면이 조여든다**. 결승선에 닿을 때 최대 2.0배로, 구슬과 번호가 판에서 가장 크게 보인다
- [ ] TC221 넓히기와 조이기가 서로 다투지 않는다 — 깔때기에 들어선 뒤에는 뒤 무리가 아무리 퍼져도 다시 넓어지지 않는다. 전환이 튀지 않고 0.3초 안에 부드럽게 넘어간다

**TC222~TC227 (긴장 곡선 재설계 · v9.93)**
- [ ] TC222 코스 어디에도 **가만히 떨어지기만 하는 구간이 없다** — 어느 1자길이든 성긴 배경 못이 깔려 있어 구슬이 계속 흩어진다
- [ ] TC223 장애물이 **약한 것부터 센 것 순**으로 나온다. 초반엔 못밭·기둥, 후반엔 탄성 못밭·큰 날. 연달아 두 판 돌려 순서가 비슷한지 본다
- [ ] TC224 코스 중간 두 곳(대략 40%·70% 지점)에 **코스를 통째로 쓸고 지나가는 긴 관문**이 있다. 먼저 온 구슬이 잠깐 붙잡혔다가 뒤 무리와 함께 풀려난다
- [ ] TC225 결승 통로가 **넓어지고 짧아졌다** — 구슬 세 개가 나란히 서고, 양 벽에 짧은 날개가 사다리처럼 붙어 빠르게 돈다(좌우 반대 방향). 통로 안에서도 순위가 뒤집힌다
- [ ] TC226 통로 날개가 통로를 **막지는 않는다** — 양쪽에서 들어와도 가운데로 구슬이 지난다. 한 판이라도 통로에서 멈추면 실패
- [ ] TC227 전체 소요가 **15~30초** 안이다. 24명·2명 양쪽에서 확인

**TC228~TC232 (버그 셋 · v9.94)**
- [ ] TC228 돌림판을 열고 **아무것도 누르지 않고 10초 이상 둔다** → 계속 돈다. 저절로 멈추거나 결과가 뜨면 실패
- [ ] TC229 `[그만]`을 누르면 그때부터 0.9초에 걸쳐 부드럽게 멈추고 결과가 나온다. 판이 뒤로 튀지 않는다
- [ ] TC230 돌림판이 도는 동안 **화면이 좌우로 밀리거나 덜컹거리지 않는다**. 손가락을 화면에 대고 좌우로 밀어도 가로 스크롤이 없다. 핀(🔻)이 잘려 보이지 않는다
- [ ] TC231 구슬 레이스 `[▶ 출발!]` 전에 구슬들이 **가지런한 격자로 정렬**돼 있다(한 줄에 여덟 개). 흩어져 있으면 실패. 자기 번호를 바로 찾을 수 있다
- [ ] TC232 같은 사람이 늘 같은 자리에 서지 **않는다** — 연달아 두 판 돌려 1번 구슬의 위치가 바뀌는지 본다(자리가 고정이면 그 자리가 곧 답이 된다)
