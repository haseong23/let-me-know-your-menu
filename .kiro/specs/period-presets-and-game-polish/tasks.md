# Tasks — 기간 프리셋 정리와 게임 마무리 다듬기

범위는 `design.md`의 「이번 범위와 범위 밖」을 따른다 — **R1~R6 · R11 · R12 · R13(부분)**. R7~R10(구슬 레이스 연출)과 R13의 인라인 전수 이관은 다음 스펙이다.

순서 제약 두 개: **1번이 2·3번보다 먼저**(테스트 하네스), **4번이 9번보다 먼저**(R13-24).

---

- [x] **1. 테스트 하네스를 여러 마커 블록으로 확장한다**
  - `tests/game-logic.test.mjs`의 `extractBlock`이 모든 마커 블록을 파일 순서대로 이어 붙이게 고친다
  - 실패 모드 네 가지(시작 마커 없음 · 종료 마커 없음 · 평가 실패 · 심볼 누락)가 그대로 살아 있는지 확인한다
  - "시작 마커는 한 줄이어야 한다" 테스트를 모든 블록 검사로 넓힌다
  - 블록이 2개 이상임을 단언하는 테스트를 추가한다
  - _설계: A.0 · 요구사항: R11-6, R11-7_

- [x] **2. R1·R2 기간 프리셋의 의미를 바로잡는다**
  - [x] 2.1 `shiftDate` 위에 두 번째 마커 블록을 열고 `weekBackDays(ds)` · `q3Start(y,m)`를 넣는다
  - [x] 2.2 `presetRange`의 `week`·`q3` 갈래를 새 함수로 바꾸고 주석 두 개를 갱신한다(삭제가 아니라 갱신 — R1-7, R2-8)
  - [x] 2.3 `DASH_PRESETS`의 `최근3개월` 라벨을 `3개월`로 줄인다. 키 `q3`와 계산은 그대로
  - [x] 2.4 테스트 6종 추가 — 월요일 시작 · 7일 요일 전수 · 범위 1~7일 · 1월/2월/3월+ 경계 · 92일 이하
  - _설계: A.1 · 요구사항: R1-1~R1-10, R2-1~R2-10_

- [x] **3. R6 돌림판의 라벨 각도 정보 누출을 막는다**
  - [x] 3.1 게임 마커 블록에 `wheelSpin(n, wi, rnd)`를 추가한다(난수는 인자로만)
  - [x] 3.2 `.wlab` CSS를 `@supports (transform-box: fill-box)`로 감싸 추가한다
  - [x] 3.3 `playWheel`이 `wheelSpin`을 쓰게 하고, 라벨에 `class="wlab"` + 폴백 속성 `rotate(labelEnd)`를 준다
  - [x] 3.4 판과 같은 리플로우·같은 이징으로 라벨 인라인 `transform`을 0 → `labelEnd`로 보간한다. `CSS.supports` 게이트를 둔다
  - [x] 3.5 테스트 4종 추가 — `labelStart === 0`(n 6종 × 모든 wi) · `(spin+labelEnd) mod 360 === 0` · jitter 무관 · 승자 조각이 12시
  - _설계: C · 요구사항: R6-1~R6-11_

- [x] **4. R3 프리셋 칩 6개를 320px 한 줄에 넣는다** _(9번보다 먼저)_
  - [x] 4.1 `.dash-filter` gap 4→3, `.dash-filter .tab` padding 11→7 · font-size 13→12
  - [x] 4.2 `@media (max-width:360px)`에 gap 2 · padding 5 티어를 추가한다
  - [x] 4.3 `3개월` 칩에 `title`/`aria-label`("3개월 — 이번 달 포함 최근 3개월")을 단다
  - [x] 4.4 가로 스크롤 폴백 · `scrollPresetIntoView()` · `white-space:nowrap` · `role="group"`이 그대로인지 확인한다
  - [x] 4.5 폭 예산 정정(268px)과 실측 합계(257px)를 주석에 남긴다
  - _설계: A.2 · 요구사항: R3-1~R3-11, R13-3_

- [x] **5. R4 손가락 룰렛이 인원 불일치를 묻는다**
  - [x] 5.1 `okAt`(승인 당시 손가락 수)을 도입하고 `refresh()`의 우선순위 3단계를 구현한다
  - [x] 5.2 방향별 문구 두 가지와 두 수 표시, `[이대로 시작]` 버튼을 `.fp-mid` 안에 넣는다
  - [x] 5.3 재평가 지점 세 곳(refresh · countdown 진입 · 3·2·1 매 틱)을 배선하고 주석에 명시한다
  - [x] 5.4 `#fpMid`에 `role="status" aria-live="polite"`를 준다
  - _설계: B.1 · 요구사항: R4-1~R4-14_

- [x] **6. R5 가운데 멘트가 손가락에 묻히지 않게 한다**
  - [x] 6.1 `.fp-plate`(불투명 · inline-block) CSS를 추가한다
  - [x] 6.2 `.fp-mid`가 그리는 모든 내용(안내 · 3·2·1 · 불일치)을 판으로 감싼다
  - [x] 6.3 `[이대로 시작]`에 `pointer-events:auto`와 `pointerdown` 전파 차단을 건다
  - [x] 6.4 `.fp-pad`의 `inset:0`·테두리 없음과 `at()` 좌표 기준이 그대로인지 확인한다
  - _설계: B.2 · 요구사항: R5-1~R5-9_

- [x] **7. R12 열린 섬김 화면을 하나로 합친다**
  - [x] 7.1 탭을 2개로 줄인다 — `주문 · 현황`(`order`) · `카페 주문서`(`cafe`). `role="tab"`/`tablist`를 떼고 `aria-pressed` 토글 버튼으로, 이유를 주석에 남긴다
  - [x] 7.2 `renderOpen`의 두 갈래를 하나로 합친다. 순서: 배너 → 탭 → myStatus → changeLink → grid → game → stats → 다른 분들 → 로스터
  - [x] 7.3 `othersHtml`(1056~1064)을 삭제한다
  - [x] 7.4 `dashPeopleHtml`에 `SESSION.state==="open"` 게이트로 (a) 나 제외 (b) `주문` 알약 → `[나도]` 교체를 넣는다. 시그니처는 그대로
  - [x] 7.5 `다른 분들` 라벨을 로스터 카드 위에 단다(열린 화면에서만)
  - [x] 7.6 `renderHostView()`(1482)를 제거한다
  - [x] 7.7 1127~1129행 `#gamepick` 주석을 정리한다. 배선은 한 번만
  - [x] 7.8 히스토리 7개 지점이 하나도 안 바뀌었는지 대조한다(설계 F.3 표)
  - [x] 7.9 닫힌 화면(`renderClosed`)의 출력이 변경 전과 같은지 확인한다
  - _설계: F · 요구사항: R12-1~R12-22_

- [x] **8. R13 서체 결함을 고친다**
  - [x] 8.1 `RACE_FONT`에 `'Malgun Gothic'`을 넣고 2848행의 틀린 주석을 정정한다
  - [x] 8.2 100행 규칙에 `select`를 넣는다. 3958행의 `font-size:16px`은 유지
  - [x] 8.3 `.gl-it` 굵기 700→800. 크기 11px 유지 사유와 미측정 사실을 주석에 남긴다
  - [x] 8.4 11px 미만 렌더 3곳(사다리 n=10 · 레이스 구슬 · 돌림판)의 결합 관계와 미측정 사실을 주석에 남긴다
  - _설계: G.1 · 요구사항: R13-1~R13-5, R13-23_

- [x] **9. R13 고아값을 흡수하고 타입 스케일을 도입한다** _(4번 뒤)_
  - [x] 9.1 고아값 7곳 정리 — 11.5→12(5곳) · 12.5→12 · 13.5→13
  - [x] 9.2 `:root`에 7단 토큰 + `--fs-title` 440/330 램프를 넣고 16px 하한 주석을 옮긴다
  - [x] 9.3 `<style>` 블록에서 값이 일치하는 선언을 토큰으로 바꾼다
  - [x] 9.4 `.room-name` override 2곳(190 · 227)과 `.sheet h3` override(201)를 제거한다
  - [x] 9.5 `.sheet-x` 19→18px
  - [x] 9.6 `.stat .num` · `.game-win .wn`에 `line-height:1.1`
  - [x] 9.7 `.room-name` `-.017em` · `.game-title` `-.006em`
  - [x] 9.8 `.hbar-name` 600→700 · `.hbar-n` 800→700 · `td.mx-sum` 800→700
  - [x] 9.9 치환 후 `<style>`의 `font-size` 리터럴을 전부 뽑아 허용 목록과 대조한다
  - _설계: G.2, G.3, G.4 · 요구사항: R13-6, R13-7, R13-10~R13-12, R13-17~R13-23_

- [x] **10. R11 배포 위생을 마무리한다**
  - [x] 10.1 `APP_VERSION` v9.80 → v9.81
  - [x] 10.2 `node --test`로 기존 27개 + 신규가 모두 통과하는지 확인한다
  - [x] 10.3 `QA-TESTCASES.md`에 TC129부터 추가한다(설계 「QA 계획」의 8개 묶음)
  - [x] 10.4 재작성 대상 8개(TC59 · 61 · 81 · 82 · 84 · 87 · 88 · 102)를 갱신하고, TC60은 그대로 둔다
  - [x] 10.5 결정 N1/N2/N3에 이번 통합이 그 결정을 어떻게 지키는지 행을 추가한다
  - [x] 10.6 `[test]` 표시 3곳이 그대로 남아 있는지 확인한다
  - _설계: E · 요구사항: R11-1~R11-9, R12-22_

---

## 완료 기준

1. `node --test`가 저장소 루트에서 전부 통과한다(기존 27 + 신규).
2. `<style>` 블록에 `11.5px` · `12.5px` · `13.5px` 선언이 남아 있지 않다.
3. 히스토리 7개 지점(설계 F.3 표)의 diff가 0이다.
4. 열린 화면에서 같은 사람이 두 번 그려지지 않는다.
5. 돌림판 시작 상태의 라벨 기울기가 모든 `wi`에서 0이다.
6. `package.json` · 빌드 스크립트 · 새 소스 파일이 없다.
