# 적대적 사용성 검토 — 본질 대비 화면 구성 (v10.10)

**전제**: 이 앱의 본질은 **여러 사람에게서 카페 메뉴를 취합해, 카페에 낼 주문서를 만드는 것**이다. 아래는 그 하나만을 기준으로 놓고, 나머지가 그 길을 얼마나 막는지를 적대적으로 본 결과다.

---

## 결론 먼저

**주문서를 만드는 마지막 단계는 이 앱에서 가장 잘 만든 화면이다.** 검은 카드에 메뉴·수량·합계만 있고 버튼은 `📋 주문서 복사` 하나다. 카페에 그대로 내밀 수 있다. 여기는 손댈 것이 없다.

문제는 **거기 도달하기 전**이다. 세 가지가 본질을 밀어낸다.

| # | 무엇이 | 실측 |
|---|---|---|
| **C-1** | 주문하려면 먼저 "제가 사겠습니다"를 눌러야 한다 | 섬김 시작 전 참가자 화면의 조작 가능 요소 = `⚙️` 와 `☕ 오늘 섬기기 시작` **둘뿐**, 확인창 **없음** |
| **C-2** | 주문 화면에서 본질은 **26%** | 전체 1,337px 중 내 주문+메뉴+안 마실게요 = 351px. 게임·현황·로스터 = 702px(**53%**) |
| **C-3** | 화면 글자의 **51%가 13px 이하**이고 사용자가 키울 수 없다 | 본문 요소 59개 중 30개 ≤13px · 16px 이상은 12개(20%) · 루트를 130%로 키워도 13px 그대로 |

색 대비(WCAG AA 미달 **0건**)와 탭 타겟(44px 미만 **0건**)은 이미 좋다. 이 검토는 "덜 다듬어졌다"가 아니라 **"본질이 아닌 것이 본질 앞에 서 있다"**는 지적이다.

**검증 방법**: 로컬 사본(백엔드 쓰기 없음)을 헤드리스 Chrome 으로 띄워 390×844·320×568 에서 실측. 아래 숫자는 전부 계산이 아니라 렌더된 화면에서 잰 값이다.

---

## C-1 · 주문하려는 사람에게 앱이 처음 묻는 것이 "누가 살래요?"다 (치명)

**재현** — 아직 아무도 섬김을 시작하지 않은 셀에 링크로 들어가 이름을 고른다.

```
화면 글:  오늘은 아직 / 섬기기로 하신 분이 없어요
          오늘 음료로 섬겨주실 분은 아래 [오늘 섬기기 시작]을 눌러주세요
누를 수 있는 것:  ⚙️ ,  ☕ 오늘 섬기기 시작      ← 이 둘이 전부다
```

이 상태에서 **메뉴를 고를 방법이 없다.** 화면에서 유일하게 크고 진한 버튼(`.btn-brand`, 전폭)이 "내가 오늘 산다"는 선언이고, 누르면 **확인창 없이 즉시** 호스트가 된다(`bind("startserv") → doStartServing()`).

**왜 치명인가**
- 대부분의 사람이 이 링크를 여는 이유는 *"뭐 드실래요?"* 라는 물음에 답하기 위해서다. 그런데 앱은 답할 칸을 주지 않고 먼저 지불 의사를 묻는다.
- 잘못 누른 결과가 되돌리기 어렵다. 사회적·금전적 약속이고, 취소하려면 `섬김 취소`라는 또 다른 앱 용어를 찾아야 한다.
- **노인 관점에서 특히 나쁘다.** 화면에 진한 버튼이 하나뿐이면 그것이 "다음"이라고 읽힌다. 버튼 글자 `오늘 섬기기 시작`은 "시작"이라는 중립적인 말로 끝나서, 무엇이 시작되는지가 아니라 *진행하려면 눌러라*로 읽힐 여지가 크다.
- 실수의 비용이 비대칭이다. 안 눌러서 생기는 일(아무 일도 안 일어남)보다 잘못 눌러서 생기는 일(내가 6명 음료값을 낸다)이 훨씬 크다. 이런 자리에는 확인이 있어야 한다.

**권고 (우선순위 1)**
1. **확인창을 붙인다.** `안 마실게요`(되돌릴 수 있는 선택)에는 확인창이 있는데, 되돌리기 어려운 이 버튼에는 없다. 우선순위가 뒤집혀 있다.
2. **버튼 글자에 결과를 넣는다.** `☕ 오늘 섬기기 시작` → `☕ 오늘은 제가 살게요` 처럼, 누른 뒤 무슨 일이 생기는지가 버튼 안에 있어야 한다.
3. **주문을 먼저 받을 수 있게 한다(구조 변경).** 섬김이 없어도 메뉴를 골라 두게 하고, 사는 사람은 나중에 정한다. 게임 설계 판단 ②("주문이 모인 뒤에 돌린다")가 이미 같은 방향을 말하고 있으므로 설계 철학과도 맞는다. 지금은 그 원칙이 게임에만 적용돼 있고 주문 자체에는 적용돼 있지 않다.

---

## C-2 · 주문 화면에서 본질이 차지하는 세로 공간은 26%다 (높음)

390×844 화면, 다른 분 3명이 주문한 상태, 나는 아직 안 고른 상태에서 잰 값이다.

| 구간 | 세로 px | 비중 | 본질인가 |
|---|---|---|---|
| 헤더 · 배너 · 탭 | 0 – 284 | 21% | 오리엔테이션 |
| **☕ 내 주문 · 메뉴 선택 · 음료를 안 드신다면** | **284 – 635** | **26%** | **본질** |
| 🎲 게임 | 635 – 738 | 8% | 부속 |
| 👀 현황 KPI(주문·안마심·미응답) | 738 – 867 | 10% | 호스트에게만 |
| 다른 분들 로스터 + 미응답 안내 복사 | 867 – 1,337 | 35% | 호스트에게만 |

**첫 화면(스크롤 없이 보이는 844px) 안의 버튼 10개 중 메뉴 고르기에 속한 것은 4개**(타일 3 + 전체 메뉴판)다. 나머지 6개는 이름 변경·설정·탭 2개·안 마실게요·**게임**이다.

**게임 진입점이 메뉴 그리드 바로 아래, 첫 화면 안에 있다.** 그리고 내가 호스트가 아니면 그 버튼은 `🎲 게임 연습으로 돌려보기` — 눌러도 아무것도 확정되지 않는 **연습**이다. 아무 결과도 만들지 않는 기능이, 본질 바로 다음 자리에 본질과 같은 시각 무게(전폭 흰 카드)로 앉아 있다.

**노인 관점** — 자기 할 일은 "아메리카노 누르기" 하나로 끝났는데, 화면은 아직 절반이 남아 있고 거기에 누를 수 있는 것이 계속 나온다. *아직 뭔가 더 해야 하나?* 를 묻게 만든다. 끝났다는 신호가 없다.

**권고 (우선순위 2)**
1. **게임을 첫 화면에서 뺀다.** 게임은 "누가 살지 아직 안 정했을 때" 쓰는 도구다. 호스트가 이미 있으면 그 화면에서 게임은 할 일이 없으므로, 호스트가 있을 때는 진입점을 감추거나 화면 맨 아래로 내린다. 연습(dry-run) 진입점은 설정 시트로 옮겨도 아무도 손해 보지 않는다.
2. **현황·로스터를 참가자에게는 접어 둔다.** `다른 분들` 470px 은 호스트가 "다 걷혔나"를 보는 화면이지 참가자가 볼 것이 아니다. 참가자에게는 `3명 주문 · 3명 대기` 한 줄이면 충분하고, 펼치고 싶으면 그때 펼치게 한다.
3. **끝났다는 신호를 준다.** 주문을 담은 뒤 상태 카드가 "주문 완료"라고만 말하지 말고, 그 아래를 접어 화면을 짧게 만든다. *더 할 일이 없다*는 것이 화면 길이로 보여야 한다.

---

## C-3 · 글자의 절반이 13px 이하이고, 사용자가 키울 수 없다 (높음)

**분포** (합친 화면의 본문 요소 59개)

| 크기 | 개수 | 무엇 |
|---|---|---|
| 11px | 3 | `데모·공용`, `1번째 섬김` 배지 |
| 12px | 6 | 날짜 줄, 상태 알약(`주문`·`안마심`·`미응답`) |
| 13px | 21 | **섹션 제목**(`메뉴 선택`·`음료를 안 드신다면`), 배너 안내문, 탭 라벨, 이름 칩 |
| 14–15px | 17 | 메뉴 타일 글자, 사람 이름 |
| 16px 이상 | 12 (20%) | 버튼, 제목, KPI 숫자 |

**13px 이하가 30개 = 51%.** 그리고 이 중에 *무엇을 하라는 안내*가 들어 있다 — `메뉴 선택`, `음료를 안 드신다면`, `메뉴를 고르거나, 안 드시면 알려주세요`가 전부 13px 다. 화면에서 가장 큰 글자는 배너의 호스트 이름(24~26px)인데, 그건 **읽어도 할 일이 생기지 않는 정보**다. 크기 순서와 중요도 순서가 어긋나 있다.

**키울 수 없다** — 모든 글자 크기가 `px` 고정(`--fs-xs:11px … --fs-title:18px`)이다. 루트 글꼴을 130%로 올려도 본문은 **13px 그대로**임을 실측했다. 결과적으로 iOS 사파리의 글자 크기 설정(Dynamic Type)과 데스크톱 브라우저의 기본 글꼴 크기 설정이 **이 앱에는 아무 효과가 없다**. 노안이 있는 사용자가 OS 에서 이미 해 둔 조정이 무시된다.

유일한 탈출구는 핀치 줌인데, 이건 열려 있다(`viewport` 에 `user-scalable=no` 가 없다). 잘한 선택이다 — 다만 매번 두 손가락으로 확대해야 한다는 뜻이기도 하다.

**권고 (우선순위 3)**
1. `--fs-*` 를 `rem` 으로 바꾸고 `:root { font-size: 100% }` 를 기준으로 삼는다. 그러면 사용자가 OS·브라우저에서 해 둔 설정이 그대로 들어온다. 값 자체는 안 바꿔도 되므로 회귀 위험이 낮다.
2. **본문 최소치를 올린다.** 지시문(`메뉴를 고르거나…`)과 섹션 제목은 13→15px, 상태 알약은 12→13px. 대신 배너의 장식(🎉·호스트 이름 24px)을 줄여 총 높이를 상쇄한다.
3. 11px 배지(`데모·공용`)는 대부분의 사용자에게 뜻이 없다. 지워서 자리를 만든다.

---

## C-4 · 화면이 앱 내부 용어로 말한다 (중)

화면에 그대로 보이는 말: **섬김 / 섬기기 / N번째 섬김 / 셀 / 연습 / 마감**.

`섬김`은 이 공동체의 언어일 수 있으므로 그대로 두는 판단이 가능하다. 문제는 **앱이 만들어 낸 개념**이다.

- `1번째 섬김` — 사용자가 만든 적 없는 번호다. 카페 주문서 탭 배너에까지 `1번째 섬김 · 카페에 그대로 보여주세요` 로 나온다. 카페 직원에게 보여줄 화면에 우리 앱의 내부 번호가 붙어 있다.
- `게임이 궁금하면 (연습)` — '연습'이 무엇의 반대말인지 화면 안에 없다.
- `섬김 취소` — 하단바 왼쪽. "취소"가 무엇을 취소하는지(내 주문인가, 오늘 모임인가) 모호하다.

**권고** — `N번째 섬김` 은 하루 두 번 이상 주문할 때만 의미가 있으므로 **2회차부터만** 표시한다. 첫 판에서는 없는 편이 항상 낫다. `섬김 취소` → `제가 안 살게요` 처럼 결과로 적는다.

---

## C-5 · 주문 시트가 한 번에 17개 칩을 편다 (중)

아메리카노를 누르면 열리는 시트: **옵션 그룹 6개**(온도·사이즈·샷·물·얼음·메모), **칩 17개**, 시트 전체 높이 807px(보이는 높이 743px).

다행히 **`[이걸로 주문]` 버튼은 스크롤 없이 보인다** — 기본값 그대로 두 번 탭이면 주문이 끝난다는 뜻이고, 이건 잘 설계돼 있다. 실제 최단 경로는 **탭 2회**(타일 → 이걸로 주문)로 짧다.

문제는 그걸 **모른다**는 것이다. 17개 칩이 펼쳐진 화면을 보면 "이걸 다 골라야 하나"로 읽힌다. 노인 사용자가 여기서 멈추거나, 하나씩 다 눌러 보다 원치 않는 옵션을 켤 가능성이 크다.

**권고** — 시트 맨 위에 `그냥 [이걸로 주문]을 누르셔도 됩니다` 한 줄을 넣거나, 자주 안 쓰는 그룹(물·얼음·사이즈)을 `옵션 더보기`로 접는다. 접는 쪽이 시트 높이도 함께 줄인다.

---

## C-6 · 되돌리기 쉬운 일에는 확인창이 있고, 어려운 일에는 없다 (중)

| 동작 | 되돌리기 | 확인창 |
|---|---|---|
| `🙅 안 마실게요` | 쉽다(아래 메뉴 그리드가 그대로 열려 있다) | **있다** |
| `☕ 오늘 섬기기 시작` (내가 산다) | 어렵다 | **없다** |

`안 마실게요` 의 확인창은 근거가 코드 주석에 적혀 있다("메뉴 타일 바로 아래 전폭이라 스치는 자리"). 타당하다. 다만 **그 근거는 C-1 의 버튼에 훨씬 더 강하게 적용된다.** 지금은 둘의 우선순위가 뒤집혀 있다.

**권고** — C-1 에 확인창을 붙이는 것이 먼저다. 그러고 나면 `안 마실게요` 의 확인창은 빼도 된다(되돌릴 수 있고, 확인창 하나를 줄이면 흔한 길이 한 번 짧아진다).

---

## C-7 · 이름 고르기가 2단계다 (소)

이름 행을 탭 → `✓ 선택됨` → 화면 아래 `🙋 '한사랑님'으로 시작하기` 를 다시 탭.

오탭 방지 장치이므로 근거가 있다. 다만 **이 화면은 잘못 눌러도 손해가 없다**(다시 고르면 된다). C-1 과 정반대로, 안전한 자리에 단계가 하나 더 있다.

**권고** — 확인 버튼을 없애고 바로 들어가거나, 최소한 선택한 이름 행 **바로 옆**에 확인 버튼을 두어 시선이 화면 아래까지 내려가지 않게 한다.

---

## 잘 되어 있는 것 (건드리지 말 것)

1. **카페 주문서 화면** — 검은 카드, 큰 글자, `합계 N잔`, 복사 버튼 하나. 본질의 종착점이 가장 단순하다.
2. **색 대비** — WCAG AA 기준 미달 **0건**. 보조문구까지 전부 통과했다.
3. **탭 타겟** — 44px 미만 **0건**(v10.10 에서 헤더 이름 칩까지 정리).
4. **주문 최단 경로 2탭** — 기본값으로 바로 담을 수 있다.
5. **핀치 줌 허용** — 확대를 막지 않았다.
6. **`[나도]` 버튼** — 남의 주문을 한 번에 복사하는 것은 이 앱의 본질에 정확히 부합하는 기능이다. 노인에게 특히 유효하다(고르지 않고 따라가면 된다).

---

## 권고 우선순위

| 순위 | 항목 | 크기 | 본질에 대한 효과 |
|---|---|---|---|
| 1 | **C-1** `오늘 섬기기 시작`에 확인창 + 버튼 글자를 결과로 | 작음 | 잘못 누르면 돈을 내게 되는 유일한 자리를 막는다 |
| 2 | **C-2** 게임 진입점을 첫 화면에서 내리고, 참가자에게 로스터를 접는다 | 중간 | 본질 비중 26% → 50%+ |
| 3 | **C-3** `--fs-*` 를 rem 으로 · 지시문 13→15px | 중간 | OS 글자 크기 설정이 살아난다 |
| 4 | **C-1-③** 섬김 없이도 주문을 먼저 받는다 | 큼(구조) | "누가 살래?"보다 "뭐 마실래?"를 먼저 묻는다 |
| 5 | **C-5** 옵션 접기 + "그냥 눌러도 됩니다" 한 줄 | 작음 | 시트에서 멈추는 것을 막는다 |
| 6 | **C-4** `N번째 섬김` 을 2회차부터만 | 작음 | 카페에 보여줄 화면에서 내부 용어가 빠진다 |
| 7 | **C-6 · C-7** 확인창 위치를 위험도 순으로 재배치 | 작음 | 흔한 길이 짧아진다 |

1·5·6·7 은 각각 한두 줄 수정이고 본질을 건드리지 않는다. 2·3 은 레이아웃 변경이라 회귀 테스트가 필요하다(QA-TESTCASES.md 의 B2·B4·I1 이 가드다). 4 는 데이터 모델까지 닿으므로 별도 설계가 필요하다.

---

## 이 검토가 보지 않은 것

- **실기기 노인 사용자 관찰** — 위 내용은 전부 화면 계측과 구조 분석이다. "7.1px 사다리 라벨이 읽히는가"(QA U-2) 같은 판독 문제는 사람이 봐야 한다.
- **음성 안내(스크린리더) 흐름** — `aria-*` 는 붙어 있으나 읽히는 순서를 검증하지 않았다.
- **관리자 대시보드** — Supabase RPC 가 필요해 로컬에서 렌더되지 않는다.

---

# 대응 계획 (ui-ux-pro-max 대조 · v10.10 기준)

검토 레포트의 C-1~C-7 을 UI/UX 규칙 데이터베이스에 대조해 **무엇을 근거로, 어디를, 어떻게** 고칠지로 옮긴 것이다. 스택은 프레임워크 없는 단일 HTML(바닐라 CSS/JS)이라 `html-tailwind` 지침을 개념으로만 참고했다.

## 0. 데이터베이스가 지목한 이 앱의 성격

`--design-system` 이 이 제품 유형에 돌려준 스타일은 **Accessible & Ethical** 이다.

> 키워드: High contrast, **large text (16px+)**, keyboard navigation, screen reader friendly, WCAG compliant, focus state
> 패턴: **Minimal Single Column** — Single CTA focus. Large typography. **No nav clutter.**
> 피할 것: **Small text + Complex navigation**

검토 레포트가 손으로 찾은 C-2(화면의 53%가 부속)·C-3(글자 51%가 13px 이하)이 이 스타일의 **피할 것 두 줄과 정확히 일치한다.** 방향이 맞다는 뜻이므로 아래 대응은 전부 이 스타일로 수렴시키는 작업이다.

## 1. 근거 대조표

| 검토 항목 | DB 규칙 | 심각도 | 지금 상태 |
|---|---|---|---|
| **C-1** 섬기기 시작에 확인창 없음 | Interaction / *Confirmation Dialogs* — "Confirm before delete/irreversible actions" | **High** | 위반 |
| **C-2** 본질 26% · 게임이 첫 화면 | Style / *Minimal Single Column* — "No nav clutter", 피할 것 "Complex navigation" | **High** | 위반 |
| **C-3** 본문 13px · rem 아님 | Responsive / *Readable Font Size* — "**Minimum 16px body text on mobile**" | **High** | 위반 |
| **C-5** 옵션 17칩 일시 노출 | Forms / *Progressive disclosure* (SKILL 우선순위 8) | Medium | 위반 |
| **C-6·C-7** 확인창 위치가 위험도와 반대 | 위 *Confirmation Dialogs* 의 이면 | Medium | 부분 위반 |
| 색 대비 | Typography / *Contrast Readability* — "body text needs good contrast" | High | **통과**(AA 미달 0건) |
| 탭 타겟 | Interaction / *Touch Target Minimum* 44×44 | Critical | **통과**(v10.10) |
| 뒤로가기 | Navigation / *back-behavior* | High | **통과**(QA G1·G2·G3) |
| 동작 줄이기 | Animation / *prefers-reduced-motion* | Medium | **통과**(QA F14) |
| 폼 라벨 | Accessibility / *Form Labels* — placeholder-only 금지 | High | **통과**(`aria-labelledby` 로 연결됨) |

## 2. 스킬 패스에서 새로 나온 것

| # | 발견 | DB 규칙 | 실측 |
|---|---|---|---|
| **D-1** | 화면에 제목이 `h1`·`h2` **둘뿐**이다. `☕ 내 주문`·`👀 현황`·`메뉴 선택` 은 전부 `div` 라, 스크린리더로 구역을 건너뛸 수 없다 | Accessibility / *Heading Hierarchy* — "Screen readers use headings for navigation" | Medium |
| **D-2** | 본문 줄간격이 **1.4** 인 곳이 있다 | Typography / *Line Height* — "Use 1.5–1.75 for body text" | Medium |
| **D-3** | `h1` 안에 배지가 들어 있어 페이지 제목이 `☕ ㅇ-ㅇ셀데모·공용1번째 섬김` 으로 읽힌다 | 위 *Heading Hierarchy* | Low |
| **D-4** | 이모지만으로 뜻을 전하는 컨트롤은 `⚙️` **하나뿐**이고 `aria-label="설정"` 이 붙어 있다 | Icons / *No Emoji as Structural Icons* | Low — §5 참고 |

## 3. 대응책 — 무엇을 어디에

### 대응 1 · `오늘 섬기기 시작`에 확인창 + 결과를 말하는 문구 〔C-1〕
- **어디** — `renderBottom()` 의 `id="startserv"` 버튼과 그 배선 `bind("startserv", …)`
- **어떻게** — 이미 있는 `askConfirm()` 을 그대로 쓴다. 새 컴포넌트를 만들지 않는다.
  ```
  "오늘 음료를 사기로 하시는 거죠?\n주문이 모이면 카페에 낼 주문서를 만들어 드려요."
  ok: "네, 제가 살게요"
  ```
- **버튼 글자** — `☕ 오늘 섬기기 시작` → `☕ 오늘은 제가 살게요`. "시작"은 중립어라 *진행하려면 눌러라*로 읽히고, 그게 C-1 의 핵심이다.
- **위험** — 없음. 한 줄 추가 + 문자열 하나. QA **G3**(확인창 뒤로가기)이 이미 가드다.

### 대응 2 · 게임을 첫 화면에서 내린다 〔C-2〕
- **어디** — `renderOpen()` 의 `gameHtml` 삽입 위치(현재 메뉴 그리드 바로 아래, y≈635)
- **어떻게** — 두 단계로 나눈다.
  1. **호스트가 이미 있으면 진입점을 숨긴다.** 게임은 "누가 살지 안 정했을 때" 쓰는 도구이므로, 정해진 화면에서는 할 일이 없다. 지금은 이 경우에 `🎲 게임 연습으로 돌려보기`(아무것도 확정하지 않는 연습)가 본질 바로 다음 자리를 차지한다.
  2. **연습 진입점은 `⚙️ 설정` 시트로 옮긴다.** 궁금한 사람은 거기서 열면 된다. 아무도 잃는 것이 없다.
- **지켜야 할 것** — 호스트가 **없는** 화면에서는 지금 자리를 유지한다. 그 화면은 "누가 섬길까"가 본론이고, 과거 회귀(G9 "기본 탭에 없어서 아무도 못 봤다")가 정확히 이 진입점을 아래로 내렸을 때 났다.
- **위험** — 중간. QA **B2**(합친 화면 순서)·**E1**(진입점 1개)이 가드이고 둘 다 갱신이 필요하다.

### 대응 3 · 참가자에게 로스터를 접는다 〔C-2〕
- **어디** — `renderOpen()` 의 `dashPeopleHtml()`(y 867–1,337, **전체의 35%**)
- **어떻게** — 내가 호스트가 아니면 요약 한 줄(`3명 주문 · 3명 대기`)만 두고 `자세히 보기`로 편다. 호스트에게는 지금대로 펼친 채 둔다 — 걷혔는지 확인하는 것이 호스트의 일이다.
- **효과** — 참가자 화면이 1,337 → 약 900px. 본질 비중 26% → 약 39%. 대응 2 와 합치면 **약 44%**.
- **위험** — 중간. QA **B6**(로스터에 내가 없다)·**B7**(`[나도]`)이 가드. `[나도]` 는 본질에 기여하는 기능이므로 접힌 안에 숨기지 말고 요약 줄에 살려 둔다.

### 대응 4 · 글자 크기를 rem 으로 바꾸고 지시문을 키운다 〔C-3〕
- **어디** — `:root` 의 `--fs-xs … --fs-title`
- **어떻게** — 두 가지를 한 번에 하지 않는다.
  1. **단위만 바꾼다.** `--fs-md: 13px` → `--fs-md: 0.8125rem` 식으로 값은 그대로. 이것만으로 iOS·브라우저의 글자 크기 설정이 살아난다. 화면은 한 픽셀도 안 바뀌므로 회귀 위험이 사실상 없다.
  2. **그 다음 값을 올린다.** 지시문·섹션 제목 13 → 15px, 상태 알약 12 → 13px. DB 권고는 16px 이지만 이 앱은 320px 화면을 지원해야 하므로 **15px 를 현실적 목표**로 잡고, 늘어난 높이는 배너 장식(🎉 + 호스트 이름 24~26px)을 줄여 상쇄한다. 지금은 *읽어도 할 일이 안 생기는 정보*가 화면에서 가장 크다.
- **위험** — 2단계는 중간(줄바꿈·넘침). QA **B3**(가로 스크롤 0)·**B4**(머리말 높이 고정)·**I8**(긴 이름 한 줄)이 가드.

### 대응 5 · 주문 시트의 옵션을 접는다 〔C-5〕
- **어디** — `openOrderSheet()` 의 `draw()`
- **어떻게** — 온도는 펼친 채 두고(거의 항상 고른다), 나머지(사이즈·샷·물·얼음·우유)는 `옵션 더보기`로 접는다. 시트 맨 위에 `그대로 [이걸로 주문]을 누르셔도 됩니다` 한 줄.
- **근거** — 최단 경로는 이미 **탭 2회**이고 확정 버튼도 스크롤 없이 보인다. 즉 **동작은 옳고 표시가 그걸 감추고 있다.** 17칩을 펼쳐 보이면 "다 골라야 하나"로 읽힌다.
- **위험** — 낮음. QA **C1·C6**(시트가 값으로 채워져 열림)이 가드.

### 대응 6 · 확인창을 위험도 순으로 재배치 〔C-6·C-7〕
- `안 마실게요` 확인창은 **대응 1 이 끝난 뒤에** 뺀다. 되돌릴 수 있는 선택이고, 흔한 길이 한 번 짧아진다.
- 이름 고르기 2단계는 확인 버튼을 화면 아래가 아니라 **고른 행 옆**으로 옮긴다. 시선이 이동하지 않는다.
- **순서가 중요하다** — 지금 확인창을 먼저 빼면 확인 없는 위험한 자리만 남는다.

### 대응 7 · 구역에 제목 태그를 준다 〔D-1·D-2·D-3〕
- `.sec-div`(`☕ 내 주문`·`👀 현황`) → `<h2>`, `.label`(`메뉴 선택` 등) → `<h3>`. 보이는 모양은 그대로 두고 태그만 바꾼다.
- `h1` 안의 배지(`데모·공용`·`N번째 섬김`)를 `h1` **밖으로** 빼 페이지 제목이 셀 이름만 읽히게 한다.
- 본문 `line-height` 1.4 → 1.5.
- **위험** — 낮음. CSS 선택자를 클래스 기준으로 유지하면 모양이 안 바뀐다.

## 4. 실행 순서

| 단계 | 항목 | 크기 | 회귀 위험 |
|---|---|---|---|
| **1차 — 한두 줄짜리** | 대응 1(확인창·문구) · 대응 5의 안내 한 줄 · 대응 7(제목 태그·줄간격) · `N번째 섬김` 2회차부터 | 작음 | 낮음 |
| **2차 — 단위 교체** | 대응 4-①(px → rem, 값 동결) | 작음 | 거의 없음 |
| **3차 — 레이아웃** | 대응 2(게임 내리기) · 대응 3(로스터 접기) · 대응 4-②(값 올리기) · 대응 5(옵션 접기) | 중간 | 중간 — QA B2·B3·B4·E1 갱신 필요 |
| **4차 — 구조** | 섬김 없이도 주문을 먼저 받는다(C-1-③) | 큼 | 데이터 모델까지 닿음 — 별도 설계 |

1차·2차는 **화면 배치를 하나도 안 바꾸면서** DB 기준 High 위반 두 개(확인창·글자 크기 조절 가능성)를 닫는다. 여기서 끊어도 남는 것이 있다.

## 5. 데이터베이스 권고 중 **따르지 않기로** 한 것

기본값을 그대로 적용하면 이 앱이 나빠지는 항목이다. 근거를 남긴다.

| 권고 | 따르지 않는 이유 |
|---|---|
| 팔레트를 `#171717 / #A16207` 로 | 이 앱의 베이지·브라운은 "카페"라는 제품 은유 그 자체이고, **이미 WCAG AA 를 전부 통과했다**(미달 0건). 통과한 팔레트를 통과한 다른 팔레트로 바꿀 이유가 없다. |
| 본문 폰트를 Plus Jakarta Sans 로 | ① 한글 글립이 없어 결국 시스템 폰트로 폴백한다 ② 단일 HTML 파일에 웹폰트를 얹으면 첫 화면이 늦어진다 ③ 지금 스택(`-apple-system` → `Apple SD Gothic Neo` → `Malgun Gothic`)이 한글에 가장 안전하다. |
| **이모지를 전부 SVG 아이콘으로** | 이 앱의 이모지는 **대부분 텍스트와 짝을 이룬다**(`☕ 아메리카노`, `🙅 안 마실게요`) — 규칙이 겨냥하는 "아이콘 대용"이 아니라 장식이다. 실측으로 이모지만으로 뜻을 전하는 컨트롤은 `⚙️` 하나였고 `aria-label` 도 붙어 있다. 게다가 메뉴 이모지는 카페 메뉴 데이터의 일부라 아이콘 세트로 대체할 수 없다. **`⚙️` 하나만** 검토 대상으로 남긴다. 또한 이 규칙(`pro-rules.md`)은 스스로 "네이티브/모바일 앱 UI 대상"이라고 범위를 밝히고 있다. |
| `cursor: pointer` 를 모든 클릭 요소에 | 이 앱은 사실상 폰 전용이고 커서가 없다. 우선순위를 여기 쓸 이유가 없다(원하면 한 줄이므로 언제든). |
| Minimal Single Column 의 "Single CTA" | 이 화면은 랜딩이 아니라 도구다. 다만 **"주된 행동은 한 화면에 하나"** 라는 정신은 대응 2·3 이 이미 따르고 있다. |


---

# 대응 결과 (v10.11 · 4단계 전부 적용)

계획한 1~4차를 모두 반영했다. 아래는 **같은 방법으로 다시 잰 값**이다(390×844, 다른 분 3명 주문, 나는 아직 안 고른 참가자 시점).

## 숫자로 본 전후

| 항목 | 검토 시점(v10.09) | 지금(v10.11) |
|---|---|---|
| 참가자 화면 전체 높이 | 1,337px (뷰포트의 **1.58배**) | **844px (1.00배 — 스크롤 없이 한 화면)** |
| 본질(내 주문·메뉴·안 마실게요)이 차지하는 비중 | **26%** | **44%** (369/844) |
| 화면의 누를 수 있는 요소 | 14개 | **12개** |
| 13px 이하 본문 요소 | 30/59 = **51%** | **22/50 = 44%** |
| 사용자 글자 크기 설정 반영 | 안 됨(px 고정) | **됨**(루트 130% → 본문 13→20px) |
| 되돌리기 어려운 동작의 확인창 | 없음 | **있음** |
| 색 대비 AA 미달 | 0건 | 0건 (유지) |
| 44px 미만 탭 타겟 | 1건 | 0건 (유지) |

1080×2340(CSS 360×780) 실기기 규격에서도 참가자 화면이 **825→780px**로, 스크롤 없이 들어온다.

## 무엇을 했나

**1차 — 화면 배치를 안 건드리는 것들**
- `☕ 오늘 섬기기 시작` → `☕ 제가 살게요` + **확인창**(`오늘 음료를 사기로 하시는 거죠?` / `네, 제가 살게요`). 취소하면 호스트가 안 된다.
- 주문 시트 맨 위에 `고르실 게 없으면 그대로 [이걸로 주문]을 누르시면 돼요.`
- `.sec-div`→`<h2>`, `.label`→`<h3>`. 클래스가 UA 제목 스타일을 이미 전부 덮으므로 **보이는 모양은 그대로**다. (리셋 규칙은 넣지 않았다 — `h2.sec-div{all:unset}` 은 specificity 0,1,1 로 `.sec-div` 0,1,0 을 이겨 구분선을 통째로 지운다.)
- `h1` 안의 배지를 밖으로 빼 페이지 제목이 셀 이름만 읽히게 했다.
- 본문 `line-height` 1.4 → 1.5.
- `N번째 섬김` 을 **2회차부터만** — 카페 주문서 화면에서 내부 번호가 빠졌다.
- `섬김 취소` 확인창 문구를 결과로(`오늘은 안 사시는 걸로 할까요?` / `네, 안 살게요`).

**2차 — 단위만 교체**
- `--fs-*` 를 px → rem 으로, 1rem=16px 기준 **같은 픽셀값**으로 환산. 화면은 한 픽셀도 안 바뀌고 OS·브라우저 글자 크기 설정이 살아났다.

**3차 — 레이아웃**
- 게임 진입점을 **결정할 수 있는 사람에게만**(호스트이거나 호스트가 없을 때) 남겼다. 아무것도 확정하지 않는 `게임 연습으로 돌려보기` 는 ⚙️ 설정의 `🎲 게임 구경하기 (연습)` 로 옮겼다 — 기능은 그대로다.
- 참가자에게 `다른 분들` 을 `<details>` 로 접었다(요약 `3명 주문 · 2명 대기`). `[나도]` 는 펼치면 그대로 있다. 호스트 화면은 예전대로 펼쳐진다.
- 참가자 화면에서 KPI 3칸을 뺐다 — 접힌 요약이 같은 숫자를 이미 말한다. 그러고 나니 구분선 아래 제목이 두 겹으로 남아, 참가자에게는 `👀 다른 분들` **한 층**으로 합쳤다.
- `📢 미응답 안내 문구 복사` 를 게임 진입점과 같은 규칙으로 좁혔다(재촉은 사는 사람의 일).
- 지시문·구역 제목 13→15px, 상태 알약 12→13px.
- 주문 시트의 옵션 17칩을 `옵션 고르기` 서랍으로 접었다. 온도는 밖에 둔다. 이미 고른 옵션이 있으면 펼친 채로 열리고, 접힌 요약(`1개 선택됨`)은 칩을 누를 때마다 그 자리에서 갱신된다(칩은 일부러 리렌더하지 않는 설계라 `refreshSum()` 에 물렸다).

**4차 — 구조**
- **주문을 먼저 받는다.** 섬김 전 화면의 갈림길이 둘이 됐다 — `📝 메뉴 고르기`(진한 버튼)와 `☕ 제가 살게요`(흰 버튼). 무게를 뒤집어, 대부분의 사람이 할 일이 주(主)가 됐다.
- 호스트 없는 열린 섬김을 만드는 `open_serving` RPC 를 `db-migration-order-first.sql` 로 추가했다. **새 상태를 들이지 않는다** — `host_id IS NULL` 인 열린 섬김은 `unclaim_serving` 이 이미 만들고 `claim_host` 가 이어받는, 앱이 오래 다뤄 온 상태다. 없던 것은 그것을 '처음부터' 만드는 길뿐이었다(`start_serving` 은 `room_has_member(p_me)` 를 요구해 null 호스트로 못 부른다).
- **마이그레이션을 안 걸어도 앱은 그대로 동작한다.** `PGRST202`(함수 없음)를 만나면 `orderFirstRPC=false` 로 내려 예전처럼 한 갈래만 그린다. 안내 문구도 그에 맞춰 갈린다.

## 검증

- 순수 로직 `node --test` **44/44**
- 화면·플로우 **75/75** (검토 대응 회귀 11건 신규 — K1~K11)
- 콘솔 오류 0 · 390/360/320px 가로 넘침 0

## 남긴 것

- **U-2**(사다리 10명 라벨 7.1px) — 처방이 둘로 갈리고 어느 쪽인지는 실기기 판독에 달렸다. QA **M-14**.
- **U-3**(320px 참가자 시트 일괄 버튼 2줄) — 동작 지장 없음.
- `open_serving` 마이그레이션은 **실 DB 에 적용하지 않았다.** Supabase SQL Editor 에서 `db-migration-order-first.sql` 을 실행해야 4차가 켜진다. 적용 전에는 폴백으로 예전 화면이 나온다.


---

# 후속 — 주문 먼저 받기가 만든 구멍을 막다 (v10.12)

사용자 지적: *"메뉴부터 고르게 하면 누가 섬기는지 누락될 확률이 있는 거 아냐?"* — 맞았다. 실측으로 확인하고 셋을 고쳤다.

## 무엇이 뚫려 있었나 (실측)

호스트 없는 섬김에 주문 4건이 모인 상태를 만들어 봤다.

| 확인 | 결과 |
|---|---|
| 배너 | `오늘 음료, 함께 섬겨요` — **섬기실 분이 없다는 사실을 말하지 않음** |
| 카페 주문서 | **호스트 없이도 그대로 뽑힘** — 복사해 다녀오면 `host_id = null` 로 남아 대시보드 섬김 랭킹에서 누락 |
| 마감 버튼 | **없음**(`amHost` 분기에만 있었다) — 섬김을 닫을 수 없음 |
| 다음 섬김 | **거부**(`startServing` → 0). `sessions_one_open` 때문에 그날 2번째 섬김을 못 엶 |

두 번째가 특히 나빴다. 이 상태는 원래 `섬김 취소`를 눌러야만 도달하는 희귀 상태였는데, 주문 먼저 받기로 **기본 진입 경로**가 되면서 빈도가 확 올라갔다 — 사실상 스스로 만든 회귀다.

## 처방

- **P1** 호스트 없는 섬김의 배너를 사실대로 — `아직 섬기실 분이 안 정해졌어요`. 합친 화면과 카페 주문서 탭 두 곳 모두.
- **P2** 카페 주문서에 경고 한 줄. **막지는 않는다** — 먼저 다녀오고 나중에 정하는 순서가 실제로 있다.
- **P3** **마감 = 섬기신 분 확정 + 닫기.** 호스트 없는 섬김의 하단바에 `[✅ 주문 마감]`(주문 있음) 또는 `[이 섬김 접기]`(주문 0건)를 넣고, 마감을 누르면 `☕ 누가 섬기셨어요?` 시트에서 고르게 한다. 고르면 `claim → close` 순으로 기록하고 닫는다.

P3 가 본질이다. P1·P2 는 알려만 주지만 P3 는 **마감이 유일한 종료 지점**이라는 사실을 이용해 누락 경로 자체를 없앤다. `claim → close` 순서인 이유: 닫힌 섬김은 `claim_host` 가 안 잡으므로, 닫아 놓고 호스트를 못 채우는 상태를 만들지 않는다.

부수로 하단바의 `🎲 게임으로 정하기`를 뺐다 — 본문 `#gamepick` 이 같은 조건으로 이미 첫 화면에 있어 같은 자리를 두 번 차지하고 있었다. 그 자리를 출구에 줬고, 결과적으로 하단바가 호스트 유무와 무관하게 **두 칸**으로 고정된다(N2).

## 용어

사용자 지시로 `사다` 계열을 전부 `섬기다`로 되돌렸다 — `제가 살게요`→`제가 섬길게요`, `누가 사셨어요?`→`누가 섬기셨어요?`, `사실 분`→`섬기실 분`, `이 판`→`이 섬김`. 코드 주석도 같은 말을 쓰게 맞췄다. 검토 C-4 에서 "`섬김`은 이 공동체의 언어라 그대로 두는 판단이 가능하다"고 적었던 대로, 공동체의 말이 우선한다.

## 검증

순수 로직 **44/44** · 화면·플로우 **80/80**(K12~K16 신규) · 콘솔 오류 0.
